# Zoho OAuth 2.0 credentials
# Register your app at https://api-console.zoho.com/
ZOHO_CLIENT_ID=your_client_id_here
ZOHO_CLIENT_SECRET=your_client_secret_here
ZOHO_REDIRECT_URI=http://localhost:3001/auth/zoho/callback
ZOHO_ORG_ID=your_organization_id_here

# Zoho data center — adjust if your account is on a different region
# Options: .com (US), .ca (Canada), .eu (EU), .in (India), .com.au (AU)
ZOHO_DOMAIN=.com

# Redis (optional — server works without it, caching is just disabled)
REDIS_URL=redis://localhost:6379

# Server
PORT=3001

# Frontend origin (for CORS)
FRONTEND_URL=http://localhost:3000
