---
phase: 74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split
plan: 05
type: execute
wave: 2
depends_on: ["74-04"]
files_modified:
  - products/ferment-in-store.html
  - beer.html
  - index.html
autonomous: false
requirements: [D-10, D-11, D-12, D-13]

must_haves:
  truths:
    - "D-10: products/ferment-in-store.html is a neutral informational hub — it no longer hosts a catalogue grid, and instead routes to Wine, Beer and Ingredients & Supplies"
    - "D-10: the hub's wine section survives verbatim, including the h2 'Make Your Own Wine in Squamish' and the full landing-copy detail block, so only one page competes for those ranking terms and no canonical tag is needed"
    - "D-11: beer.html keeps its existing launch copy — hero, What It Is, How It Works, What's Included, waitlist and FAQ are all still present and in their existing order"
    - "D-11: the beer catalogue is inserted BETWEEN What's Included and the waitlist section, so the booked-ahead framing reaches a visitor before any product does"
    - "D-13: beer.html carries only the Style and ABV filter rows, not wine's body/oak/sweetness set"
    - "D-12: the homepage hero CTA no longer lands a kit-shopping visitor on a page with no buy grid"
  artifacts:
    - path: "products/ferment-in-store.html"
      provides: "the neutral hub with three category entry points"
      contains: "hub-categories"
    - path: "beer.html"
      provides: "the beer launch page with a catalogue block below its launch copy"
      contains: "id=\"catalog-blocks\""
  key_links:
    - from: "products/ferment-in-store.html"
      to: "wine.html, beer.html, products/ingredients-supplies.html"
      via: "three .hub-category-item entries with .btn-secondary links"
      pattern: "hub-category-item"
    - from: "beer.html #product-catalog"
      to: "js/main.min.js catalogue dispatch"
      via: "body data-page=\"beer\" plus the two-block DOM contract"
      pattern: "id=\"recipe-catalog\""
---

<objective>
Rewrite `products/ferment-in-store.html` into a neutral informational hub (D-10) and append a catalogue block to `beer.html` below its existing launch copy (D-11).

Purpose: the hub is currently both the SEO landing page for winemaking in Squamish AND the only wine catalogue. Plan 74-04 moved the catalogue surface to `/wine`, so the hub keeps the ranking copy and hands off the shopping. Beer is the mirror image: it is a booked-ahead product, so its announcement framing must land before a visitor sees anything resembling a purchasable item — the catalogue is added below the hero, never in place of it.
Output: a hub that routes to three category pages, and a beer page whose narrative order is unchanged with a catalogue inserted into its existing section rhythm.

**Do NOT run `npm run build` in this plan** — the single rebuild and page stamp happens in plan 74-06.
**Do NOT edit `css/styles.css` in this plan** — `.hub-categories`, `.hub-category-item`, `#catalog-blocks` and `.product-grid--compact` were all added by plan 74-04 (wave 1) and already exist.
**Do NOT edit `js/modules/*` in this plan** — the catalogue JS is owned by plans 74-02 and 74-03, and the page dispatch by plan 74-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-CONTEXT.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-RESEARCH.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-PATTERNS.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-04-SUMMARY.md
@CLAUDE.md

<interfaces>
DOM contract the catalogue JS binds to (identical to the one plan 74-04 put on wine.html):

  div.catalog-layout
    div#catalog-blocks
      div#product-catalog
        div.catalog-controls#catalog-controls-kits
          input#catalog-search.catalog-search
          div.catalog-view-toggle  (two button[data-view])
          button#catalog-toggle.catalog-toggle
          div.catalog-collapsible#catalog-collapsible   (one div.catalog-filter-row per filter, keyed by id)
          div.filter-summary.hidden#filter-summary
      div#recipe-catalog
    aside.cart-sidebar#cart-sidebar
    div.cart-drawer-backdrop#cart-drawer-backdrop
    div.cart-drawer#cart-drawer

Beer filter row ids — ONLY these two, per D-13 and the beer branch of the buildFilterRow call list
added in plan 74-02:
  filter-subcategory   ("Style:")
  filter-abv           ("ABV:")
Wine's filter-type / filter-brand / filter-manufacturer / filter-time / filter-body / filter-oak /
filter-sweetness / filter-sale rows must NOT appear on beer.html. buildFilterRow self-suppresses a
row with no data, but the beer call list never asks for them, so an unused container would simply be
dead markup.

CSS classes available (all added by plan 74-04, do not redefine):
  #catalog-blocks           flex:1; min-width:0
  .product-grid--compact    caps a 1-3 card grid at 320px columns
  .hub-categories           display:flex; flex-wrap:wrap; gap:2rem; justify-content:center
  .hub-category-item        flex:1 1 280px; max-width:340px; text-align:center
  .btn-secondary            css/styles.css:809, existing, reused as-is

beer.html current section order (verified 2026-09-01) — D-11 requires this order survive:
  127-139  section.beer-banner.beer-banner--green   (hero; line 134 is the live CTA
                                                     a[href="#waitlist"].btn "Join the Waitlist")
  141-143  div.section-icon divider
  145-150  section.intro       h2 "What It Is"
  152-154  div.section-icon divider
  156-177  section.content     h2 "How It Works"
  179-181  div.section-icon divider
  183-189  section.intro       h2 "What's Included"
  191-193  div.section-icon divider      <-- catalogue is inserted AFTER this divider
  195-209  section.content#waitlist      h2 "Ready to Brew?" + form#beer-waitlist-form
  211-213  div.section-icon divider
  215-225  section.content     (FAQ)
  227-229  div.section-icon divider
  231-235  section.content
  263-266  script block (currently missing js/vendor/fuse.min.js and 17-search-overlay.min.js)

products/ferment-in-store.html current structure (verified 2026-09-01):
  119-126  section.page-header      h1 "Ferment-in-Store"        — keep unchanged
  128-171  section.landing-copy#landing-copy                     — KEEP VERBATIM (D-10)
             h2 "Make Your Own Wine in Squamish"
             button.landing-copy-toggle[aria-controls=landing-copy-detail]
             div.landing-copy-detail#landing-copy-detail
  173-261  section.catalog-section                               — subject of the Task 1 decision
             div#product-catalog
               div.product-tabs#product-tabs  (tab-kits / tab-ingredients)
               div.catalog-controls#catalog-controls-kits  (9 wine filter rows + sort)
             aside.cart-sidebar#cart-sidebar, cart drawer backdrop, cart drawer
  ~266     div[role=status][aria-live=polite].sr-only#filter-status
  294-299  script block
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Owner sign-off on hub catalogue removal and the homepage CTA target</name>
  <decision>Two coupled questions the UI contract explicitly refused to settle: (a) does the hub's entire `#product-catalog` block come out, INCLUDING the "Ingredients &amp; Supplies" tab, and (b) does `index.html`'s hero CTA get repointed off the hub.</decision>
  <context>
`74-UI-SPEC.md` ends with an explicit gate: "Open for owner sign-off before implementation: removal
of the hub's `#product-catalog` block including the 'Ingredients &amp; Supplies' tab (§8). RESEARCH
classifies this as an open question and CONTEXT D-10 is silent on the ingredients tab... Planner must
confirm rather than treat as settled by this contract alone."

(a) Ingredients tab. D-10 moves the wine catalogue GRID to `/wine`, but says nothing about the
second tab living in the same `#product-catalog` block. Removing the block removes both tabs. A
dedicated `products/ingredients-supplies.html` page already exists and is reachable from the nav as
"All Ingredients", so access is preserved by a link-out — but this does remove a live, nav-embedded
shopping surface, which is why it needs a decision rather than an assumption.

(b) Homepage CTA. `74-RESEARCH.md` Open Question 3: `index.html`'s primary hero CTA
("Reserve Your Kit", carrying `data-content="hero-cta"`) points at `products/ferment-in-store.html`.
After this rewrite that page has no buy-capable grid, so the CTA lands a purchase-intent visitor on
an informational page and costs them a second click. This is a one-line href change on the site's
highest-intent link, against 238 live purchasable kits.

Important mechanical caveat for (b), discovered during planning: `index.html`'s hero CTA text is
overwritten at runtime from `content/home.json` by `js/modules/13-init.js` (the same
`[data-content]` mechanism that silently reverted hand-edited banner text in Phase 72-02, recorded in
STATE.md). Only the `href` is safe to change in the HTML; if the CTA's LABEL also needs to change,
`content/home.json` must be edited too. This plan changes the href only.
  </context>
  <options>
    <option id="remove-both-and-repoint">
      <name>Remove the whole #product-catalog block (both tabs) and repoint the homepage CTA to /wine (RECOMMENDED)</name>
      <pros>Delivers a genuinely neutral hub — a hub that still embeds one category's full shopping grid is not neutral; removes a duplicate of products/ingredients-supplies.html; keeps the highest-intent CTA one click from a buy button; matches the UI-SPEC §8 recommendation and the RESEARCH Open Question 2 and 3 recommendations</pros>
      <cons>Removes a live nav-embedded ingredients shopping surface; changes the destination of the site's primary conversion link, so post-launch CTA behaviour should be watched</cons>
    </option>
    <option id="remove-both-keep-cta">
      <name>Remove the whole block, leave the homepage CTA pointing at the hub</name>
      <pros>Smallest blast radius on the conversion path; the hub still routes onward to /wine via a category entry</pros>
      <cons>A "Reserve Your Kit" click lands on a page with nothing to reserve — an extra hop on the highest-intent link in the site</cons>
    </option>
    <option id="keep-ingredients-tab">
      <name>Remove only the kits tab, keep a slimmed ingredients-only catalogue on the hub</name>
      <pros>No live shopping surface is removed</pros>
      <cons>Requires keeping the product-tabs component, the catalogue controls and the cart sidebar on a page whose stated job is now routing; duplicates products/ingredients-supplies.html; leaves 'ferment-in-store' in the catalogue-init gate that plan 74-06 wants to remove, so the JS simplification does not land</cons>
    </option>
  </options>
  <action>
Present the decision below to the developer and pause. Do not edit `products/ferment-in-store.html`
or `index.html` until an option is selected. Before pausing, report two facts gathered from the repo:
the current line range of the `#product-catalog` block in `products/ferment-in-store.html`, and the
current `href` of the `index.html` hero anchor carrying `data-content="hero-cta"`. Record both
sub-decisions separately in the plan SUMMARY, since Task 2 and plan 74-06's gate cleanup each read a
different one.
  </action>
  <read_first>
    - products/ferment-in-store.html (lines 119-261 — the page-header, the landing-copy block to preserve, and the whole `#product-catalog` block under discussion)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§8, and the closing "Open for owner sign-off before implementation" paragraph)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-RESEARCH.md (Open Questions 2 and 3; Pitfall 6 — the homepage CTA)
    - index.html (the hero section — locate the anchor carrying `data-content="hero-cta"` and note its current href)
    - products/ingredients-supplies.html (confirm it is a complete standalone ingredients catalogue, so a link-out genuinely preserves access)
    - .planning/STATE.md (Decisions, entry "Phase 72-02" — content/home.json overwrites [data-content] elements at runtime)
  </read_first>
  <acceptance_criteria>
    - A recorded selection of `remove-both-and-repoint`, `remove-both-keep-cta`, or `keep-ingredients-tab` exists in the plan SUMMARY
    - If `keep-ingredients-tab` is chosen, the SUMMARY records that plan 74-06 must NOT remove `'ferment-in-store'` from the catalogue-init gate in `js/modules/13-init.js`, because the hub would still need `loadProducts()` and `initProductTabs()`
    - The SUMMARY records the homepage-CTA decision separately from the ingredients-tab decision
  </acceptance_criteria>
  <done>The ingredients-tab disposition and the homepage-CTA target are both decided and recorded in the SUMMARY.</done>
  <resume-signal>Select: remove-both-and-repoint, remove-both-keep-cta, or keep-ingredients-tab</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rewrite products/ferment-in-store.html into the neutral hub (D-10)</name>
  <files>products/ferment-in-store.html, index.html</files>
  <read_first>
    - products/ferment-in-store.html (the full file — especially lines 119-126 page-header, 128-171 landing-copy which must survive verbatim, 173-261 the catalogue block being removed, ~266 the `#filter-status` live region, 294-299 the script block)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§8 hub structure top-to-bottom; Copywriting Contract row "Hub — category entry copy" for the three exact teaser strings; Visual Hierarchy — the retained wine section is the hub's focal point, the category links are secondary)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-CONTEXT.md (D-10, including its decision history — the user reopened this rewrite specifically to avoid page duplication)
    - css/styles.css (the `.hub-categories` / `.hub-category-item` rules added by plan 74-04, plus `.btn-secondary` at ~809 — confirm they exist before authoring markup against them)
    - index.html (the hero anchor carrying `data-content="hero-cta"`)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-05-PLAN.md Task 1 outcome as recorded in the working SUMMARY
  </read_first>
  <action>
Apply the Task 1 decision to `products/ferment-in-store.html`. The default path below assumes
`remove-both-and-repoint`; if the owner chose otherwise, follow the selected option and record the
divergence in the SUMMARY.

PRESERVE VERBATIM — do not paraphrase, trim, re-order or re-wrap a single sentence:
  - `section.page-header` with its `h1` reading "Ferment-in-Store". The title is already
    category-neutral; it needs no change.
  - the entire `section.landing-copy#landing-copy` block (lines 128-171), including the
    `h2` reading "Make Your Own Wine in Squamish", every paragraph, the
    `button.landing-copy-toggle` and its `div.landing-copy-detail#landing-copy-detail`. This is
    live ranking copy and D-10 exists specifically to keep it here. Its "Read more" toggle is wired
    by `js/modules/13-init.js`'s `.landing-copy-toggle` handler, which is page-agnostic and keeps
    working.

REMOVE — the entire `section.catalog-section` spanning lines 173-261: the `div#product-catalog`
wrapper, the `div.product-tabs#product-tabs` switcher with its `tab-kits` and `tab-ingredients`
buttons, the `div.catalog-controls#catalog-controls-kits` block with all nine wine filter rows and
the sort select, the `#filter-summary` div, the `noscript` fallback, the
`aside.cart-sidebar#cart-sidebar`, the `div.cart-drawer-backdrop#cart-drawer-backdrop` and the
`div.cart-drawer#cart-drawer`. The hub becomes a routing page with no cart affordance of its own;
the cart lives on `/wine`, which plan 74-04 gave its own sidebar and drawer. Also remove the
now-orphaned `div[role=status][aria-live=polite].sr-only#filter-status` live region, which exists
solely to announce filter results.

ADD in its place — a category entry-points section, sitting between the preserved landing-copy
section and the existing closing `section-icon` divider. Structure: a `section` using the page's
existing section class rhythm, containing `div.container`, containing `div.hub-categories`, holding
exactly three `div.hub-category-item` children. Each item is an `h3` with the category name, one
`p` with the teaser, and one anchor with class `btn-secondary`. Use these exact strings and targets
from the UI-SPEC Copywriting Contract, in this order:
  1. h3 "Wine" — teaser "Wine kits, ready to ferment in-store." — link text "Explore Wine",
     href `../wine.html`
  2. h3 "Beer" — teaser "Small-batch beer, booked ahead." — link text "Explore Beer",
     href `../beer.html`
  3. h3 "Ingredients &amp; Supplies" — teaser
     "Everything you need to brew or make wine at home." — link text "Browse Ingredients",
     href `ingredients-supplies.html`
Note the href prefixes: this file lives under `products/`, so `wine.html` and `beer.html` need the
`../` prefix while `ingredients-supplies.html` is a sibling. There is deliberately NO Cider entry —
cider is out of scope for this phase and has no live page to link to.

Give the section a heading above `div.hub-categories` at `h2` level, so the heading tree runs
h1 (page-header) → h2 ("Make Your Own Wine in Squamish") → h2 (this section) → h3 (each category)
with no skipped level. Add no border, background, shadow or card chrome to the items — this section
is navigational and must not visually impersonate `.product-card`, which visitors associate with a
buyable item.

Leave the head, CSP meta, canonical, schema.org block, GTM tags, header, nav, footer and the script
block at lines 294-299 untouched. Do not remove `js/vendor/fuse.min.js` or
`js/modules/17-search-overlay.min.js` from the script block — the header search overlay is
site-wide and still used.

Then, if Task 1 selected a CTA repoint: in `index.html`, change ONLY the `href` of the hero anchor
carrying `data-content="hero-cta"` from `products/ferment-in-store.html` to `wine.html`. Do not
change its text — `js/modules/13-init.js` overwrites `[data-content]` element text at runtime from
`content/home.json`, so a hand-edited label would silently revert (the Phase 72-02 lesson recorded
in STATE.md). Change nothing else in `index.html`; its nav Wine entry was already added by plan
74-04.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs');var h=fs.readFileSync('products/ferment-in-store.html','utf8');if(h.indexOf('Make Your Own Wine in Squamish')===-1)throw new Error('landing copy lost');if(h.indexOf('id=\"product-catalog\"')!==-1)throw new Error('catalogue block still present');if(h.indexOf('hub-category-item')===-1)throw new Error('hub categories missing');if((h.match(/hub-category-item/g)||[]).length!==3)throw new Error('expected exactly 3 hub category items');console.log('hub OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Make Your Own Wine in Squamish' products/ferment-in-store.html` returns 1
    - `grep -c 'id="landing-copy-detail"' products/ferment-in-store.html` returns 1 — the Read more detail block survives
    - `git diff products/ferment-in-store.html | grep -c '^-.*landing-copy'` returns 0 — no line of the landing-copy block was removed or reflowed
    - `grep -c 'id="product-catalog"' products/ferment-in-store.html` returns 0
    - `grep -c 'id="product-tabs"' products/ferment-in-store.html` returns 0
    - `grep -c 'id="cart-sidebar"' products/ferment-in-store.html` returns 0 and `grep -c 'id="cart-drawer"' products/ferment-in-store.html` returns 0
    - `grep -c 'id="filter-status"' products/ferment-in-store.html` returns 0
    - `grep -c 'hub-category-item' products/ferment-in-store.html` returns 3
    - `grep -c '\.\./wine.html' products/ferment-in-store.html` returns at least 1 and `grep -c 'ingredients-supplies.html' products/ferment-in-store.html` returns at least 1
    - `grep -c 'Cider' products/ferment-in-store.html` returns 0 in the hub-categories section
    - `grep -c 'Content-Security-Policy' products/ferment-in-store.html` returns 1 — the CSP meta was not disturbed
    - If the CTA repoint was selected: `grep -c 'data-content="hero-cta"' index.html` returns 1 and the same line's href is `wine.html`; `git diff index.html | grep -c '^[-+].*Reserve Your Kit'` returns 0 (label untouched)
  </acceptance_criteria>
  <done>The hub keeps its ranking copy and routes to three category pages; it hosts no catalogue, no filters and no cart.</done>
</task>

<task type="auto">
  <name>Task 3: Append the catalogue block to beer.html below its launch copy (D-11, D-13)</name>
  <files>beer.html</files>
  <read_first>
    - beer.html (the full file — the section order at 127-235 so the insertion lands in the right seam, the hero CTA string at line 134, the waitlist section at 195-209, and the script block at 263-266)
    - products/ferment-in-store.html as it stood BEFORE Task 2, or equivalently `wine.html` as written by plan 74-04 (the catalogue-controls markup to copy — `wine.html` is the better source because it already carries the `#catalog-blocks` / `#recipe-catalog` two-block contract and root-relative asset paths)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§9 the insertion contract and its exact seam; §2 the single-card grid fix; §7 the beer filter row set)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-RESEARCH.md (Pitfall 5 — beer.html is missing `js/vendor/fuse.min.js` and `js/modules/17-search-overlay.min.js`)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-CONTEXT.md (D-11 and the `<specifics>` note: "Beer must not read as walk-in")
  </read_first>
  <action>
Insert a catalogue section into `beer.html` without disturbing any existing section.

Insertion point: immediately AFTER the `div.section-icon` divider that currently sits between the
"What's Included" `section.intro` (lines 183-189) and the `section.content#waitlist` (line 195), and
BEFORE that waitlist section. Add a NEW `div.section-icon` divider after the new catalogue section so
the page keeps its existing alternating section-then-divider rhythm. The resulting order is:
What's Included → existing divider → NEW catalogue section → NEW divider → Ready to Brew (waitlist)
→ FAQ. This ordering is D-11's entire point: the announcement framing must reach a visitor before
the products do, because beer is booked-ahead rather than walk-in.

Do not modify, move, re-word or re-order the hero banner (`section.beer-banner.beer-banner--green`
at 127-139, including its `a[href="#waitlist"].btn` reading "Join the Waitlist"), "What It Is",
"How It Works", "What's Included", the waitlist section with `form#beer-waitlist-form`, the FAQ, or
the final content section. D-11 keeps every one of them. Do not touch `#beer-waitlist-form`,
`#beer-waitlist-email` or `#beer-waitlist-confirm` — plan 74-06 fixes their JS wiring and needs them
exactly as they are.

The new catalogue section uses `section.catalog-section` wrapping `div.container` wrapping
`div.catalog-layout`, matching the vertical rhythm of every other section on the page — no special
background tint, no visual container marking it as a bolted-on widget. Inside, reproduce the
two-block DOM contract from `wine.html`: `div#catalog-blocks` containing `div#product-catalog` and
an empty `div#recipe-catalog`, with the cart sidebar, cart-drawer backdrop and cart drawer as direct
children of `div.catalog-layout` outside `#catalog-blocks`.

Inside `#product-catalog`, copy `wine.html`'s `div.catalog-controls#catalog-controls-kits` but with
the BEER filter set only (D-13): keep `input#catalog-search.catalog-search` (change its placeholder
to read "Search beer..."), the `div.catalog-view-toggle` with its two `button[data-view]` controls,
the `button#catalog-toggle.catalog-toggle`, and `div.catalog-collapsible#catalog-collapsible`
containing exactly two rows — `div.catalog-filter-row#filter-subcategory` and
`div.catalog-filter-row#filter-abv` — followed by the sort row with `select#catalog-sort`. Then
`div.filter-summary.hidden#filter-summary` and the `noscript` fallback paragraph. Do NOT include
`filter-type`, `filter-brand`, `filter-manufacturer`, `filter-time`, `filter-body`, `filter-oak`,
`filter-sweetness` or `filter-sale`: the beer branch of the `buildFilterRow` call list added in plan
74-02 never asks for them, so those containers would be permanently empty markup.

For the sort row, drop the `time-asc` / `time-desc` "Production Time" options from the copied
`select#catalog-sort`, since beer.html carries no `filter-time` row and production time is not a
beer kit dimension surfaced on this page. Keep every other option value unchanged so the existing
sort handler continues to match on them.

Add a `div[role=status][aria-live=polite].sr-only#filter-status` live region after the catalogue
section, matching the one `wine.html` carries, so filter result counts are announced to screen
readers.

Add the two missing script tags to the existing script block at lines 263-266, root-relative and in
the same relative order `products/ferment-in-store.html` uses: `js/vendor/fuse.min.js` with `defer`
placed before `js/sheets-config.js`, and `js/modules/17-search-overlay.min.js` with `defer` and a
`?v=` placeholder placed after `js/main.min.js`. Without Fuse the in-page catalogue search silently
degrades to substring matching, inconsistent with `/wine`; without the overlay script the header
search icon is dead on this page. Add no other script and no new third-party origin — `beer.html`'s
existing CSP meta stays exactly as it is.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs');var b=fs.readFileSync('beer.html','utf8');['id=\"catalog-blocks\"','id=\"recipe-catalog\"','id=\"filter-subcategory\"','id=\"filter-abv\"','js/vendor/fuse.min.js','17-search-overlay.min.js'].forEach(function(s){if(b.indexOf(s)===-1)throw new Error('missing '+s)});['id=\"filter-body\"','id=\"filter-oak\"','id=\"filter-sweetness\"','id=\"filter-sale\"','id=\"filter-type\"','id=\"filter-brand\"'].forEach(function(s){if(b.indexOf(s)!==-1)throw new Error('wine-only row leaked onto beer: '+s)});if(b.indexOf('id=\"waitlist\"')===-1)throw new Error('waitlist section lost');if(b.indexOf('id=\"beer-waitlist-form\"')===-1)throw new Error('waitlist form lost');console.log('beer.html OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="catalog-blocks"' beer.html` returns 1 and `grep -c 'id="recipe-catalog"' beer.html` returns 1
    - `grep -c 'id="filter-subcategory"' beer.html` returns 1 and `grep -c 'id="filter-abv"' beer.html` returns 1
    - `grep -cE 'id="filter-(type|brand|manufacturer|time|body|oak|sweetness|sale)"' beer.html` returns 0
    - Behaviour/order: the byte offset of `id="product-catalog"` in beer.html is greater than the offset of `What&rsquo;s Included` and less than the offset of `id="waitlist"` — the catalogue sits between the launch copy and the waitlist (D-11)
    - `grep -c 'id="beer-waitlist-form"' beer.html` returns 1 and `grep -c 'Join the Waitlist' beer.html` returns at least 2 (hero CTA + waitlist submit button, both untouched)
    - `grep -c 'beer-banner' beer.html` is unchanged from before this task
    - `grep -c 'js/vendor/fuse.min.js' beer.html` returns 1 and `grep -c '17-search-overlay.min.js' beer.html` returns 1
    - `grep -c '\.\./' beer.html` returns 0 — beer.html is a root-level page
    - `git diff beer.html | grep -c '^-'` shows deletions only on the script-block line being extended and the sort-option lines removed — no deletion inside any narrative section
    - `grep -c 'Content-Security-Policy' beer.html` returns 1 and `git diff beer.html | grep -c 'Content-Security-Policy'` returns 0
  </acceptance_criteria>
  <done>beer.html keeps its full launch narrative with a catalogue block inserted between "What's Included" and the waitlist, carrying only the Style and ABV filters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → `beer.html`, `products/ferment-in-store.html`, `index.html` | Public static pages. Each already declares its own CSP meta; this plan must not weaken or disturb any of them. |
| `beer.html` → middleware `/api/products`, `/api/recipes`, `/api/waitlist` | Catalogue and waitlist fetches, all already permitted by beer.html's existing `connect-src`. |
| `content/home.json` → `index.html` `[data-content]` elements | Runtime text substitution performed by `js/modules/13-init.js`; hand-edited labels are silently overwritten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-22 | Tampering | CSP meta disturbed while restructuring three public pages | mitigate | No task edits any `<head>`; acceptance criteria assert `grep -c 'Content-Security-Policy'` returns 1 on each page and that the CSP line appears zero times in the diff for `beer.html`. No new third-party origin is introduced — the two added script tags are first-party `js/` assets already used by sibling pages. |
| T-74-23 | Denial of Service (self-inflicted) | removing the hub's catalogue while a link still points at it | mitigate | The hub gains three explicit `.btn-secondary` routes before the grid is removed, and the highest-intent inbound link (`index.html` hero CTA) is repointed under the Task 1 decision rather than left dangling |
| T-74-24 | Repudiation | silently losing live SEO ranking copy | mitigate | The landing-copy block is preserved verbatim; acceptance criteria assert the exact `h2` string survives, the detail block id survives, and that the diff contains zero deletions touching `landing-copy` |
| T-74-25 | Tampering | homepage CTA label reverting after deploy | mitigate | Only the `href` is changed; the action explicitly forbids editing the label because `content/home.json` overwrites it at runtime (Phase 72-02 precedent in STATE.md), and an acceptance criterion asserts the label does not appear in the diff |
| T-74-26 | Information Disclosure | wine-only filter rows leaking onto the beer page and exposing wine inventory dimensions | mitigate | Beer carries only `filter-subcategory` and `filter-abv`; an acceptance criterion greps for all eight wine-only row ids and requires zero hits |
| T-74-27 | Elevation of Privilege | cart affordance left on a page with no purchasable inventory | mitigate | The hub's cart sidebar and cart drawer are removed with the catalogue block; acceptance criteria assert both ids are gone |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (`74-RESEARCH.md` § Package Legitimacy Audit: "N/A"). No install task in this plan. |
</threat_model>

<verification>
- `npm test` exits 0 and `npm run lint` exits 0
- `git diff --name-only` lists only `products/ferment-in-store.html`, `beer.html` and `index.html`
- No change to any file under `js/`, `css/`, or to `package.json` or `sitemap.xml`
- `python3 -c "import html.parser,sys; p=html.parser.HTMLParser(); p.feed(open('beer.html').read()); p.feed(open('products/ferment-in-store.html').read()); print('parsed')"` exits 0
</verification>

<success_criteria>
- The hub retains "Make Your Own Wine in Squamish" verbatim and hosts no catalogue, filters or cart
- The hub routes to Wine, Beer and Ingredients &amp; Supplies with three `.btn-secondary` links
- beer.html's launch narrative is byte-identical and the catalogue sits between "What's Included" and the waitlist
- beer.html carries only the Style and ABV filter rows and now loads Fuse and the search overlay
</success_criteria>

<output>
Create `.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-05-SUMMARY.md` when done
</output>
