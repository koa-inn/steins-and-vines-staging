---
phase: 74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - wine.html
  - sitemap.xml
  - package.json
  - css/styles.css
  - index.html
  - about.html
  - contact.html
  - reservation.html
  - custom-labels.html
  - ingredients.html
  - products.html
  - hops.html
  - beer.html
  - products/ferment-in-store.html
  - products/ingredients-supplies.html
  - products/hops.html
  - products/grains.html
  - products/yeast.html
  - products/additives.html
  - products/packaging.html
  - products/equipment.html
autonomous: true
requirements: [D-01, D-04, D-09, D-13]

must_haves:
  truths:
    - "D-09: /wine exists as a root sibling of /beer — wine.html at the repository root, no move under products/"
    - "wine.html carries a Content-Security-Policy meta tag byte-identical to beer.html's, so no tracking service is silently blocked"
    - "wine.html is registered in the stamp:pages build script so npm run build stamps its asset versions"
    - "sitemap.xml lists the clean root URL https://steinsandvines.ca/wine"
    - "All 17 nav-carrying pages offer a Wine entry in the Products dropdown alongside the existing Beer entry"
    - "D-01/D-04: wine.html provides the two-block DOM contract — #catalog-blocks wrapping #product-catalog and an empty #recipe-catalog"
    - "D-13: wine.html carries the wine filter row set only, and every new CSS rule this phase needs exists in css/styles.css"
  artifacts:
    - path: "wine.html"
      provides: "the /wine catalogue page"
      contains: "data-page=\"wine\""
      min_lines: 250
    - path: "css/styles.css"
      provides: "#catalog-blocks, .product-grid--compact, .hub-categories, .hub-category-item"
      contains: "product-grid--compact"
    - path: "sitemap.xml"
      provides: "search-engine discovery of /wine"
      contains: "steinsandvines.ca/wine"
  key_links:
    - from: "wine.html"
      to: "js/main.min.js catalogue dispatch"
      via: "body data-page=\"wine\""
      pattern: "data-page=\"wine\""
    - from: "package.json stamp:pages"
      to: "wine.html"
      via: "page list entry"
      pattern: "wine\\.html"
---

<objective>
Create `/wine` as a root-level public page, register it everywhere a new public page must be registered, wire the site nav to it, and add every new CSS rule this phase needs.

Purpose: D-09 puts category pages at the root as siblings — `/beer` shipped there on 2026-08-31, is listed in `sitemap.xml`, and is linked from 17 pages, so moving it would need a server-side redirect GitHub Pages cannot provide. `/wine` joins it rather than the other way round. This plan also owns ALL new CSS for the phase so plans 74-03 and 74-05 never contend for `css/styles.css`.
Output: a complete `wine.html`, nav links across the site, sitemap and build registration, and four new CSS rules.

**Do NOT run `npm run build` in this plan** — the single rebuild and page stamp happens in plan 74-06, after `wine.html` is registered here.
**This plan edits `beer.html` and `products/ferment-in-store.html` for the nav dropdown ONLY.** Plan 74-05 owns their body content; do not touch it here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-CONTEXT.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-RESEARCH.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-PATTERNS.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md
@CLAUDE.md
@docs/TRACKING.md

<interfaces>
DOM contract the catalogue JS (plans 74-02 / 74-03) binds to. The element ids ARE the API:

  div.catalog-layout
    div#catalog-blocks
      div#product-catalog
        div.catalog-controls#catalog-controls-kits
          input#catalog-search.catalog-search
          div.catalog-view-toggle  (two button[data-view])
          button#catalog-toggle.catalog-toggle
          div.catalog-collapsible#catalog-collapsible   (one div.catalog-filter-row per filter, keyed by id)
          div.filter-summary.hidden#filter-summary
        noscript fallback paragraph
      div#recipe-catalog                    (empty in markup; populated or left empty by JS)
    aside.cart-sidebar#cart-sidebar
    div.cart-drawer-backdrop#cart-drawer-backdrop
    div.cart-drawer#cart-drawer

Wine filter row ids (the wine call list in js/modules/07-catalog-kits.js after plan 74-02):
  filter-type, filter-brand, filter-manufacturer, filter-subcategory, filter-time,
  filter-body, filter-oak, filter-sweetness, filter-sale, plus the sort row (label + select#catalog-sort)

Beer filter row ids (plan 74-05 adds these two to beer.html — listed so the contract stays visible):
  filter-subcategory, filter-abv

Page dispatch key read by js/modules/13-init.js: document.body.getAttribute('data-page').
Existing values: home, beer, products, ferment-in-store, ingredients, about, reservation.
This plan introduces: wine. Plan 74-06 adds the matching dispatch branch.

Existing nav dropdown item shape (about.html:105-115, identical on all 17 pages, differing only in
the href prefix — root pages use `beer.html`, pages under products/ use `../beer.html`):
  li.nav-dropdown > a[href=products/ferment-in-store.html] "Products"
    ul.nav-dropdown-menu
      li > a "Ferment in Store"
      li.nav-dropdown-indent > a[href=beer.html] "Beer"
      li > a[href=custom-labels.html] "Custom Labels"
      li[role=separator].nav-dropdown-divider
      li > a[href=products/ingredients-supplies.html] "All Ingredients"
      li.nav-dropdown-indent x6  (Hops, Grains, Yeast, Additives, Packaging, Equipment)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author wine.html at the repository root</name>
  <files>wine.html</files>
  <read_first>
    - products/ferment-in-store.html (the full file — the structural scaffold: GTM block, schema.org LocalBusiness JSON-LD, page-header at 119-126, the catalogue markup at 173-261 including cart sidebar and cart drawer, the `#filter-status` live region at ~266, footer, and the script block at 294-299)
    - beer.html (line 19 — the CSP meta tag to copy verbatim; lines 1-77 — root-relative asset paths, apple-mobile-web-app meta tags, manifest link; line 78 — the `data-page` body attribute; lines 263-266 — the root-relative script block)
    - CLAUDE.md (§ Security rule 12 — every public page needs a CSP meta copied from a sibling page)
    - docs/TRACKING.md (which third-party domains must appear in the CSP)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§1 — /wine is kits-only at launch and must look complete with no "coming soon" placeholder; Visual Hierarchy — the kit grid is the focal point)
  </read_first>
  <action>
Create `wine.html` in the repository ROOT (not under `products/`), using
`products/ferment-in-store.html` as the structural scaffold with every asset path rewritten
root-relative (`js/...`, `css/...`, `images/...`, `index.html`, `favicon.ico`) because `wine.html`
sits one directory level above the scaffold source. Mirror `beer.html`'s path convention exactly.

Head:
  - the Google Tag Manager script block for container `GTM-NHRCGLC5` as the first element in `head`,
    and its matching `noscript` iframe as the first element in `body`
  - a `Content-Security-Policy` meta tag copied BYTE-FOR-BYTE from `beer.html` line 19. Do not
    retype it, do not reorder directives, do not drop a domain — a missing domain silently breaks
    that service for every visitor site-wide (CLAUDE.md rule 12, and the incident recorded there).
  - `link rel=canonical` pointing at `https://steinsandvines.ca/wine` (clean root URL, matching how
    `/beer` is canonicalised)
  - title `Wine Kits in Squamish | Steins &amp; Vines`
  - the schema.org `LocalBusiness` JSON-LD block from the scaffold, `url` left as
    `https://steinsandvines.ca`
  - Open Graph and Twitter meta mirroring `beer.html`'s set, with `og:url`
    `https://steinsandvines.ca/wine` and `og:image`
    `https://steinsandvines.ca/images/icon-512.png`
  - the same stylesheet links the scaffold uses (`css/styles.min.css`,
    `css/search-overlay.min.css`) keeping their `?v=` query placeholders in the same shape so
    `stamp:pages` can rewrite them
  - favicon, apple-touch-icon, apple-mobile-web-app meta and `manifest.json` links as on `beer.html`

Body:
  - `body` element with attribute `data-page="wine"` — this exact string is the dispatch key
    `js/modules/13-init.js` reads
  - skip-to-content link, `header.site-header` with the shared nav, and `footer.site-footer` copied
    from the scaffold with root-relative paths. On this page the Products dropdown must already
    contain the Wine entry, marked with `class="active"` (Task 2 handles the other 16 pages).
  - `section.page-header` containing an `h1` using the scaffold's
    `img.title-icon[aria-hidden=true]` treatment, reading `Wine Kits`
  - directly below the page header, ONE short lead paragraph of one or two sentences introducing the
    kit range. It must NOT reuse the hub's ranking phrases: the string
    "Make Your Own Wine in Squamish" and the whole `landing-copy` / `landing-copy-detail` /
    "Read more" block stay exclusively on `products/ferment-in-store.html` per D-10, so only one
    page competes for those terms and no canonical tag is needed.
  - the catalogue section per the `interfaces` DOM contract above: reuse the scaffold's
    `section.catalog-section > div.container > div.catalog-layout`, but insert a new
    `div#catalog-blocks` wrapping `#product-catalog`, with a new empty `div#recipe-catalog` as its
    second child. The cart sidebar, cart-drawer backdrop and cart drawer remain direct children of
    `.catalog-layout`, OUTSIDE `#catalog-blocks`.
  - inside `#product-catalog`, copy the scaffold's `div.catalog-controls#catalog-controls-kits`
    verbatim: search input, view toggle, filters toggle, `#catalog-collapsible` containing the nine
    wine filter rows (`filter-type`, `filter-brand`, `filter-manufacturer`, `filter-subcategory`,
    `filter-time`, `filter-body`, `filter-oak`, `filter-sweetness`, `filter-sale`) plus the sort row
    with `select#catalog-sort` and all twelve of its existing options, then `#filter-summary`, then
    the `noscript` fallback paragraph.
  - OMIT the scaffold's `div.product-tabs#product-tabs` (the kits / Ingredients &amp; Supplies
    switcher) entirely — it is a hub-only artifact, and plan 74-05 removes it from the hub as well
    (UI-SPEC §8).
  - keep the scaffold's `div[role=status][aria-live=polite].sr-only#filter-status` live region near
    the end of `main`.

Script block at the end of `body`, root-relative, in this order — deliberately following
`products/ferment-in-store.html`'s list rather than `beer.html`'s, because `beer.html` is currently
missing `fuse.min.js` and the search overlay (`74-RESEARCH.md` Pitfall 5):
  `js/vendor/sentry.min.js`, `js/sentry-init.js`, `js/vendor/fuse.min.js` (defer),
  `js/sheets-config.js` (defer), `js/main.min.js` (defer, with a `?v=` placeholder),
  `js/modules/17-search-overlay.min.js` (defer, with a `?v=` placeholder).

Add no script, style, font, pixel or iframe source that the copied CSP does not already permit.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs');var re=/&lt;meta http-equiv=.Content-Security-Policy.[^&gt;]*&gt;/;var w=(fs.readFileSync('wine.html','utf8').match(re)||[])[0];var b=(fs.readFileSync('beer.html','utf8').match(re)||[])[0];if(!w){throw new Error('wine.html has no CSP meta')}if(w!==b){throw new Error('wine.html CSP differs from beer.html')}console.log('CSP identical OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `wine.html` exists at the repository root and `git ls-files wine.html` lists it
    - `grep -c 'data-page="wine"' wine.html` returns 1
    - `grep -c 'id="catalog-blocks"' wine.html` returns 1 and `grep -c 'id="recipe-catalog"' wine.html` returns 1
    - `grep -c 'id="product-tabs"' wine.html` returns 0
    - `grep -c 'Make Your Own Wine in Squamish' wine.html` returns 0 — the hub keeps that copy exclusively (D-10)
    - `grep -c 'id="filter-body"' wine.html`, `grep -c 'id="filter-oak"' wine.html`, `grep -c 'id="filter-sweetness"' wine.html`, `grep -c 'id="filter-sale"' wine.html` each return 1
    - `grep -c 'id="catalog-sort"' wine.html` returns 1
    - `grep -c '\.\./' wine.html` returns 0 — no parent-relative asset paths on a root-level page
    - `grep -c 'js/vendor/fuse.min.js' wine.html` returns 1
    - `grep -c 'GTM-NHRCGLC5' wine.html` returns 2 (script + noscript)
    - The CSP meta tag in `wine.html` is string-identical to the one in `beer.html` (proved by the automated verify command)
    - `grep -c 'canonical' wine.html` returns 1 and the href is `https://steinsandvines.ca/wine`
  </acceptance_criteria>
  <done>`/wine` renders as a complete, CSP-clean, root-level catalogue page carrying the two-block DOM contract and the wine filter set.</done>
</task>

<task type="auto">
  <name>Task 2: Register /wine — nav on 17 pages, sitemap entry, stamp:pages entry</name>
  <files>package.json, sitemap.xml, index.html, about.html, contact.html, reservation.html, custom-labels.html, ingredients.html, products.html, hops.html, beer.html, products/ferment-in-store.html, products/ingredients-supplies.html, products/hops.html, products/grains.html, products/yeast.html, products/additives.html, products/packaging.html, products/equipment.html</files>
  <read_first>
    - about.html (lines 100-120 — the canonical Products dropdown markup and the exact `li.nav-dropdown-indent` shape for the Beer entry)
    - products/hops.html (line 136 — the same entry with the `../` prefix used by every page under `products/`)
    - sitemap.xml (lines 75-92 — the `/beer` url block to mirror, including the clean-URL form with no `.html`)
    - package.json (line 17 — the `stamp:pages` script and its page array, which currently lists `beer.html` first)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-CONTEXT.md (D-09 — root siblings, and the note that Beer sits under Products → Ferment in Store since 2026-08-31)
  </read_first>
  <action>
Register the new page in the three places a new public page must be registered, plus the site nav.

Nav — in each of these 17 files, insert a new list item immediately BEFORE the existing Beer entry,
so the dropdown reads Ferment in Store → Wine → Beer → Custom Labels:
  index.html, about.html, contact.html, reservation.html, custom-labels.html, ingredients.html,
  products.html, hops.html, beer.html, products/ferment-in-store.html,
  products/ingredients-supplies.html, products/hops.html, products/grains.html, products/yeast.html,
  products/additives.html, products/packaging.html, products/equipment.html
The new item copies the Beer entry's shape exactly: an `li` with class `nav-dropdown-indent`
containing an anchor whose text is `Wine`. Match each file's existing href prefix — root-level pages
use `wine.html`, pages under `products/` use `../wine.html`. Read the Beer anchor in each file and
mirror its prefix rather than assuming. Do not add `class="active"` on any of these 17 pages; only
`wine.html` itself (written in Task 1) carries the active state.

Do not touch anything else in these files. In particular, `beer.html` and
`products/ferment-in-store.html` receive ONLY this one-line nav insertion in this plan — their body
content belongs to plan 74-05.

Sitemap — add a `url` block to `sitemap.xml` mirroring the existing `/beer` block: `loc`
`https://steinsandvines.ca/wine`, `lastmod` set to today's date, `changefreq` `weekly` (wine kit
stock changes far more often than beer's announcement page), `priority` `0.8` (this is now the
primary shopping surface for 238 live purchasable kits). Place it adjacent to the `/beer` block.
Leave the existing `/cider` entry alone — cider is out of scope for this phase and its disposition
is not this plan's call.

Build registration — add `'wine.html'` to the page array in the `stamp:pages` script in
`package.json`. Without it, `npm run build` never rewrites `wine.html`'s `?v=` cache-busters and
visitors get stale CSS and JS after every deploy. Insert it immediately before the existing
`'beer.html'` entry to keep the array reading in the same order as the nav.

Do not run `npm run build` — plan 74-06 runs it once for the whole phase.
  </action>
  <verify>
    <automated>node -e "var cp=require('child_process');var out=cp.execSync('grep -rl \"nav-dropdown-indent\\\"><a href=\\\"[^\\\"]*wine.html\\\"\" --include=*.html . | grep -v node_modules | wc -l').toString().trim();if(Number(out)!==17){throw new Error('expected 17 nav files with a Wine entry, got '+out)}console.log('nav OK 17/17')"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 17 HTML files contain a `nav-dropdown-indent` list item linking to `wine.html` or `../wine.html` (proved by the automated verify command)
    - `grep -c 'wine.html' package.json` returns 1 and `node -e "require('./package.json')"` exits 0 (JSON still parses)
    - `grep -c 'steinsandvines.ca/wine' sitemap.xml` returns 1
    - `python3 -c "import xml.dom.minidom; xml.dom.minidom.parse('sitemap.xml'); print('sitemap parses')"` exits 0
    - `git diff --stat beer.html products/ferment-in-store.html` shows 1 insertion each and 0 deletions — nav only
    - In every edited file the new Wine anchor's href prefix matches that file's existing Beer anchor prefix: `grep -rn 'wine.html' --include=*.html . | grep -v node_modules | grep -c '\.\./wine.html'` returns 8 (the eight files under `products/`)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>`/wine` is reachable from every page's Products dropdown, discoverable in the sitemap, and stamped by the build.</done>
</task>

<task type="auto">
  <name>Task 3: Add the four new CSS rules this phase needs</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css (lines 1502-1535 — `.catalog-section` and `.product-grid`, the rules the new modifier sits beside; lines 5600-5620 — `.catalog-layout` and the existing `#product-catalog { flex: 1; min-width: 0; }` rule; line ~809 — `.btn-secondary`)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§2 — the LOCKED `.product-grid--compact` rule and its media query; §8 — the `.hub-categories` / `.hub-category-item` rules; Spacing Scale — new CSS may use only the clean 4px column: 0.5rem, 1rem, 1.5rem, 2rem, 3rem)
  </read_first>
  <action>
Add four additive rules to `css/styles.css`. Modify no existing rule — in particular leave
`.product-grid`, `.catalog-layout` and `#product-catalog` exactly as they are, so the hub and the
ingredients pages are unaffected.

1. `#catalog-blocks` — placed immediately after the existing `#product-catalog` rule near line 5612.
   Declare `flex: 1;` and `min-width: 0;` so the new wrapper takes the flex slot `#product-catalog`
   previously occupied as a direct child of `.catalog-layout`. Pages without a `#catalog-blocks`
   element are untouched.

2. `.product-grid--compact` — placed immediately after the existing `.product-grid` rule at
   lines 1526-1531. Declare `grid-template-columns: repeat(auto-fit, minmax(280px, 320px));` and
   `justify-content: start;`. This is the LOCKED fix from UI-SPEC §2 for the launch state: with a
   single grid item, `.product-grid`'s `repeat(auto-fit, minmax(280px, 1fr))` collapses to one track
   that stretches to the full 1100px container, marooning a card's fixed-size contents in a huge
   empty rectangle. `/beer` launches with exactly 1 kit and 1 active recipe, so this is the real
   launch state, not an edge case.

3. A `@media (max-width: 480px)` override for `.product-grid--compact` setting
   `grid-template-columns: 1fr;` and `justify-content: stretch;`, placed with the rule above. 480px
   is an existing site breakpoint tier; introduce no new breakpoint value.

4. `.hub-categories` and `.hub-category-item` — the navigational row plan 74-05 puts on the rewritten
   hub. `.hub-categories` uses `display: flex; flex-wrap: wrap; gap: 2rem; justify-content: center;`.
   `.hub-category-item` uses `flex: 1 1 280px; max-width: 340px; text-align: center;`. The `2rem` gap
   and `280px` basis are the same two values `.product-grid` already uses — reuse the established
   rhythm rather than picking new numbers. Add no border, background, shadow or card chrome: this
   section is navigational and must not impersonate `.product-card`, which visitors associate with a
   buyable item.

Use only spacing values from the clean 4px column (`0.5rem`, `1rem`, `1.5rem`, `2rem`, `3rem`). Do
not introduce a new colour, font size or font weight — every new element reuses existing type roles
and the existing `.btn-secondary` class.

Do not run `npm run build`; `css/styles.min.css` is regenerated in plan 74-06.
  </action>
  <verify>
    <automated>node -e "var c=require('fs').readFileSync('css/styles.css','utf8');['#catalog-blocks','.product-grid--compact','.hub-categories','.hub-category-item'].forEach(function(s){if(c.indexOf(s)===-1)throw new Error('missing rule '+s)});console.log('all four rules present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^#catalog-blocks' css/styles.css` returns 1
    - `grep -c '^\.product-grid--compact' css/styles.css` returns at least 1 and the file also contains a `max-width: 480px` media block referencing `.product-grid--compact`
    - `grep -c '^\.hub-categories' css/styles.css` returns 1 and `grep -c '^\.hub-category-item' css/styles.css` returns 1
    - `git diff css/styles.css | grep -c '^-'` returns 0 beyond the diff header — the change is purely additive, no existing line removed
    - `npx cleancss -o /dev/null css/styles.css` exits 0 (the stylesheet still parses)
    - The new rules use no spacing value outside `0.5rem`, `1rem`, `1.5rem`, `2rem`, `3rem`, `280px`, `320px`, `340px`, `480px`
  </acceptance_criteria>
  <done>Every CSS rule plans 74-03 and 74-05 depend on exists, added without touching an existing rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → `wine.html` | A brand-new public surface served by GitHub Pages. Its `<head>` is the only place browser-side content policy is declared. |
| third-party tags (GTM, Metricool, Meta Pixel, Google Ads, Sentry) → page | Executed on the page under the CSP declared in the meta tag. |
| `wine.html` → middleware `svmiddleware-*.up.railway.app` | Fetches from the catalogue JS; must be permitted by `connect-src`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-17 | Tampering | new public page without a CSP | mitigate | CSP meta copied byte-for-byte from `beer.html`; the task's automated verify performs a string-equality comparison between the two tags and fails the task on any difference |
| T-74-18 | Information Disclosure | a dropped `connect-src` domain silently breaking Sentry, GA4 or Meta tracking | mitigate | Copy-verbatim requirement plus the string-equality gate. This has bitten the project before (CLAUDE.md rule 12 records site-wide Meta pixel breakage until 2026-07-22) |
| T-74-19 | Tampering | injecting a new third-party origin while authoring the page | mitigate | The action forbids adding any script, style, font, pixel or iframe source not already permitted by the copied CSP; no CSP edit is authorised in this plan |
| T-74-20 | Spoofing | a nav Wine link with a wrong relative prefix resolving to a 404 or an unexpected origin | mitigate | Acceptance criteria assert exactly 17 files carry the entry and exactly 8 of them (those under `products/`) use the `../` prefix |
| T-74-21 | Repudiation | unstamped assets serving stale JS/CSS after deploy | mitigate | `wine.html` added to `stamp:pages`; acceptance criteria assert the entry exists and that `package.json` still parses |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (`74-RESEARCH.md` § Package Legitimacy Audit: "N/A"). No install task in this plan. |
</threat_model>

<verification>
- `npm test` exits 0 and `npm run lint` exits 0
- `node -e "require('./package.json')"` exits 0
- `git diff --name-only` lists only the 21 files named in `files_modified`
- No change to `js/main.js`, `js/main.min.js`, `css/styles.min.css` or any `*.min.js`
</verification>

<success_criteria>
- `wine.html` exists at the root with a CSP identical to `beer.html`'s and `data-page="wine"`
- The two-block DOM contract (`#catalog-blocks`, `#product-catalog`, `#recipe-catalog`) is present
- 17 pages link to `/wine`; the sitemap lists it; `stamp:pages` stamps it
- All four new CSS rules exist and no existing rule was modified
</success_criteria>

<output>
Create `.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-04-SUMMARY.md` when done
</output>
