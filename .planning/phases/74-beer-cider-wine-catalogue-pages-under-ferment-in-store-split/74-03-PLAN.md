---
phase: 74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split
plan: 03
type: execute
wave: 2
depends_on: ["74-02", "74-04"]
files_modified:
  - js/modules/07-catalog-kits.js
  - tests/frontend/catalog-recipe-block.test.js
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07]

must_haves:
  truths:
    - "D-01: a category page renders a kit block and a recipe block, both fed from live data"
    - "D-02: the two blocks are separate, each with its own heading — 'Wine Kits' / 'Beer Kits' / 'Beer Recipes' — and recipe cards use the plain .product-card idiom, not the .label-wine/.label-beer bottle-label idiom"
    - "D-03: whichever block has more items renders first; on an exact count tie the kit block leads"
    - "D-04: a block with zero items produces no DOM at all — no heading, no wrapper, no placeholder"
    - "D-05: the recipe block fetches GET /api/recipes?status=active with no auth header"
    - "D-07: a recipe card shows name, style, price and blurb only, and never any ingredient or cost field"
    - "Neither block paints until both fetches settle, so the final order never reflows"
    - "A recipe-fetch failure shows an inline retry inside the recipe block only and does not blank a kit block that loaded successfully"
  artifacts:
    - path: "js/modules/07-catalog-kits.js"
      provides: "recipe fetch, recipe card builder, dual-block ordering and suppression"
      contains: "buildRecipeCard"
    - path: "tests/frontend/catalog-recipe-block.test.js"
      provides: "regression coverage for block ordering, zero-item suppression, and the recipe card field allowlist"
      min_lines: 100
  key_links:
    - from: "js/modules/07-catalog-kits.js"
      to: "GET /api/recipes?status=active"
      via: "fetch against SHEETS_CONFIG.MIDDLEWARE_URL with no credential header"
      pattern: "api/recipes\\?status=active"
    - from: "js/modules/07-catalog-kits.js orderCatalogBlocks"
      to: "#catalog-blocks child order"
      via: "insertBefore when recipes lead"
      pattern: "orderCatalogBlocks"
---

<objective>
Build the public recipe block — the first public surface for recipes anywhere on the site — and turn each category page into the two-block layout D-01 through D-04 describe.

Purpose: no module renders recipes today. This plan adds the fetch, the card, the dual-block ordering rule, zero-item suppression, and per-block error isolation. It consumes the public read contract shipped by plan 74-01, so a card can only ever be built from fields the server is willing to publish.
Output: `/beer` renders "Beer Kits" and "Beer Recipes"; `/wine` renders "Wine Kits" alone (zero wine recipes exist, so D-04 suppresses the recipe block entirely).

**Do NOT run `npm run build` in this plan** — the single rebuild happens in plan 74-06.
**Do NOT edit `css/styles.css` in this plan** — every new CSS rule this phase needs, including `.product-grid--compact` and `#catalog-blocks`, is added by plan 74-04 (wave 1), which runs before this one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-CONTEXT.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-RESEARCH.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-PATTERNS.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-01-SUMMARY.md
@.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Public recipe payload — the contract shipped by plan 74-01. `GET {MIDDLEWARE_URL}/api/recipes?status=active`
with NO auth header returns:

  { source: "cache"|"apps-script", recipes: PublicRecipe[], total: number }

  PublicRecipe:
    recipe_id    string
    name         string
    style        string      (may be absent)
    description  string      (may be absent or empty)
    price        number|null (single fee-inclusive figure; null when not computable)
    price_from   true        (present ONLY when the source recipe is dynamically priced)

  The server does not emit ingredients, locked_price, service_fee, materials_fee, computed_price,
  pricing_mode, status, notes, abv, ibu or batch_size_l on this path. The client must therefore
  never try to read them.

DOM contract supplied by plans 74-04 (`wine.html`) and 74-05 (`beer.html`) — both blocks are
siblings inside a wrapper so their order can be swapped at runtime:

  <div class="catalog-layout">
    <div id="catalog-blocks">
      <div id="product-catalog"> ...catalog-controls, filter rows... </div>
      <div id="recipe-catalog"></div>
    </div>
    <aside class="cart-sidebar" id="cart-sidebar"> ... </aside>
    ...cart drawer...
  </div>

  Pages that do NOT have a recipe block (products.html, products/ingredients-supplies.html) keep
  #product-catalog as a direct child of .catalog-layout with no #catalog-blocks wrapper. All new
  code must no-op cleanly when #recipe-catalog is absent.

Existing helpers available in js/modules/07-catalog-kits.js after plan 74-02:
  matchesKitCategory(obj, categoryFilter)     module scope, exported
  buildWaitlistCtaLink(doc)                   module scope, exported — returns div.reserve-link > a.btn "Join the Waitlist"
  sortFilterValues(field, values, category)   module scope, exported
  _categoryFilter                             closure-scope lowercased category string inside loadProducts
  renderSection(catalog, title, items, extraClass)   closure scope, line ~1066; early-returns on items.length === 0

Globals from sibling concatenated modules:
  formatCurrency(val)                       js/lib/utils.js:11
  showCatalogSkeletons(container, count)    js/modules/02-utils.js:143
  SHEETS_CONFIG.MIDDLEWARE_URL              js/sheets-config.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recipe card builder and price label, as testable module-scope helpers (D-07)</name>
  <files>js/modules/07-catalog-kits.js, tests/frontend/catalog-recipe-block.test.js</files>
  <read_first>
    - js/modules/07-catalog-kits.js (lines 902-1000 — `buildDefaultCard`, the plain `.product-card` idiom this card copies; lines 1000-1030 — the `.product-price-box` / `.product-price-value` DOM shape; the module-scope helpers added by 74-02 and the current `module.exports`)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§5 recipe card anatomy and the LOCKED field order name → style → price → blurb; §4 "recipes must not read as products"; Copywriting Contract rows for the empty-description fallback)
    - css/styles.css (`.product-card` ~1543, `.product-card h4` ~1596, `.product-card-category` ~1624, `.service-description` ~1866 — all already defined, currently unused by any JS)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-01-SUMMARY.md (the exact public payload the server ships)
  </read_first>
  <action>
Add two module-scope functions to `js/modules/07-catalog-kits.js`, next to the helpers 74-02 added,
and export both.

`recipeDisplayPrice(recipe)` — returns the string to render in the price slot:
  - when `recipe.price` is not a finite number (null, undefined, NaN), return
    `'Price set when you book'` verbatim (UI-SPEC §5 fallback, mirrors beer.html's existing
    "we'll give you a price when we book your session" copy)
  - otherwise return `formatCurrency(recipe.price)`, prefixed with `'From '` when
    `recipe.price_from` is truthy — so a dynamically priced recipe reads `From $185.00`, matching the
    site's existing "starting at" convention on kit price footers

`buildRecipeCard(recipe, doc)` — takes an optional document (defaulting to the global) and returns a
DOM node built only with `createElement` and `textContent`, never `innerHTML`:
  - root `div` with `className` `'product-card'` and attribute `data-recipe-id` set to
    `recipe.recipe_id`. It must NOT use `label-wine`, `label-beer`, or any bottle-label class —
    UI-SPEC §4 requires the plain listing idiom so a recipe does not read as a purchasable product.
  - `div.product-card-header` containing an `h4` whose `textContent` is `recipe.name`
  - inside the header, a `p.product-card-category` whose `textContent` is `recipe.style`, appended
    only when `recipe.style` is a non-empty string
  - `div` with `className` `'product-prices service-price'` containing
    `div.product-price-box` > `span.product-price-value` whose `textContent` is
    `recipeDisplayPrice(recipe)`
  - `p.service-description` whose `textContent` is `recipe.description`, appended ONLY when
    `recipe.description` is a non-empty string. Never render an empty paragraph and never render
    placeholder text such as "No description available."
  - finally `buildWaitlistCtaLink(doc)` from plan 74-02, so a recipe card carries the same
    `Join the Waitlist` action a beer kit card does (UI-SPEC §6 applies the CTA to both)
  - element order is exactly: name → style → price → blurb → CTA (UI-SPEC §5 deliberately places
    price above the blurb)
  - read no property outside `recipe_id`, `name`, `style`, `description`, `price`, `price_from`

Create `tests/frontend/catalog-recipe-block.test.js` with the same browser-global stub harness used
by `tests/frontend/catalog-category-scope.test.js` (add a `formatCurrency` stub). Assert:
`recipeDisplayPrice` returns `'Price set when you book'` for `price: null` and for a missing `price`;
returns a bare formatted currency for a locked-style payload without `price_from`; returns a
`'From '`-prefixed string when `price_from` is true. For `buildRecipeCard`: the root has class
`product-card` and not `label-beer`; the `h4` text equals the recipe name; a card built from a recipe
with an empty `description` contains zero `.service-description` elements; a card built with a
description contains exactly one whose text equals the description; the card's full `textContent`
contains no `$`-prefixed number other than the single price string; and the card contains exactly one
`a.btn` reading `Join the Waitlist`.
  </action>
  <verify>
    <automated>npx jest tests/frontend/catalog-recipe-block.test.js 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^function buildRecipeCard" js/modules/07-catalog-kits.js` returns 1
    - `grep -c "^function recipeDisplayPrice" js/modules/07-catalog-kits.js` returns 1
    - `grep -c "buildRecipeCard: buildRecipeCard" js/modules/07-catalog-kits.js` returns 1
    - Source assertion: `sed -n "/^function buildRecipeCard/,/^}/p" js/modules/07-catalog-kits.js | grep -c "innerHTML"` returns 0
    - Source assertion: `sed -n "/^function buildRecipeCard/,/^}/p" js/modules/07-catalog-kits.js | grep -cE "ingredients|locked_price|service_fee|materials_fee|computed_price|pricing_mode"` returns 0
    - Behaviour: a card built from `{ recipe_id:'SV-R-000002', name:'Czech Lager', style:'Lager', description:'' , price:null }` contains zero `.service-description` nodes and its price slot reads `Price set when you book`
    - `npm test` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>A recipe card renders name, style, price and blurb from the public payload and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch active recipes and render the recipe block with per-block error isolation (D-05)</name>
  <files>js/modules/07-catalog-kits.js, tests/frontend/catalog-recipe-block.test.js</files>
  <read_first>
    - js/modules/07-catalog-kits.js (lines 132-200 — `loadFromMiddleware`'s fetch/skeleton/render shape; lines 285-310 — the existing monolithic `.catalog-error` + `.btn-retry.btn-outline` catch handler to mirror but scope down; lines 1066-1100 — `renderSection`)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§10 states and Copywriting Contract rows "Error state — recipe block", "Block heading — beer recipes", "Block sub-copy")
    - js/modules/02-utils.js (lines 140-155 — `showCatalogSkeletons(container, count)`)
    - js/sheets-config.js (how `SHEETS_CONFIG.MIDDLEWARE_URL` is resolved per hostname)
  </read_first>
  <action>
Inside `loadProducts`'s closure, add the recipe fetch and the recipe block renderer.

`fetchActiveRecipes()` — returns a promise that always resolves, never rejects, to either
`{ ok: true, recipes: [...] }` or `{ ok: false }`:
  - immediately return `{ ok: true, recipes: [] }` when `_categoryFilter` is falsy, so the existing
    unscoped callers (`products.html`, `products/ingredients-supplies.html`, and the hub until 74-05
    removes its catalogue) do no extra network work at all
  - return `{ ok: true, recipes: [] }` when `_categoryFilter` is not `'beer'`. Recipes carry no
    category field of any kind — `74-RESEARCH.md` Pitfall 2 confirms the Recipes sheet has no
    category column and that every existing recipe is beer-styled. Do NOT build style-keyword
    inference; route recipes to the beer page only. Record this as a locked design decision in the
    SUMMARY: if a wine recipe is ever created it will need an explicit category field, not a
    heuristic.
  - otherwise `fetch(SHEETS_CONFIG.MIDDLEWARE_URL + '/api/recipes?status=active')` with NO headers
    and no credentials — this is a public read. Never send `status=all` from a public page.
  - on a non-ok response or a thrown error, resolve `{ ok: false }`

`renderRecipeBlock(result)` — paints into `document.getElementById('recipe-catalog')`:
  - return immediately when that element is absent (every page without a recipe block)
  - clear the container first
  - when `result.ok` is false, append a `div.catalog-error` containing a `p` whose text is
    `"Couldn't load recipes right now. Check your connection and try again."` and a
    `button.btn-retry.btn-outline` of type `button` reading `Try again` whose click handler re-runs
    `fetchActiveRecipes().then(renderRecipeBlock)` — scoped to this container only. It must never
    touch `#product-catalog`; a recipe failure must not blank a kit block that loaded.
  - when `result.recipes.length === 0`, append nothing and return. D-04 is absolute: no heading, no
    wrapper, no "coming soon" placeholder. Implement as an early return, not a CSS `display:none`.
  - otherwise build `div.catalog-section` > `div.catalog-section-header` > `h2.catalog-section-title`
    with `textContent` `'Beer Recipes'`, then a `div.product-grid` (adding the additional class
    `product-grid--compact` when there are 3 or fewer cards, per UI-SPEC §2 — the rule is added by
    plan 74-04), then one `buildRecipeCard(recipe)` per item.
  - sub-copy rule (LOCKED here, because CONTEXT and UI-SPEC together leave it ambiguous): append a
    `p.process-note` reading `"Book a session and brew your own batch in our studio."` under the
    recipe heading ONLY when the kit block is also being rendered on the same page. The sub-copy
    exists to differentiate the two blocks (D-02); with only one block on the page there is nothing
    to differentiate, and UI-SPEC's kit sub-copy ("Take a kit home to ferment yourself.") is
    inaccurate for wine, whose primary action is reserve-to-ferment-in-store. Pass a boolean into
    the renderer rather than re-querying the DOM.

Wire the skeleton: when `_categoryFilter` is truthy and `#recipe-catalog` exists, call
`showCatalogSkeletons(recipeEl, 2)` before the fetch starts, sized to the small expected card count
per UI-SPEC §10.

Extend `tests/frontend/catalog-recipe-block.test.js` with jsdom-backed assertions: a container plus a
zero-length recipe list yields `innerHTML === ''`; a failed result yields exactly one
`.catalog-error` with the exact copy string and one `.btn-retry`; a two-recipe result yields one
`.catalog-section`, one `h2.catalog-section-title` reading `Beer Recipes`, and a `.product-grid` that
also carries `product-grid--compact`; a four-recipe result yields a `.product-grid` WITHOUT
`product-grid--compact`.
  </action>
  <verify>
    <automated>npx jest tests/frontend/catalog-recipe-block.test.js 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/recipes?status=active" js/modules/07-catalog-kits.js` returns 1
    - `grep -c "status=all" js/modules/07-catalog-kits.js` returns 0
    - `grep -c "recipe-catalog" js/modules/07-catalog-kits.js` returns at least 1
    - Source assertion: the fetch call site passes no second argument containing `headers` — `sed -n "/api\/recipes?status=active/p" js/modules/07-catalog-kits.js | grep -c "headers"` returns 0
    - Behaviour: rendering an empty recipe list into a jsdom `#recipe-catalog` leaves `innerHTML` exactly `''`
    - Behaviour: rendering a failed fetch result leaves `#product-catalog` untouched (test asserts a sentinel child previously placed in `#product-catalog` still exists)
    - `npm test` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>The recipe block fetches active recipes publicly, renders or suppresses itself correctly, and fails independently of the kit block.</done>
</task>

<task type="auto">
  <name>Task 3: Dual-block ordering, category kit headings, and single-paint commit (D-01/D-02/D-03)</name>
  <files>js/modules/07-catalog-kits.js, tests/frontend/catalog-recipe-block.test.js</files>
  <read_first>
    - js/modules/07-catalog-kits.js (lines 160-210 — the `dataPromise.then` chain that ends with `if (_activeCartTab === 'kits') applyFilters();`; lines 657-694 — `renderCatalog`, including its `catalog.querySelectorAll('.catalog-section, ...')` teardown and its single `renderSection(catalog, 'Currently available', inStock)` call; lines 1066-1105 — `renderSection` and its per-`type` grouping with `.product-group-title` headings)
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-UI-SPEC.md (§3 LOCKED tie-break "kits lead on a count tie"; §10 "requires BOTH counts before either block paints")
    - .planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-CONTEXT.md (D-02, D-03, D-04)
  </read_first>
  <action>
Add the ordering rule and make the kit block carry its category heading.

Add a module-scope pure function `orderCatalogBlocks(kitCount, recipeCount)` returning
`['kits', 'recipes']` when `kitCount >= recipeCount` and `['recipes', 'kits']` otherwise. The `>=`
encodes UI-SPEC §3's LOCKED tie-break: at launch `/beer` has 1 kit and 1 active recipe, an exact tie,
and kits lead. Export it. Implement the order as this explicit comparison, never as an emergent
property of array order.

In `renderCatalog`, derive the kit block heading from `_categoryFilter` instead of the hardcoded
`'Currently available'`: `'Wine Kits'` for `'wine'`, `'Beer Kits'` for `'beer'`, and the existing
`'Currently available'` for every unscoped caller. Pass the kit sub-copy
`"Take a kit home to ferment yourself."` through only when both blocks render on the page (same rule
as Task 2). Extend `renderSection`'s signature with a trailing optional `subCopy` argument and have
it append a `p.process-note` under the heading when one is supplied, reusing the class its existing
`catalog-section--order` branch already uses; leave every current call site's behaviour unchanged.

Suppress the redundant inner group heading: `renderSection` groups items by `r.type` and emits a
`.product-group-title` per group. With a category filter active there is exactly one group, so the
page would read "Wine Kits" immediately followed by "Wine". Skip appending the group heading when
`_categoryFilter` is truthy and there is exactly one group. Leave multi-group behaviour (the
unscoped hub/products pages) untouched.

Commit both blocks in one paint. In the `dataPromise.then` chain, start `fetchActiveRecipes()`
before or alongside the kit work and replace the trailing `if (_activeCartTab === 'kits') applyFilters();`
with a `Promise.all`-style join that waits for the recipe result, then in a single synchronous pass:
  - compute the in-stock kit count using `renderCatalog`'s existing available/stock rule
  - call `orderCatalogBlocks(kitCount, recipeCount)`; when it returns `['recipes','kits']`, reorder
    the DOM once via `catalogBlocks.insertBefore(recipeEl, productEl)` on the `#catalog-blocks`
    wrapper. When it returns `['kits','recipes']` leave the markup order alone. Treat a failed
    recipe fetch as `recipeCount === 0` for ordering purposes.
  - call `renderRecipeBlock(...)` and then `applyFilters()`
Do not paint the kit block as soon as `/api/products` resolves and pop the recipe block in later —
UI-SPEC §10 forbids the reflow. When `_categoryFilter` is falsy the recipe promise resolves
synchronously-empty, so the existing pages keep their current timing.

Guard everything for a missing `#catalog-blocks` or `#recipe-catalog` so unscoped pages are inert.

Extend the test file: `orderCatalogBlocks(1, 1)` deep-equals `['kits','recipes']` (the launch tie);
`orderCatalogBlocks(0, 3)` deep-equals `['recipes','kits']`; `orderCatalogBlocks(5, 2)` deep-equals
`['kits','recipes']`; `orderCatalogBlocks(0, 0)` deep-equals `['kits','recipes']`.
  </action>
  <verify>
    <automated>npx jest tests/frontend/catalog-recipe-block.test.js tests/frontend/catalog-category-scope.test.js 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^function orderCatalogBlocks" js/modules/07-catalog-kits.js` returns 1
    - `grep -c "orderCatalogBlocks: orderCatalogBlocks" js/modules/07-catalog-kits.js` returns 1
    - `grep -c "'Wine Kits'" js/modules/07-catalog-kits.js` returns 1 and `grep -c "'Beer Kits'" js/modules/07-catalog-kits.js` returns 1
    - `grep -c "'Currently available'" js/modules/07-catalog-kits.js` returns 1 — the unscoped heading survives
    - `grep -c "insertBefore" js/modules/07-catalog-kits.js` returns at least 1
    - Behaviour: `orderCatalogBlocks(1, 1)` deep-equals `['kits','recipes']`
    - Behaviour: with `_categoryFilter` unset, `renderCatalog` still emits a section titled `Currently available` (assert via the existing suite staying green)
    - `npm test` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Both blocks commit in one paint with the larger set leading and kits winning a tie; the kit block carries its category heading; unscoped pages are byte-identical in behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /api/recipes?status=active` → browser | Public, unauthenticated. After plan 74-01 the server projects the payload; the client must still treat every field as untrusted text. |
| recipe `name` / `style` / `description` → DOM | Staff-authored free text originating in BrewPad, rendered on a public page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-12 | Tampering / XSS | `buildRecipeCard` | mitigate | Every node created with `createElement` and populated with `textContent`; acceptance criteria assert zero `innerHTML` occurrences inside the function body |
| T-74-13 | Information Disclosure | client re-requesting drafts | mitigate | The only recipe fetch in the module is the literal `?status=active` URL with no headers; acceptance criteria assert `status=all` appears zero times in the file. The server-side guard from 74-01 remains the real control — this is defence in depth, not the boundary. |
| T-74-14 | Information Disclosure | cost data reaching the DOM | mitigate | The card reads a fixed six-field allowlist; acceptance criteria grep the function body for `ingredients`, `locked_price`, `service_fee`, `materials_fee`, `computed_price` and `pricing_mode` and require zero hits |
| T-74-15 | Denial of Service | retry button hammering the middleware | accept | Retry is user-initiated and single-shot per click; the middleware serves this path from a 10-minute Redis cache under a single `:active:0:0` key |
| T-74-16 | Spoofing | mixed-origin fetch | mitigate | The middleware origin is already enumerated in `connect-src` on every public page's CSP; plan 74-04 copies that CSP verbatim onto `wine.html`, so no CSP change is required or permitted here |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (`74-RESEARCH.md` § Package Legitimacy Audit: "N/A"). No install task in this plan. |
</threat_model>

<verification>
- `npm test` exits 0
- `npm run lint` exits 0
- `git diff --name-only` lists only `js/modules/07-catalog-kits.js` and `tests/frontend/catalog-recipe-block.test.js`
- No change to `js/main.js`, `js/main.min.js` or `css/styles.css`
</verification>

<success_criteria>
- A recipe card exposes exactly name, style, price and blurb
- Zero-item blocks emit no DOM
- Block order is decided by an explicit count comparison with kits leading a tie
- A recipe-fetch failure leaves the kit block intact
</success_criteria>

<output>
Create `.planning/phases/74-beer-cider-wine-catalogue-pages-under-ferment-in-store-split/74-03-SUMMARY.md` when done
</output>
