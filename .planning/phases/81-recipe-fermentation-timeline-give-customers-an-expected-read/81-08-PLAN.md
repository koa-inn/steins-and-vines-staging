---
phase: 81-recipe-fermentation-timeline-give-customers-an-expected-read
plan: 08
type: execute
wave: 5
depends_on: ["81-02", "81-03", "81-04", "81-05", "81-06", "81-07"]
files_modified:
  - .planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-SCHEDULES-INVENTORY.md
autonomous: false
requirements: [OPS-05]

must_haves:
  truths:
    - "All three active recipes render a fermentation timeline on their card — the D-10 release gate"
    - "The two ales read about 3 weeks and the lager reads about 5 weeks, matching what staff say out loud"
    - "No anonymous response carries schedule_id, template names, step titles or transfer flags"
    - "Every admin surface added this phase works on a real browser: the picker, the warning, the import review, the blast-radius note, the activation default"
  artifacts:
    - path: ".planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-SCHEDULES-INVENTORY.md"
      provides: "Appended: the templates created or reused, the recipe→template attachment record, and the staging verification evidence"
  key_links:
    - from: "staging beer.html recipe cards"
      to: "GET /api/recipes ferment_days"
      via: "buildRecipeCard second .price-col"
      pattern: "Ready in"
    - from: "Recipes sheet schedule_id"
      to: "FermSchedules template"
      via: "the D-10 backfill of all three active recipes"
      pattern: "schedule_id"
---

<objective>
Deploy the middleware and frontend to staging, create or attach the fermentation templates the
three active recipes need, and prove the release gate: every active recipe shows a timeline.

Purpose: D-10's gate is unconditional — "all 3 active recipes render a timeline on the day this
ships" — because without it the phase delivers an empty feature. This plan is where the phase
either works or does not.

It is also where the phase writes to live production data. Staging and production share ONE
Google Sheet, so attaching a schedule to a real recipe is a production write no matter which
environment's admin UI performs it. That is not a hazard to route around; it IS the deliverable.
The point of ordering it here, after the reversible code steps and after the sanity-checkable
inventory, is that by now the correct value is known before it is written.

Output: staging running the new code, three recipes attached to correct templates, and recorded
evidence that the customer-facing result matches the owner's own ~3-week / ~5-week figures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-CONTEXT.md
@.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-UI-SPEC.md
@.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-SCHEDULES-INVENTORY.md
@.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-07-SUMMARY.md
@docs/RUNBOOK.md
@CLAUDE.md

<interfaces>
DEPLOY REALITY:
- `git push origin main` auto-deploys the staging middleware at `svmiddleware-staging.up.railway.app`
  (root `railway.toml` watches `zoho-middleware/**`) AND publishes the staging frontend at
  `staging.steinsandvines.ca`, which is behind Cloudflare Access.
- The staging frontend routes to the staging middleware by hostname (`js/sheets-config.js:66-67`).
- **Staging and production share ONE Google Sheet.** Sheet writes made from the staging admin UI
  hit live production data. There is no isolated staging sheet.
- The Apps Script layer is already deployed by plan 81-07 and serves both environments.
- Production deploy is deliberately NOT in this plan — it is plan 81-09.

REVERSIBILITY of each action in this plan:
- `git push origin main` — reversible (revert + push).
- Creating a FermSchedules template — additive and inert; a template affects nothing until a
  recipe or batch references it. Effectively harmless.
- Attaching `schedule_id` to a recipe — a live production write, but trivially reversible by
  clearing the field in the recipe editor. This is the only genuinely production-visible data
  change in the phase, and only because the middleware derives from it.

THE THREE ACTIVE RECIPES (verified live 2026-09-05):
  SV-R-000011  West Coast IPA   — ale,   expect about 3 weeks
  SV-R-000003  Hazy Pale Ale    — ale,   expect about 3 weeks
  SV-R-000002  Czech Lager      — lager, expect about 5 weeks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to staging and verify the API contract before touching any data</name>

  <read_first>
    - `docs/RUNBOOK.md` — the staging deploy sequence
    - `.planning/STATE.md` — the 2026-08-27 correction confirming the staging middleware exists and auto-deploys on `git push origin main`
    - `js/sheets-config.js:60-70` — the hostname-based middleware routing that sends the staging frontend to the staging middleware
    - `.planning/phases/81-.../81-02-SUMMARY.md` — what the middleware now returns and the recorded worst-case cache staleness
    - `zoho-middleware/routes/recipes.js` — `PUBLIC_RECIPE_FIELDS` and `enrichFermentDays`, so you know what to assert against
  </read_first>

  <files>(none — this task deploys and verifies; it commits no source change)</files>

  <action>
Confirm the working tree is clean and that all of plans 81-01 through 81-06 are merged to `main`.
Run the full gates one final time before pushing: `npm test`, `cd zoho-middleware && npm test`,
and `npm run lint`. All three must pass — CLAUDE.md forbids committing or shipping with a failing
suite. Confirm `js/main.js` and `js/main.min.js` are in sync with `js/modules/07-catalog-kits.js`
by running `npm run build` and checking `git status` reports no change; a diff here means the
artifacts were committed stale.

Push to staging with `git push origin main`. Wait for the Railway staging middleware to finish
deploying and for GitHub Pages to publish the staging frontend.

Then verify the API contract on staging BEFORE any schedule is attached. At this point every
recipe still has `schedule_id === ''`, so this is the "no timeline" baseline and it must be
clean:
- `curl -s https://svmiddleware-staging.up.railway.app/api/recipes?status=active` returns 200 with
  three recipes.
- No recipe object in that response carries a `ferment_days` key — nothing is attached yet, and
  D-09 requires the key be absent rather than `null` or `0`.
- The serialized response contains none of the substrings `schedule_id`, `steps`, `steps_parsed`,
  `is_transfer`. This is the D-16 / T-74-04 boundary assertion against a live server rather than a
  mocked one.
- Attempt `?status=all` and `?status=draft` as an anonymous caller and confirm both still return
  only active recipes — the pre-existing T-74-03 guarantee must not have regressed.

Record every response in `81-SCHEDULES-INVENTORY.md` under a new "Staging verification (pre-backfill)"
heading. Do not proceed to Task 2 if any of these fail — a leak found now is trivial to fix, and a
leak found after the production cutover is not.
  </action>

  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm test && cd zoho-middleware && npm test && cd .. && npm run lint && npm run build && git status --porcelain js/main.js js/main.min.js</automated>
  </verify>

  <acceptance_criteria>
    - `npm test`, `cd zoho-middleware && npm test` and `npm run lint` all exit 0
    - `npm run build` followed by `git status --porcelain js/main.js js/main.min.js` produces no output (artifacts were committed in sync)
    - `curl -s https://svmiddleware-staging.up.railway.app/api/recipes?status=active` returns HTTP 200 with exactly 3 recipes
    - `curl -s https://svmiddleware-staging.up.railway.app/api/recipes?status=active | grep -c ferment_days` returns 0 at this pre-backfill point
    - The same response piped through `grep -c -E 'schedule_id|steps_parsed|is_transfer'` returns 0
    - `curl -s '...?status=all'` as an anonymous caller returns only recipes whose status is active
    - `81-SCHEDULES-INVENTORY.md` contains a "Staging verification (pre-backfill)" section with all of the above recorded
  </acceptance_criteria>

  <done>Staging runs the new middleware and frontend, returns a clean no-timeline baseline, and leaks nothing across the public boundary.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create or attach the templates, and attach a schedule to all three active recipes</name>

  <read_first>
    - `.planning/phases/81-.../81-SCHEDULES-INVENTORY.md` — the recorded Branch A / Branch B decision and the per-recipe template mapping from plan 81-07
    - `.planning/phases/81-.../81-CONTEXT.md` D-10 and `<specifics>` — the release gate and the owner's ~3-week / ~5-week figures
    - `js/admin.js` `renderScheduleForm` — the template editor used to create a template, including its step rows and the `is_packaging` final step
    - `.planning/phases/81-.../81-04-SUMMARY.md` — the recipe editor's new `#recipe-schedule-select` and where it sits in the form
  </read_first>

  <files>.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-SCHEDULES-INVENTORY.md</files>

  <action>
Operator action against the STAGING admin UI, which writes to the shared production Google Sheet.
Following the branch recorded in `81-SCHEDULES-INVENTORY.md`: either confirm the existing template
still yields the recorded figure (Branch A), or create an ale template whose largest non-packaging
`day_offset` is about 21 and a lager template whose largest is about 35, both category `beer`, both
ending in an `is_packaging` step (Branch B). Then attach `SV-R-000011` and `SV-R-000003` to the ale
template and `SV-R-000002` to the lager template via the recipe editor's Fermentation Schedule
picker, reload to confirm persistence, and confirm at the source with `get_recipes`. Record the
branch, the templates, the attachments and the derived week figures under a "Backfill" heading.
Undo for a wrong template is clearing the recipe's schedule back to "None". Full procedure in
`<how-to-verify>`.
  </action>

  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && grep -q 'Backfill' .planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-SCHEDULES-INVENTORY.md && ! grep -q 'server_token=' .planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-SCHEDULES-INVENTORY.md</automated>
  </verify>

  <what-built>
    Staging now runs the middleware that derives `ferment_days` and the frontend that renders it,
    and the Apps Script layer deployed in plan 81-07 can persist `schedule_id`. Every piece is in
    place except the data: no recipe is linked to a template yet, so every card still renders the
    single-column footer it renders today. This task supplies the data.
  </what-built>

  <how-to-verify>
    Work in the STAGING admin UI (`staging.steinsandvines.ca/admin.html`, behind Cloudflare
    Access). Be aware throughout: **staging and production share one Google Sheet.** Everything
    below is a live production write. That is intended — attaching these schedules IS the
    deliverable, not a test of it — but it means there is no rehearsal. Get the numbers right
    before saving.

    **Step 1 — templates. Follow the branch recorded in `81-SCHEDULES-INVENTORY.md`.**

    *If Branch A (a usable template already exists):* open it in the schedule editor and confirm
    its non-packaging day offsets still produce the figure recorded in plan 81-07. Change nothing.
    If an ale template exists but no lager one, treat the lager as Branch B.

    *If Branch B (create them):* create two templates via Admin > Batches > schedule templates.
      - Ale template: name it clearly (e.g. "Ale — standard"), category `beer`, with steps whose
        largest non-packaging `day_offset` is about **21** (about 3 weeks). Build a realistic step
        ladder rather than a single step — this template will also drive real batch task lists.
      - Lager template: category `beer`, largest non-packaging `day_offset` about **35** (about 5
        weeks).
      - Both MUST end with a step flagged `is_packaging`. That step deliberately carries no
        meaningful offset ("always last, date TBD until all other steps are done") and is excluded
        from the derivation. If you give the packaging step the largest offset, the derivation
        correctly ignores it — but the batch task list will be wrong, so set it properly anyway.
      - After saving each, re-run
        `curl -s "$APPS_SCRIPT_URL?action=get_ferm_schedules&server_token=$APPS_SCRIPT_SERVER_TOKEN"`
        and confirm the new template appears immediately. It should — plan 81-01 added the `'gfs'`
        eviction. If it does not appear for up to five minutes, that eviction did not deploy;
        stop and report.

    **Step 2 — attach a schedule to each of the three active recipes.** In Admin > Recipes, open
    each recipe, set Fermentation Schedule, and save:
      - `SV-R-000011` West Coast IPA → the ale template
      - `SV-R-000003` Hazy Pale Ale → the ale template
      - `SV-R-000002` Czech Lager → the lager template

    Note the picker should have options the moment you open the Recipes tab, even if you never
    visited Batches this session — plan 81-04 Task 1 fixed the lazy-load gap that would otherwise
    leave it empty. If the dropdown is empty, that fix did not deploy; stop and report rather than
    working around it by visiting Batches first.

    **Step 3 — confirm persistence.** Reload the admin page, reopen each recipe, and confirm the
    picker still shows the attached template. Then confirm at the source:
    `curl -s "$APPS_SCRIPT_URL?action=get_recipes&server_token=$APPS_SCRIPT_SERVER_TOKEN&status=active"`
    and check all three recipes carry a non-empty `schedule_id`.

    **Rollback if a template turns out to be wrong:** clear the recipe's Fermentation Schedule
    back to "None" and save. The card reverts to its single-column footer (D-09), nothing looks
    broken, and no customer promise is left standing. That is the whole undo.

    Record in `81-SCHEDULES-INVENTORY.md` under "Backfill": which branch was executed, the
    `schedule_id` and day-offset ladder of each template used or created, the three attachments,
    and the derived week figure for each recipe.
  </how-to-verify>

  <resume-signal>Type "attached" with the three recipe→template mappings and their derived day counts, or describe what went wrong</resume-signal>

  <acceptance_criteria>
    - `curl -s "$APPS_SCRIPT_URL?action=get_recipes&...&status=active"` shows a non-empty `schedule_id` on all three of `SV-R-000011`, `SV-R-000003`, `SV-R-000002`
    - Each template used ends with a step whose `is_packaging` is true
    - The ale template's largest non-packaging `day_offset` rounds to 3 weeks (18-24 days inclusive); the lager template's rounds to 5 weeks (32-38 days inclusive)
    - A newly saved template appeared on the very next `get_ferm_schedules` read, not after a delay
    - The schedule dropdown had options on a fresh Recipes-tab-first page load
    - `81-SCHEDULES-INVENTORY.md` has a "Backfill" section recording branch, templates, attachments and derived figures
    - No secret appears in the committed file
  </acceptance_criteria>

  <done>All three active recipes are linked to a fermentation template whose non-packaging ladder matches the owner's stated figures, and the link survives a reload and reads back from the sheet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Release gate — every active recipe renders a timeline, and every admin surface works</name>

  <read_first>
    - `.planning/phases/81-.../81-UI-SPEC.md` §1 through §6 — the binding visual and copy contract for every surface verified here
    - `.planning/phases/81-.../81-CONTEXT.md` D-05, D-06, D-07, D-09, D-10, D-11, D-14, D-15
    - `.planning/phases/81-.../81-06-SUMMARY.md` — including its recorded finding on the D-04 create-batch leg, which must NOT be reported as working
    - `beer.html` — the two rewritten passages
  </read_first>

  <files>.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-SCHEDULES-INVENTORY.md</files>

  <action>
Operator verification against the running staging site. First the unconditional D-10 release gate:
all three active recipe cards on `staging.steinsandvines.ca/beer.html` must render a `Ready in`
column reading `about 3 weeks` (the two ales) or `about 5 weeks` (the lager) over `from brew day`.
Then the D-09 negative case, the two rewritten `beer.html` passages, and the five admin surfaces
(D-11 warn-don't-block save, D-12/D-13/D-14 BeerXML review with no pre-selection and a neutral
compare line, D-15 blast-radius note, D-04 activation pre-selection). Do NOT sign off a
create-batch pre-selection — plan 81-06 recorded that leg as not implementable; report it as an
open finding. Any failure blocks plan 81-09. Full numbered procedure in `<how-to-verify>`.
  </action>

  <verify>
    <automated>curl -s 'https://svmiddleware-staging.up.railway.app/api/recipes?status=active' | grep -c ferment_days</automated>
  </verify>

  <what-built>
    Everything: the derived field, the card column, the rewritten page copy, the recipe editor
    picker and warning, the BeerXML review additions, the template blast-radius note, and the
    batch-activation default. All of it is live on staging with real data attached.
  </what-built>

  <how-to-verify>
    **The release gate first. D-10 makes this unconditional — if it fails, the phase is not done.**

    1. Visit `https://staging.steinsandvines.ca/beer.html`. On the recipe cards, confirm **all
       three** show a second footer column. Each must read the label `Ready in` above two lines:
       `about N weeks` then `from brew day`. West Coast IPA and Hazy Pale Ale should read
       `about 3 weeks`; Czech Lager should read `about 5 weeks`.
    2. Confirm the word is `about`, not `~`, on the card — D-05 governs and it must match the page
       copy.
    3. Confirm the price column is still FIRST and the Ready-in column second, with the gold
       divider between them, and that the two columns split the footer evenly.
    4. Check the same page on a phone-width viewport. The grid collapses to one column under
       480px, so cards get wider, not narrower — the two-line value must not wrap into three.

    **D-09 negative case.** Temporarily clear the Fermentation Schedule on ONE recipe, save,
    reload the public page (allow for the recipe cache TTL), and confirm that card shows a
    single-column footer that looks deliberate — no "TBD", no em-dash, no "0 weeks", no empty
    column. Then re-attach it. This is the only surface where a mistake would look broken to a
    customer, so it is worth proving rather than trusting.

    **Page copy (D-08).** On the same page confirm How It Works step 4 and the FAQ "How long until
    it's ready?" both state the ale/lager split and say the time is counted from brew day, and
    that neither still says the timeline comes at the consult. Confirm the canning/bottling
    sentences after the rewritten part are intact, and that the Phase-80 queue-order copy ("we
    work through the list in order") is untouched.

    **Admin surfaces.** In `staging.steinsandvines.ca/admin.html`:
    5. *D-11:* open a recipe, clear its schedule, set status to Active. An amber (not red) message
       appears saying the recipe won't show a timeline. **Click Save — it must succeed.** A
       blocked save here is a defect, not a safety feature. Re-attach the schedule afterwards.
    6. *D-12/D-13/D-14:* import a BeerXML file that carries `PRIMARY_AGE`/`SECONDARY_AGE`. The
       review modal's meta line ends with `BeerXML: N days ferment`. The schedule dropdown starts
       on "No schedule (add later)" with **nothing pre-selected**, however close a template is to
       the file's number. Choosing a template shows `BeerXML: N days · Template: M days` in plain
       grey text with no warning colour and no icon, even if the two numbers are far apart. Click
       Confirm Import and confirm the recipe form opens with that template already selected —
       this is the carry-through that would otherwise silently discard the choice.
    7. *D-15:* open a template that is now attached to recipes. An amber note reads
       `Used by N public recipe(s). Changing day offsets will change what customers are told.`
       with the correct count and correct singular/plural. Open the create-new-template form and
       confirm NO such note appears. Save an edited template and confirm the note did not block it.
    8. *D-04:* activate a batch whose recipe has a schedule attached. `#sa-schedule-select` opens
       with that template already selected, and you can still change it to any other. Also confirm
       a batch with no linked recipe opens with nothing pre-selected and activates normally.

    **Do not verify a create-batch pre-selection.** Plan 81-06 recorded that the create-batch modal
    has no recipe identity to default from, so that leg of D-04 is deliberately not implemented.
    Report it as an open finding; do not sign it off as working.

    Record each numbered result. Any failure blocks the production cutover in plan 81-09.
  </how-to-verify>

  <resume-signal>Type "gate passed" if all three recipes render their timeline and every admin surface behaves as described, or list the numbered checks that failed</resume-signal>

  <acceptance_criteria>
    - All three active recipe cards on staging show a `Ready in` column; two read `about 3 weeks`, one reads `about 5 weeks`; all three read `from brew day` on the second line
    - `curl -s https://svmiddleware-staging.up.railway.app/api/recipes?status=active | grep -c ferment_days` returns 3
    - That same response still contains none of `schedule_id`, `steps_parsed`, `is_transfer`
    - A recipe with its schedule cleared renders a single-column footer containing no `Ready in`, no `TBD`, no `0 weeks`
    - Saving an active recipe with no schedule SUCCEEDS while showing the amber warning
    - The BeerXML modal's schedule dropdown has no pre-selected template on open
    - The D-14 compare line renders in `--ink-secondary` grey with no warning colour, border or icon
    - Confirm Import carries the selected template into `#recipe-schedule-select`
    - The D-15 note appears when editing an attached template, is absent on create, and does not block save
    - `#sa-schedule-select` opens pre-selected for a batch whose recipe has a schedule, and remains changeable
    - The create-batch leg is reported as an open finding, not as passing
  </acceptance_criteria>

  <done>The D-10 release gate is met on staging with real data, every decision from D-05 through D-15 is confirmed against the running system, and the one unimplemented leg is reported honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous internet → staging middleware `/api/recipes` | The first live exercise of the new public field, against a real server rather than a mock |
| staging admin UI → the shared production Google Sheet | Every write in this plan lands in live production data |
| Cloudflare Access → `staging.steinsandvines.ca` | The staging frontend and its admin surface are gated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-40 | Information disclosure | The live public recipe response | mitigate | Task 1 asserts the negative boundary against the running staging server BEFORE any data is attached, and Task 3 re-asserts it after. Both check the serialized response for `schedule_id`, `steps_parsed` and `is_transfer`. A mocked test proving the same thing is not a substitute for the live check |
| T-81-41 | Elevation of privilege | Anonymous `?status=all` / `?status=draft` | mitigate | Task 1 explicitly re-tests the pre-existing T-74-03 guarantee against the live server, because this phase added a new enrichment step into both read paths and a regression there would expose draft recipes |
| T-81-42 | Tampering | Writing wrong fermentation figures into live production data | mitigate | Ordered so the correct value is known before it is written: plan 81-07 read and sanity-checked the templates first, and Task 2 re-checks the ladder against the 18-24 / 32-38 day bands before attaching. Rollback is documented as clearing one field, which reverts the card to its D-09 single-column state with no standing customer promise |
| T-81-43 | Repudiation | An over-promise reaching a real customer | mitigate | Task 3's release gate reads the actual rendered card, not the API, and checks the figures against the owner's own ~3-week / ~5-week expectation. CONTEXT.md's rule applies: a wildly different number means the template is wrong, not the display. An under-promise is recoverable; an over-promise means a customer turns up for beer that is not ready |
| T-81-44 | Denial of service | A staging deploy breaking the live public recipe path | accept | Staging is a separate Railway service and a separate Pages site; production frontend and middleware are untouched until plan 81-09. The only shared component is the Google Sheet, and the writes here are single-field and reversible |
| T-81-45 | Information disclosure | Secrets pasted into the committed inventory document | mitigate | Same control as plan 81-07: record responses, never request URLs carrying `server_token`. Asserted before commit |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package is installed. RESEARCH.md § "Package Legitimacy Audit" records the phase as installing zero new dependencies |
</threat_model>

<verification>
- `npm test`, `cd zoho-middleware && npm test`, `npm run lint` all exit 0 before the push
- `npm run build` leaves `js/main.js` and `js/main.min.js` unchanged in git status
- Staging `/api/recipes?status=active` returns 3 recipes, 3 `ferment_days` keys after backfill, 0 before
- That response contains no `schedule_id`, `steps_parsed` or `is_transfer`
- All three staging recipe cards render a `Ready in` column with the expected week counts
- Saving an active recipe with no schedule succeeds
- `grep -rc "server_token=" .planning/phases/81-*/` returns 0
</verification>

<success_criteria>
- The D-10 release gate is met: all three active recipes render a timeline
- The figures match the owner's expectation (~3 weeks ales, ~5 weeks lager)
- The public API exposes exactly one new integer and nothing else
- Every admin surface from D-11 through D-15 behaves as its decision specifies, and none of them blocks a save
- The unimplemented D-04 create-batch leg is reported, not glossed
</success_criteria>

<output>
Create `.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-08-SUMMARY.md` when done.

Record: the branch executed, the templates created or reused with their day-offset ladders, the
three attachments, the rendered week figure for each recipe, every numbered verification result
from Task 3, and any check that failed. If the release gate did not pass, say so plainly and do
not proceed to plan 81-09.
</output>
