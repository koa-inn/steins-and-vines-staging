---
phase: 81-recipe-fermentation-timeline-give-customers-an-expected-read
plan: 09
type: execute
wave: 6
depends_on: ["81-08"]
files_modified:
  - docs/RUNBOOK.md
  - .planning/STATE.md
autonomous: false
requirements: [OPS-05]

must_haves:
  truths:
    - "A customer on the real steinsandvines.ca beer page sees when their beer will be ready"
    - "The production page no longer says the timeline comes at the consult"
    - "Nothing internal about fermentation schedules is visible to anyone on the public internet"
    - "The deploy, its rollback path and the phase's open findings are written down where the next person will look"
  artifacts:
    - path: "docs/RUNBOOK.md"
      provides: "Phase 81 deployment outcome record with rollback targets"
      contains: "Phase 81"
  key_links:
    - from: "production beer.html"
      to: "production middleware /api/recipes"
      via: "ferment_days rendered as the Ready in column"
      pattern: "Ready in"
---

<objective>
Ship it. Cut the middleware and frontend over to production, verify the customer-facing result on
the real site, and write down what was deployed, how to undo it, and what is still open.

Purpose: the whole phase exists so that the question "when do I get my beer?" is answered before a
customer books rather than at a consult. Until this plan runs, that is only true on a
Cloudflare-gated staging site.

The data is already live — the three recipes were attached in plan 81-08 against the shared
production sheet, and the Apps Script layer was deployed in 81-07. What remains is the middleware
and frontend cutover, which is the reversible part.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-CONTEXT.md
@.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-07-SUMMARY.md
@.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-08-SUMMARY.md
@docs/RUNBOOK.md
@CLAUDE.md

<interfaces>
DEPLOY REALITY:
- Production frontend: `git push production main --force` → `steinsandvines.ca`.
- `enforce-cname.yml` auto-corrects the domain after every push to either repo. CNAME is
  gitignored; domains are managed in GitHub Pages settings, not the file.
- ALL changes go to staging first. Plan 81-08 did that and passed the release gate.
- The production middleware deploy follows the `docs/RUNBOOK.md` sequence — check whether the
  blessed `gated-deploy.yml` path applies to this change and follow whichever route the runbook
  specifies for a `zoho-middleware/routes/**` change.
- Apps Script is already deployed (plan 81-07) and needs nothing here.
- The Google Sheet is already backfilled (plan 81-08) and needs nothing here.

PRE-CONDITION: do not start this plan unless `81-08-SUMMARY.md` records the D-10 release gate as
PASSED on staging. If it did not pass, this plan does not run.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Cut the middleware and frontend over to production and verify on the live site</name>

  <read_first>
    - `docs/RUNBOOK.md` — the production deploy sequence for a middleware route change and for the frontend, plus the rollback procedure for each
    - `.planning/phases/81-.../81-08-SUMMARY.md` — confirm the release gate passed and note anything it flagged
    - `CLAUDE.md` § Deployment — staging first, `git push production main --force`, CNAME handling
  </read_first>

  <files>docs/RUNBOOK.md</files>

  <action>
Operator production cutover. Re-run all three gates on the shipped commit, confirm `npm run build`
leaves the bundles unchanged, record the current production middleware deployment identifier as
the rollback target, deploy the production middleware per `docs/RUNBOOK.md`'s route for a
`zoho-middleware/routes/**` change, then `git push production main --force` for the frontend and
let `enforce-cname.yml` settle the domain. Verify on the real host: three recipes each carrying
`ferment_days`, none of `schedule_id`/`steps_parsed`/`is_transfer` present, all three cards
rendering the `Ready in` column, both rewritten passages live, the Phase-80 queue copy intact, and
the `beer.html` CSP meta tag byte-identical to the value recorded in `81-03-SUMMARY.md`. Rollback
is per-layer and asymmetric: revert frontend and middleware; the Apps Script column and the sheet's
`schedule_id` values need no revert, and the narrowest undo for a wrong figure is clearing a
recipe's schedule in admin with no deploy. Full procedure in `<how-to-verify>`.
  </action>

  <verify>
    <automated>curl -s https://steinsandvines.ca/beer.html | grep -c "give you a timeline at your consult"</automated>
  </verify>

  <what-built>
    The middleware derives `ferment_days` from the recipe's linked `FermSchedules` template and
    exposes exactly that one integer. The public recipe card renders it as a second footer column
    reading "Ready in / about N weeks / from brew day". Both `beer.html` timeline passages are
    rewritten. Five staff-facing admin surfaces were added or extended. All of it passed the
    release gate on staging in plan 81-08, against the same live Google Sheet production reads
    from — so the data path is already proven end to end.
  </what-built>

  <how-to-verify>
    1. Re-confirm the gates on the exact commit being shipped: `npm test`,
       `cd zoho-middleware && npm test`, `npm run lint`. All three exit 0. Confirm
       `npm run build` leaves `js/main.js` and `js/main.min.js` unchanged in `git status` — a diff
       means stale artifacts would ship.
    2. Record the current production middleware deployment identifier before deploying. That is
       the rollback target.
    3. Deploy the production middleware following `docs/RUNBOOK.md`'s route for a
       `zoho-middleware/routes/**` change.
    4. Deploy the production frontend: `git push production main --force`. Let `enforce-cname.yml`
       run and confirm the domain settled correctly.
    5. Verify against the real site:
       - `curl -s https://<production-middleware-host>/api/recipes?status=active` returns 200 with
         three recipes, each carrying a `ferment_days` integer.
       - The same response contains none of the substrings `schedule_id`, `steps`, `steps_parsed`,
         `is_transfer`. Run this check on production explicitly — it is the D-16 boundary, and it
         is the one thing in this phase that cannot be quietly undone once a scraper has seen it.
       - `https://steinsandvines.ca/beer.html` shows all three recipe cards with a `Ready in`
         column: two reading `about 3 weeks`, one reading `about 5 weeks`, all with
         `from brew day` on the second line.
       - Both rewritten passages are live; `grep` the fetched page for
         "give you a timeline at your consult" and expect zero matches.
       - The Phase-80 queue-order sentence ("we work through the list in order") is still present
         and unchanged.
       - Check the page on a real phone, not just a narrow desktop window.
    6. Confirm no CSP change shipped: diff the production `beer.html` CSP `<meta>` tag against the
       value recorded in `81-03-SUMMARY.md`. This phase added no third-party origin and the tag
       must be identical. A silently altered CSP is how Meta pixel tracking broke site-wide until
       2026-07-22.
    7. Rollback if anything fails: revert the frontend by force-pushing the previous commit to
       `production`, and revert the middleware to the deployment identifier recorded in step 2.
       The Apps Script layer and the sheet data do NOT need to be rolled back — the column is
       inert to old code and an attached `schedule_id` is invisible to a middleware that does not
       read it. If only the figures look wrong, the narrower fix is to clear the recipes'
       Fermentation Schedule in admin, which reverts the cards to the single-column footer with no
       code change at all.
  </how-to-verify>

  <resume-signal>Type "live" with the production middleware deployment id and the recorded rollback id, or "rolled back" with what failed</resume-signal>

  <acceptance_criteria>
    - `npm test`, `cd zoho-middleware && npm test`, `npm run lint` all exit 0 on the shipped commit
    - `npm run build` leaves `js/main.js` and `js/main.min.js` unchanged in `git status --porcelain`
    - Production `/api/recipes?status=active` returns 3 recipes and `grep -c ferment_days` returns 3
    - That response piped through `grep -c -E 'schedule_id|steps_parsed|is_transfer'` returns 0
    - `curl -s https://steinsandvines.ca/beer.html | grep -c "give you a timeline at your consult"` returns 0
    - `curl -s https://steinsandvines.ca/beer.html | grep -c "counted from your brew day"` returns 2
    - `curl -s https://steinsandvines.ca/beer.html | grep -c "we work through the list in order"` is unchanged from its pre-deploy value
    - The production `beer.html` CSP `<meta>` content is byte-identical to the value recorded in `81-03-SUMMARY.md`
    - All three production recipe cards visibly render the `Ready in` column
    - Both the pre-deploy and post-deploy middleware deployment identifiers are recorded
  </acceptance_criteria>

  <done>steinsandvines.ca answers "when do I get my beer?" on every active recipe card, the page copy agrees with the cards, nothing internal leaks, and there is a written rollback path for each layer.</done>
</task>

<task type="auto">
  <name>Task 2: Record the outcome, the rollback targets, and the phase's open findings</name>

  <read_first>
    - `docs/RUNBOOK.md` — the format of the Phase 46 and Phase 51 outcome records, to match
    - `.planning/STATE.md` — the "Current Position" and "Accumulated Context" sections
    - `.planning/phases/81-.../81-06-SUMMARY.md` — the D-04 create-batch finding
    - `.planning/phases/81-.../81-02-SUMMARY.md` — the recorded worst-case cache staleness figure
    - `.planning/phases/81-.../81-07-SUMMARY.md` and `81-08-SUMMARY.md` — version numbers, branch executed, attachments
    - `.planning/REQUIREMENTS.md` line 122 — OPS-05, which this phase closes only the fermentation-time slice of
  </read_first>

  <files>docs/RUNBOOK.md, .planning/STATE.md</files>

  <action>
Append a Phase 81 outcome record to `docs/RUNBOOK.md`, matching the format of the existing Phase
46 and Phase 51 entries. It must state: the date; the Apps Script version deployed and its
rollback version (from `81-07-SUMMARY.md`); the production middleware deployment id and its
rollback id (from Task 1); that the frontend rolls back by force-pushing the previous commit to
`production`; and — importantly — the three-layer rollback asymmetry, namely that the Apps Script
column and the sheet's `schedule_id` values do NOT need reverting because both are inert to code
that does not read them, and that the narrowest possible undo for a wrong figure is clearing a
recipe's Fermentation Schedule in the admin UI, which needs no deploy at all.

Update `.planning/STATE.md`: move Current Position to Phase 81 complete with the date, and add an
Accumulated Context entry recording what shipped and, specifically, the three things a future
phase needs to know:

1. **The D-04 create-batch leg is not implemented.** `buildCreateBatchFormInner` identifies a
   batch subject by Zoho product SKU and its `create_batch` payload carries no `recipe_id`, so
   there is no recipe from which to default a schedule. Only `#sa-schedule-select` (batch
   activation) pre-selects. Closing it needs either a product↔recipe association or a recipe
   picker in that modal — a decision, not a bug fix.
2. **Worst-case template-edit propagation** to the public card is the Apps Script `'gfs'` TTL plus
   the middleware `FERM_SCHEDULES` TTL plus the recipes cache. Quote the figure recorded in
   `81-02-SUMMARY.md`. Plan 81-01 fixed the missing `'gfs'` eviction, so a template edit is
   visible to the next schedules read, but the middleware-side TTL still applies and there is no
   bust point for it because schedule CRUD does not route through the middleware.
3. **OPS-05 is NOT closed.** This phase deliberately carved out only the fermentation-time slice.
   Hop timing, mash steps and hop-unit normalization remain in Phase 66. Do not tick OPS-05 in
   `REQUIREMENTS.md`.

Also record in STATE.md the deferred items this phase kept out on purpose, so they are not
rediscovered as gaps: an auto-computed expected packaging date on the batch (now cheap, since
D-03/D-04 established the link), a per-recipe timeline override (declined — fork the template
instead), and BeerXML `AGE` as a second customer-facing number (declined under D-01).

Do NOT mark OPS-05 complete in `.planning/REQUIREMENTS.md`.
  </action>

  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && grep -c "Phase 81" docs/RUNBOOK.md && grep -c "Phase 81" .planning/STATE.md && grep -c "OPS-05" .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Phase 81" docs/RUNBOOK.md` returns at least 1 and the entry names both the Apps Script version and its rollback version
    - The RUNBOOK entry contains the production middleware deployment id and its rollback id
    - The RUNBOOK entry states that the Apps Script column and sheet `schedule_id` values do not require rollback
    - `.planning/STATE.md` records Phase 81 complete with the date
    - `.planning/STATE.md` contains all three numbered findings, including the literal phrase `create-batch` and the fact that OPS-05 remains open
    - `.planning/REQUIREMENTS.md` line 122 still shows OPS-05 as `[ ]` unchecked
    - `grep -rc "server_token=" .planning/phases/81-*/ docs/RUNBOOK.md` returns 0
  </acceptance_criteria>

  <done>The deploy, its per-layer rollback path, the three open findings and the deliberately-deferred ideas are all written where the next person will look, and OPS-05 is honestly still open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → production middleware `/api/recipes` | The real public boundary, with real traffic and real scrapers |
| public internet → `steinsandvines.ca` static pages | CSP-governed; a silent change here has previously broken a service site-wide |
| operator → production deploy controls | The irreversible-by-default step of the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-46 | Information disclosure | Production public recipe response | mitigate | The negative boundary assertion (`schedule_id`, `steps`, `steps_parsed`, `is_transfer` absent) is re-run against the PRODUCTION host, not inferred from staging. This is singled out because it is the one effect in the phase that cannot be undone by a rollback once observed — a reverted deploy does not un-see a leaked field |
| T-81-47 | Tampering | CSP drift on a live public page | mitigate | Task 1 diffs the production `beer.html` CSP meta tag against the value recorded in `81-03-SUMMARY.md` and requires byte-equality. The site has a documented incident (2026-07-22) where a missing CSP domain silently disabled tracking for every visitor |
| T-81-48 | Denial of service | A bad cutover taking the public beer page down | mitigate | Rollback targets for both the middleware and the frontend are recorded BEFORE deploying, and both routes are stated explicitly. The change is additive — a recipe card without `ferment_days` renders exactly as it does today (D-09) — so the failure mode is a missing column, not a broken page |
| T-81-49 | Repudiation | An over-promise reaching real customers | mitigate | Task 1 reads the actual rendered production cards and checks the week figures against the owner's ~3-week / ~5-week expectation. The narrowest undo — clearing a recipe's Fermentation Schedule in admin, no deploy required — is documented in the RUNBOOK so it is available under time pressure |
| T-81-50 | Repudiation | Silently closing OPS-05 | mitigate | This phase covers only the fermentation-time slice; hop timing, mash steps and hop-unit normalization stay in Phase 66. Task 2 explicitly forbids ticking OPS-05, and an acceptance criterion asserts it is still `[ ]` |
| T-81-51 | Information disclosure | Secrets in the RUNBOOK or STATE | mitigate | Deployment identifiers and version numbers are recorded; tokens and URLs carrying `server_token` are not. Asserted by grep across the phase directory and `docs/RUNBOOK.md` before commit |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package is installed. RESEARCH.md § "Package Legitimacy Audit" records the phase as installing zero new dependencies |
</threat_model>

<verification>
- Production `/api/recipes?status=active` returns 3 recipes each with `ferment_days`, and leaks none of the forbidden keys
- All three cards on `https://steinsandvines.ca/beer.html` render the `Ready in` column with correct week counts
- `curl -s https://steinsandvines.ca/beer.html | grep -c "give you a timeline at your consult"` returns 0
- The production `beer.html` CSP meta tag is byte-identical to the recorded pre-phase value
- `docs/RUNBOOK.md` and `.planning/STATE.md` both carry the Phase 81 record
- `.planning/REQUIREMENTS.md` still shows OPS-05 unchecked
- `grep -rc "server_token=" .planning/phases/81-*/ docs/RUNBOOK.md` returns 0
</verification>

<success_criteria>
- A customer on the real site sees when their beer will be ready, on every active recipe card
- The page copy and the cards agree with each other
- Exactly one new integer crosses the public boundary; nothing internal does
- Rollback paths for all three layers are written down, including the no-deploy narrow undo
- The three open findings and the deferred ideas are recorded; OPS-05 is honestly still open
</success_criteria>

<output>
Create `.planning/phases/81-recipe-fermentation-timeline-give-customers-an-expected-read/81-09-SUMMARY.md` when done.

Record the production deployment identifiers, every verification result from Task 1, and restate
the three open findings so the phase's final artifact carries them.
</output>
