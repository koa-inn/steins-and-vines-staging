---
phase: 81
slug: recipe-fermentation-timeline-give-customers-an-expected-read
status: draft
shadcn_initialized: false
preset: not applicable
created: 2026-09-05
---

# Phase 81 — UI Design Contract

> Visual and interaction contract for the "Ready in" timeline surfaced on public beer recipe cards, `beer.html` copy, and three staff-only admin surfaces (recipe editor, BeerXML review modal, FermSchedules template editor). Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**This is a static-HTML / vanilla-ES5 / hand-written-CSS project. No component library, no shadcn, no Tailwind, no build-time CSS framework.** This contract extracts and cites the design system that already ships on production and specifies exactly how this phase's one new public element (a second `.price-col` on the recipe card) and three admin-surface additions reuse it. All values below are read directly from the repository during this research session (2026-09-05) and cited to file:line.

**No CSP change.** This phase adds no third-party service, script, or origin to any public page — confirmed by reading `docs/TRACKING.md`'s trigger list (new/removed tracking service) against this phase's scope (a derived integer rendered via existing `document.createElement`/`textContent`, no new fetch target). No CSP `<meta>` edit is expected on `beer.html` or any other public page.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — hand-written CSS in `css/styles.css` (public) and `css/admin.css` (staff), both already loaded by the pages this phase touches |
| Icon library | none — no icon is introduced by this phase |
| Font | Public card: `--font-body` 'Lato' (already loaded, see Typography note on `--font-condensed`). Admin surfaces: system stack via `--font-body`/`--font-display` tokens already in `css/admin.css` |
| Frontend files touched | `js/modules/07-catalog-kits.js` (`buildRecipeCard`), `beer.html` (2 copy passages), `js/admin.js` (`showBeerXMLReviewModal`, `_buildBeerXMLRowHTML`, `renderScheduleForm`, recipe editor load/save, batch-create/activate schedule dropdowns), `admin.html` (recipe editor form group) |
| CSS touched | `css/styles.css` (additive rules next to `.label-beer .price-*`, ~line 4714-4718) and `css/admin.css` (reuse existing `.availability-banner`/`.recipes-form-field` families — no new admin CSS class is required, see §3/§5) |

---

## Spacing Scale

No new spacing values are introduced. Every measurement this phase adds is inherited verbatim from an existing sibling rule:

| Context | Value | Source |
|---------|-------|--------|
| Public card `.price-col` padding | `0.6rem 0.5rem` = **9.6px / 8px** | `.label-beer .price-col`, `css/styles.css:4715` — reused unchanged, not reauthored. **Disclosure:** 9.6px is NOT a multiple of 4 and sits outside the standard set. Pre-existing shared production CSS, not authored here — see "Pre-existing debt" below |
| Public card column divider | `1px solid rgba(184,150,62,0.3)` | `.label-beer .price-col + .price-col`, `css/styles.css:4716` — already fires automatically once a 2nd `.price-col` exists; no new rule needed |
| Admin form spacing | `var(--sp-1)`/`var(--sp-3)`/`var(--sp-4)`/`var(--sp-6)` tokens (4/12/16/24px) | `css/admin.css:44-49`, already used throughout `renderScheduleForm`/the recipe editor and the BeerXML modal's own inline styles (`gap:var(--sp-4)`, `margin-top:var(--sp-6)`, `js/admin.js:9527`) |

**Pre-existing debt — disclosed, not inherited silently.** Two reused values fall outside the {4, 8, 16, 24, 32, 48, 64} standard set:

1. `.label-beer .price-col` padding `0.6rem` = **9.6px** (`css/styles.css:4715`).
2. Admin token `var(--sp-3)` = **12px** (`css/admin.css:44-49`).

Both are shared, live production values this phase *reuses* rather than reauthors — the same category as the pre-existing `.price-label` 3.7:1 contrast shortfall disclosed in the Color section, and handled the same way: named openly, not fixed here. Changing either would mean editing CSS shared site-wide (every kit and recipe card for the first; every admin form for the second), which is disproportionate to this feature phase and would put unrelated surfaces at risk. **Recorded as known debt so it is not mistaken for compliance, and not silently re-copied as precedent by a future phase.**

Exceptions: none newly introduced by this phase.

---

## Typography

| Role | Size | Weight | Line Height | Family | Source |
|------|------|--------|-------------|--------|--------|
| Public — price-col label (both "Ferment in store" and new "Ready in") | 9px (0.5625rem) desktop / 10px (0.625rem) mobile floor | 700 | 1 (uppercase, single line) | `--font-body` (inherited, no family declared) | `.label-beer .price-label`, `css/styles.css:4717`; mobile floor `css/styles.css:5175-5179` |
| Public — new "Ready in" value, line 1 ("about 3 weeks") | 13px (0.8125rem) | 600 | 1.3 | `--font-body` 'Lato' — **deliberately NOT `--font-condensed`** | New rule, this phase (see §1) |
| Public — new "Ready in" value, line 2 ("from brew day") | 11px (0.6875rem) | 400 | 1.3 | `--font-body` 'Lato' | New rule, this phase (see §1) |
| Public — existing price value ("$X") for comparison | 17.6px (1.1rem) | 600 | normal | `--font-condensed` 'Oswald' (see flag below) | `.label-beer .price-value`, `css/styles.css:4718` — unchanged |
| Admin — form field label | 13px | 700 | normal | inherited | `.recipes-form-field label`, `css/admin.css:3031` |
| Admin — inline warning text (D-11/D-15) | 13px | 600 | normal | inherited | `.availability-banner`, `css/admin.css:3059-3065` — reused verbatim |
| Admin — BeerXML meta line (D-12 extension) | inherited from `.beerxml-meta-line` (no new size) | inherited | inherited | inherited | `js/admin.js:9508-9511` builds this string; CSS class pre-existing |

**Flag, not a fix (matches the pattern Phase 74 already documented):** `--font-condensed: 'Oswald'` (`css/styles.css:133`) is referenced by `.label-beer .price-value`/`.beer-name`/`.subcategory` but Oswald is never `<link>`-loaded on any public page — it silently falls back to `Impact`. This is pre-existing, unrelated to this phase, and out of scope to fix here. **The new "Ready in" value deliberately uses `--font-body` (Lato, which IS loaded)**, the same sidestep Phase 74 used for its recipe card — do not give the new text `--font-condensed` and inherit a fallback-font bug.

Only 3 new type roles are introduced (Ready-in label reuses an existing role; the two value lines are new). This stays within the template's 3-4 role guidance while matching what the narrow card column can actually hold (see §1's wrap rationale).

---

## Color

No new color token or hex value is introduced. This phase reuses the beer-card's existing gold/burgundy palette and the admin panel's existing warning palette.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (~60%) — public card | `#faf8f2` (`.label-beer` background) | Card surface, `css/styles.css:4679` |
| Secondary (~30%) — public card | `rgba(184,150,62,0.06)` gold-tinted footer wash | `.price-footer` background, `css/styles.css:4714` — both price-col AND the new Ready-in col sit on this |
| Accent (~10%) — public card | `--color-burgundy` `#370e13` | **Reserved for:** the price value only (unchanged). The new "Ready in" value is deliberately NOT burgundy — see contrast/hierarchy note below |
| New — public card, Ready-in value | `--color-muted-dark` `#4f4f4f` (existing token, `css/styles.css:129`) | Both lines of the new value ("about 3 weeks" / "from brew day") — chosen specifically so it does not compete with the burgundy price (see §1) |
| Admin — warning (D-11/D-15) | `--batch-warning` `#b87a1a` on `--batch-warning-bg` `rgba(184,122,26,0.08)`, left border `--batch-warning` | Reused verbatim from `.availability-banner--low`, `css/admin.css:3072-3076` — no new semantic color |
| Destructive | Not applicable — this phase adds no destructive action | — |

**Contrast — computed against the ACTUAL composited background, not the nominal card color** (this project has a documented history of shipped contrast failures: Phase 78's waitlist form rendered at ratio 1.00; `CONCERNS.md` H14 is open). `.price-footer`'s `rgba(184,150,62,0.06)` over the card's `#faf8f2` composites to approximately `rgb(246,242,231)`, not pure `#faf8f2`. Computed against that real background:

| Pairing | Ratio | Verdict |
|---------|-------|---------|
| `.price-label` `#96792e` (9px, existing, unchanged) | **3.7:1** | **Fails AA for small text (needs 4.5:1).** Pre-existing on production today (identical color/size already used for "Ferment in store" and "Kit only" labels) — NOT introduced by this phase, since D-07 requires reusing this exact class unmodified. Flagging per this role's mandate; not this phase's to fix (would require repainting an already-shipped label used site-wide on both wine and beer cards). |
| New Ready-in value, both lines, `--color-muted-dark` `#4f4f4f` solid | **7.3:1** | Passes AA comfortably at both 13px and 11px. |
| Existing `.price-value` `--color-burgundy` on same background | **15.2:1** | Passes AAA, unchanged. |

**Explicit rejection, recorded so it isn't silently reintroduced:** an earlier draft of this spec considered dimming the "from brew day" line with `opacity: 0.8` for extra visual quietness. Computed contrast at that opacity drops to **4.44:1 — below the 4.5:1 AA floor for small text.** Do not apply opacity to either Ready-in value line; use the solid `--color-muted-dark` token at full opacity for both lines instead (differentiate them by font-size/weight only, per §1).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Public card — "Ready in" label | **"Ready in"** (exact string, D-07) |
| Public card — "Ready in" value, weeks ≥ 2 | **"about N weeks"** then, on its own line, **"from brew day"** — e.g. `ferment_days=21` → "about 3 weeks" / "from brew day"; `ferment_days=35` → "about 5 weeks" / "from brew day" |
| Public card — "Ready in" value, weeks = 1 | **"about 1 week"** (singular, no "s") / "from brew day" |
| Public card — no schedule / unusable offset | **Column omitted entirely.** No "TBD", no "—", no "0 weeks" (D-09). See §1 for the exact omission rule. |
| `beer.html` — How It Works step 4 (`beer.html:179`) | See §2 for full replacement text |
| `beer.html` — FAQ "How long until it's ready?" (`beer.html:317-318`) | See §2 for full replacement text |
| Admin — recipe editor, D-11 warning (active recipe, no schedule) | **"This recipe won't show a timeline on its card — attach a fermentation schedule, or set status to Draft."** |
| Admin — FermSchedules editor, D-15 blast-radius note | **"Used by N public recipe(s). Changing day offsets will change what customers are told."** (singular/plural on N; omit the sentence entirely when N = 0 — see §5) |
| Admin — BeerXML review modal, D-12 meta-line addition | Appended to the existing meta line: **" · BeerXML: 35 days ferment"** (only when a timing field was found; omitted if the file carried none — see §4) |
| Admin — BeerXML review modal, D-14 comparison (after a template is picked) | **"BeerXML: 35 days · Template: 21 days"** (exact D-14 string, no judgement language, no threshold) |
| Destructive confirmation | None introduced by this phase. |

---

## Registry Safety

N/A — no component registry, no shadcn, no new npm/CDN package. Checker: treat Dimension 6 as N/A/PASS.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| — | — | not applicable |

---

## Component Inventory — what's reused vs. net-new

| Component | Status | Source |
|-----------|--------|--------|
| `.price-footer` / `.price-col` / `.price-label` / `.price-value` idiom | **Reuse, unchanged classes** — the multi-column case is NOT new: `buildLabelPriceFooter()` already renders 1 or 2 `.price-col` children conditionally for wine/cider kit cards ("Ferment in store" + "Kit only") | `js/modules/04-label-cards.js:104-159`; CSS `css/styles.css:4714-4716` — this is the load-bearing precedent for D-09's single-column omission (proven in production today) |
| `buildRecipeCard()` | **Extended, not rewritten** — one conditional block added after the existing price-col, before `card.appendChild(footer)` | `js/modules/07-catalog-kits.js:181-238` |
| `.availability-banner` / `.availability-banner--low` | **Reused verbatim** for both D-11 and D-15 warnings — this is the established "warn, don't block" visual language already shipping for ingredient-stock warnings | `css/admin.css:3054-3076` |
| `.recipes-form-grid` / `.recipes-form-field` | **Reused, one new field added to an existing grid** | `admin.html:508-521`; CSS `css/admin.css:3013-3037` |
| `#batch-schedule-select` / `#sa-schedule-select` | **Reused unchanged markup; behavior-only change** (D-04 pre-selection) — no new CSS | `js/admin.js:6997-7000`, `:7346-7352` |
| BeerXML review modal (`admin-modal-content--wide`, `.beerxml-review-table`, `.beerxml-meta-line`) | **Extended** — new `<select>` + comparison `<p>` inserted into the existing `bodyHTML` string, same modal, same wide-table pattern | `js/admin.js:9500-9530` |
| `renderScheduleForm()` | **Extended** — one new `<p>` inserted after the modal's opening summary fields | `js/admin.js:7576-7595` |
| New CSS — `.ferment-time-value` / `.ferment-time-weeks` / `.ferment-time-start` | **Net new, ~10 lines**, additive next to `.label-beer .price-value` | See §1 |

---

## Phase-Specific Decisions

### 1. Public recipe card — the "Ready in" `.price-col` (D-07, D-09)

**Precedent that de-risks this entirely:** `buildLabelPriceFooter()` (`js/modules/04-label-cards.js:104-159`) already renders a `.price-footer` with ONE `.price-col` when only `instore` price exists, or TWO when both `instore` and `kit` exist — and `.label-beer .price-col + .price-col { border-left: 1px solid rgba(184,150,62,0.3); }` (`css/styles.css:4716`) already draws the divider automatically the moment a second column is present. **This is a proven, shipped, owner-approved (2026-09-01 UAT) pattern for exactly the single-vs-multi-column case D-09 requires.** No new "what does 1 column look like" CSS is needed — it already looks deliberate today on kit cards, and recipe cards inherit the identical rule set (`.label-beer` is shared).

**DOM insertion point** (inside `buildRecipeCard()`, `js/modules/07-catalog-kits.js`): immediately after the existing block that builds `priceCol`/`priceLabel`/`priceValue` and calls `footer.appendChild(priceCol)`, and BEFORE `card.appendChild(footer)`:

```js
// existing, unchanged:
footer.appendChild(priceCol);

// NEW — conditional second column (D-07/D-09), createElement-only (T-74-12):
var fermTime = fermentTimeDisplay(recipe); // see helper below
if (fermTime) {
  var timeCol = d.createElement('div');
  timeCol.className = 'price-col';
  var timeLabel = d.createElement('div');
  timeLabel.className = 'price-label';
  timeLabel.textContent = 'Ready in';
  timeCol.appendChild(timeLabel);
  var timeValue = d.createElement('div');
  timeValue.className = 'price-value ferment-time-value';
  var weeksLine = d.createElement('span');
  weeksLine.className = 'ferment-time-weeks';
  weeksLine.textContent = fermTime.weeks;
  var startLine = d.createElement('span');
  startLine.className = 'ferment-time-start';
  startLine.textContent = fermTime.start;
  timeValue.appendChild(weeksLine);
  timeValue.appendChild(startLine);
  timeCol.appendChild(timeValue);
  footer.appendChild(timeCol);
}

card.appendChild(footer); // existing, unchanged
```

**Column order (resolves the "left or right of price" question):** price stays FIRST (col 1, unchanged position), "Ready in" is col 2 — matches the CONTEXT `<specifics>` target reading verbatim ("Ferment in store / $X" beside "Ready in / about 3 weeks…") and is the minimal-diff insertion (append, don't reorder). Both columns are `flex: 1` (existing rule) → an even 50/50 split when both are present; the existing single-column footer is untouched when `fermTime` is null.

**Helper function — `fermentTimeDisplay(recipe)`** (pure, colocated with `recipeDisplayPrice()` in the same module):

```js
function fermentTimeDisplay(recipe) {
  var days = recipe && recipe.ferment_days;
  if (typeof days !== 'number' || !isFinite(days) || days <= 0) return null;
  var weeks = Math.round(days / 7);
  if (weeks < 1) return null; // D-09: sub-week values are unusable, omit rather than show "0 weeks"
  var weekWord = weeks === 1 ? 'week' : 'weeks';
  return { weeks: 'about ' + weeks + ' ' + weekWord, start: 'from brew day' };
}
```
This resolves BOTH of the phase's open "Claude's Discretion" items in one place: the rounding implementation (`Math.round(days/7)`) and the D-09 unusable-value floor (any `ferment_days` that rounds to 0 weeks — i.e. under ~4 days — is treated identically to "no schedule at all": the column is omitted, not shown as "0 weeks"). `recipe.ferment_days` is the ONE derived integer D-16 puts on the public payload — the frontend never sees `schedule_id` or step data, so this function is the sole place the "about N weeks" phrasing is owned, exactly as D-16 requires.

**Wrapping — why two explicit `<span>`s, not one wrapped string:** at the live launch width (`.product-grid--compact`: `minmax(280px, 320px)` cards, confirmed live for exactly-3-card grids per `js/modules/07-catalog-kits.js:339`), each `.price-col` has roughly 130-140px of usable text width after padding/border. The full phrase "about 3 weeks from brew day" (27 characters) will NOT fit on one line at any readable size. Rather than let the browser wrap it unpredictably (risking an ugly break like "about 3 weeks from" / "brew day"), the DOM forces the break at the natural clause boundary: line 1 = the week count ("about 3 weeks"), line 2 = the start point ("from brew day"), via two `display: block` spans. This guarantees the same two-line shape at 1, 3, or 5 weeks and at any card width ≥ ~110px.

**Visual weight — why the value is NOT styled like the price:** the existing `.price-value` (burgundy, 17.6px, `--font-condensed` bold) is calibrated to be the card's second-most-prominent element after the beer name — appropriate for a price, which is the customer's primary decision input. "Ready in" is supporting context, not a competing decision point, so it must read as visually secondary. New CSS:

```css
.label-beer .price-value.ferment-time-value {
  font-family: var(--font-body);
  font-size: 0.8125rem;   /* 13px */
  font-weight: 600;
  color: var(--color-muted-dark);
  line-height: 1.3;
}
.label-beer .ferment-time-weeks,
.label-beer .ferment-time-start {
  display: block;
}
.label-beer .ferment-time-start {
  font-size: 0.6875rem;   /* 11px */
  /* No font-weight declared — inherits the body default (400). Declaring it
     explicitly would make this one component carry three declared weights
     (700/600/400); inheriting keeps it at two and the rendered result is
     identical. */
  color: var(--color-muted-dark);
}
```
No `opacity` on either line — see the Color section's explicit rejection (would drop below AA). The 13px/11px sizes both already clear the site's mobile "readability floor" pattern (`css/styles.css:5175-5179` bumps 9px labels to 10px on mobile) without needing a matching floor rule of their own, since they start above that floor.

**Narrow-width / mobile behavior:** `.product-grid--compact` collapses to `grid-template-columns: 1fr` under 480px (`css/styles.css:1571-1575`, established in Phase 74), so mobile cards get MORE width, not less — no additional narrow-width CSS is needed for the 2-column footer.

**Alignment:** unchanged — `.price-col { text-align: center }` (`css/styles.css:4715`) already centers both columns; the new column inherits this, no override.

### 2. `beer.html` copy — both passages rewritten verbatim (D-06, D-08)

Exact replacement text for both passages. Both name the start point explicitly (D-06) and never promise an exact date (D-05) — the precise packaging day is still something staff confirm, consistent with D-01 (the number counts to ready-to-package, not ready-to-drink).

**Passage 1 — How It Works, step 4 (`beer.html:179`), replace the `<p class="how-it-works-step-desc">` content:**

> We look after your batch while it ferments &mdash; typically about <strong>3 weeks</strong> for ales and about <strong>5 weeks</strong> for lagers, counted from your brew day. Each recipe's card shows its own estimate, and we'll confirm your exact packaging day as your batch gets close. Most of our beer gets canned, and <strong>canning is done with you here</strong> &mdash; about <strong>half an hour</strong> for a standard 30&nbsp;L batch. Prefer bottles? Crown tops and flip-tops are welcome; they just take a little longer.

(Only the first two sentences change — "how long depends on the style, and we'll give you a timeline at your consult" is replaced. The canning/bottling sentences that follow are untouched, verbatim.)

**Passage 2 — FAQ "How long until it's ready?" (`beer.html:317-318`), replace the `<p>`:**

> It depends on the style &mdash; typically about <strong>3 weeks</strong> for ales, about <strong>5 weeks</strong> for lagers, counted from your brew day. Each recipe's card on this page shows its own estimate. We'll confirm the exact day you come in to package it as your batch gets close.

**Why these specific sentences and not a rewrite of the whole page:** D-08 scopes exactly these two passages — nothing else on `beer.html` (the "So when do I actually need to come in?" FAQ, the queue-order copy frozen by Phase 80 D-14, the "What's Included" section) is touched. Both replacements keep the page's existing voice (short sentences, em-dashes, a rhetorical-question FAQ style) and both stay consistent with the Phase 80-frozen queue copy ("we work through the list in order") since neither passage mentions queue position at all — different sentences, different subject, exactly as the CONTEXT note under D-08 says.

### 3. Admin — recipe editor schedule picker + D-11 warning

**Placement:** a 5th field added to the EXISTING `.recipes-form-grid.recipes-form-grid--narrow` group that currently holds Batch Size / ABV / IBU / Colour (`admin.html:508-521`). Adding a field to a 4-column narrow grid makes it 5 — at `repeat(4, 1fr)` (`css/admin.css:3020-3022`) a 5th item simply wraps to a new row at the same column width; no grid-template change needed. (On mobile the grid already collapses to `1fr` per `css/admin.css:3025-3028`, so the wrap is a non-event there.)

```html
<div class="recipes-form-field">
  <label for="recipe-schedule-select">Fermentation Schedule</label>
  <select id="recipe-schedule-select" class="admin-select">
    <option value="">None</option>
    <!-- populated from fermSchedulesData, same option-building loop as
         #batch-schedule-select (js/admin.js:6997-7000) -->
  </select>
  <span id="recipe-schedule-warning" class="recipes-inline-error recipes-inline-error--warning"></span>
</div>
```

**D-11 warn-don't-block:** reuse the `.availability-banner`/`.availability-banner--low` visual language (warning amber, `css/admin.css:3072-3076`), NOT the existing `.recipes-inline-error` class as-is (that class is styled in `--batch-danger` red, `css/admin.css:3046-3051` — reusing it unmodified for a non-blocking warning would visually claim this is an error/save-blocker, which D-11 explicitly says it is not). Concretely: `#recipe-schedule-warning` gets a small new modifier,

```css
.recipes-inline-error--warning {
  color: var(--batch-warning);
}
```
(one line — reuses the existing token, changes only the color of the existing `.recipes-inline-error` block-level/font-size rules from red to amber). Populate its `textContent` with the copywriting-contract string whenever `recipe-status` is `active` AND `recipe-schedule-select` is empty; clear it otherwise. This mirrors the existing `renderAvailabilityBanner()` pattern (`js/admin.js:8791` on) of a live, non-blocking inline message tied to form state, and — critically — it must NOT be added to `validateActivation()` (`js/admin.js:9060-9067`), which is the actual save-blocking gate; D-11 is explicit that this warning never prevents a save.

**Load/save wiring:** `populateRecipeForm()` (`js/admin.js:8776-8779` region) sets `document.getElementById('recipe-schedule-select').value = r.schedule_id || ''`; `saveRecipe()`'s `formData` object (`js/admin.js:9079-9082` region) adds `schedule_id: document.getElementById('recipe-schedule-select').value || null`. No new visual element beyond the select + the reused warning span.

**iPad Safari touch target:** `.admin-select` is the same control class already used site-wide in this form (Batch Size input, Pricing Mode select, etc.) — no new touch-target work; it inherits whatever height that class already provides.

### 4. Admin — BeerXML review modal (D-12, D-13, D-14)

**Meta-line extension (D-12):** `showBeerXMLReviewModal()`'s existing `metaLine` builder (`js/admin.js:9508-9511`) gains one more conditional segment, following the exact same `metaLine ? ' · ' : ''` join pattern already used for style/ABV/batch-size:

```js
if (parsed.ferment_days_beerxml) {
  metaLine += (metaLine ? ' &middot; ' : '') + 'BeerXML: ' + parsed.ferment_days_beerxml + ' days ferment';
}
```
`parsed.ferment_days_beerxml` is the value `parseBeerXML()` derives from `PRIMARY_AGE`/`SECONDARY_AGE`/`TERTIARY_AGE` (D-12) — display-only, per D-01 this is informational, never written to the recipe. If no timing field was present in the file, this segment is omitted entirely (same omission convention as style/ABV/batch-size today) — never "BeerXML: 0 days" or "BeerXML: unknown".

**Template dropdown (D-12/D-13) — placement:** inserted into `bodyHTML` (`js/admin.js:9515-9526`) directly below the `<p class="beerxml-meta-line">` and above the ingredient `<table>`, as its own block — NOT inside the table, since it's a recipe-level field, not a per-ingredient row:

```html
<div class="form-group" style="margin-top:var(--sp-4);">
  <label for="beerxml-schedule-select">Fermentation Schedule</label>
  <select id="beerxml-schedule-select" class="admin-select">
    <option value="">No schedule (add later)</option>
    <!-- populated from fermSchedulesData; category-filtered to 'beer' first,
         with a divider before other categories — filtering, not hiding,
         since a beer recipe may legitimately reuse a generic template -->
  </select>
  <p id="beerxml-schedule-compare" class="beerxml-schedule-compare"></p>
</div>
```
**No pre-selection (D-13, hard requirement):** the `<option>` defaults to the empty "No schedule" value regardless of how close any template's offset is to `parsed.ferment_days_beerxml` — do not add nearest-match logic, do not default-select anything.

**D-14 comparison line — appears only after selection, no threshold:** wire a `change` listener on `#beerxml-schedule-select` that populates `#beerxml-schedule-compare` (empty by default, so it renders nothing until a template is chosen):

```js
scheduleSelect.addEventListener('change', function () {
  var compareEl = document.getElementById('beerxml-schedule-compare');
  var sched = fermSchedulesData.find(function (s) { return s.schedule_id === scheduleSelect.value; });
  if (!sched || !parsed.ferment_days_beerxml) { compareEl.textContent = ''; return; }
  var templateDays = maxNonPackagingOffset(sched); // see §5 helper
  compareEl.textContent = 'BeerXML: ' + parsed.ferment_days_beerxml + ' days · Template: ' + templateDays + ' days';
});
```
**Styling — deliberately neutral, no warning color, no icon, regardless of how far apart the two numbers are** (D-14's own text: "no warning threshold and no judgement"):
```css
.beerxml-schedule-compare {
  font-size: 13px;
  color: var(--ink-secondary);
  margin-top: var(--sp-1);
}
```
This uses the same neutral secondary-ink color as ordinary body/meta text elsewhere in the admin panel (`--ink-secondary`, `css/admin.css:10`) — explicitly NOT `--batch-warning` or `--batch-danger`, which would visually imply the disagreement is a problem to fix. A human is meant to read both numbers and decide; the UI must not pre-judge for them.

**Modal width:** no change — the existing `admin-modal-content--wide` class (already applied for the 5-column table, `js/admin.js:9501-9503`) already accommodates one more form-group row above the table; it does not need to widen further for a single select + one line of text.

**iPad Safari:** `#beerxml-schedule-select` uses the same `.admin-select` class as every other dropdown in this modal — same touch target, no new sizing rule.

### 5. Admin — FermSchedules template editor, D-15 blast-radius note

**Placement:** inserted into `renderScheduleForm()`'s `html` string (`js/admin.js:7595-7601`) immediately after the Category `<select>` block and before the "Fermentation Steps" `<h4>` — i.e. it sits with the template's identity fields (name/description/category), not buried among the day-offset step rows it's warning about:

```js
if (isEdit) {
  var usedByCount = countRecipesUsingSchedule(existing.schedule_id); // new small helper, counts active+draft public recipes referencing this schedule_id
  if (usedByCount > 0) {
    html += '<p class="availability-banner availability-banner--low" style="margin-bottom:var(--sp-4);">'
          + 'Used by ' + usedByCount + ' public recipe' + (usedByCount === 1 ? '' : 's') + '. '
          + 'Changing day offsets will change what customers are told.</p>';
  }
}
```
**Only shown when editing an existing template that IS attached to ≥1 recipe** (`openEditScheduleModal`, not `openCreateScheduleModal` — a brand-new template has no usage yet, so the sentence is factually empty and must not render, matching D-09's own "omit rather than show a zero" convention applied here to an admin surface).

**Reuses `.availability-banner--low` verbatim** — the exact same amber warn-don't-block visual language as §3's D-11 warning and the existing ingredient-stock warnings, keeping "a soft warning that doesn't block the save" as ONE consistent visual pattern across this phase's three warn-don't-block surfaces (D-11 recipe editor, D-15 schedule editor) rather than inventing a second warning style.

**Does not block `#sched-submit`** — no change to the existing submit handler's validation (`js/admin.js:7681-7691`, name + step-title checks only). The blast-radius note is informational only, consistent with D-15's explicit "does not block."

### 6. Batch creation/activation — pre-selection only, no new visual element (D-04)

`#batch-schedule-select` (`js/admin.js:6997-7000`) and `#sa-schedule-select` (`js/admin.js:7346-7352`) are unchanged in markup and CSS. The only change is behavioral: whichever function opens these modals for a specific recipe sets `select.value = recipe.schedule_id || ''` immediately after the existing `fermSchedulesData.forEach(...)` option-building loop, before the modal is shown — a default value in an already-existing, already-editable control. No new UI element, no new copy, nothing for the checker to visually verify beyond "the right option is pre-highlighted, and the dropdown remains changeable."

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS (N/A — no registry)

**Approval:** pending
