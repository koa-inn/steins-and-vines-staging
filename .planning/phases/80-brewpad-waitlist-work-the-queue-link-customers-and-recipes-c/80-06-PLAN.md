---
phase: 80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c
plan: 06
type: execute
wave: 4
depends_on: ["80-01", "80-02", "80-03", "80-04", "80-05"]
files_modified:
  - .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md
  - .planning/phases/78-brewpad-waitlist-tracking-make-the-beer-waitlist-a-workable-/78-CUTOVER.md
autonomous: false
requirements: [D-05, D-06, D-14, D-17, D-18, D-20, D-23]

must_haves:
  truths:
    - "The six new columns are added to the Waitlist sheet BEFORE the Apps Script is redeployed (D-18)"
    - "The new columns are appended as columns H through M, strictly after notes, never inserted between existing ones (RESEARCH Pitfall 1)"
    - "There is exactly one Apps Script redeploy for this phase, updating the existing deployment as a new version — the /exec URL never changes (D-20)"
    - "The prior and new Apps Script version numbers and the deployment ID are recorded, so the rollback procedure has a target"
    - "The contact email wording and the Cal.com event type are owner-approved before anything ships"
    - "Every UAT leg is verified server-side through the deployed get_waitlist, not by UI appearance alone"
  artifacts:
    - path: ".planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md"
      provides: "The ordered owner runsheet: migration, redeploy, rollback table, probes, staging deploy, UAT legs"
      contains: "MIGRATION ORDER"
  key_links:
    - from: "80-CUTOVER.md"
      to: "the Waitlist sheet columns H-M"
      via: "an explicit, ordered, do-this-first instruction"
      pattern: "columns H"
    - from: "80-CUTOVER.md"
      to: "the Apps Script deployment version history"
      via: "a rollback table the owner fills in at redeploy time"
      pattern: "rollback"
---

<objective>
Take Phase 80 from committed-but-unverified source to a live, staff-usable queue, through the one
sheet migration and the one Apps Script redeploy this phase is allowed (D-20) — in the only order
that never 503s a live public signup (D-18).

Purpose: `apps-script/adminApi.gs` has no CI deploy path and a single Web App deployment serves
staging AND production, so this redeploy is effectively a production release for that layer. It has
to be right the first time, and Phase 78's rollback version numbers were never recorded — this time
they are.
Output: `80-CUTOVER.md`, owner sign-off on the email template and Cal.com event type, and a
completed migration + redeploy + staging UAT with real recorded values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CONTEXT.md
@.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-RESEARCH.md
@.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-UI-SPEC.md
@.planning/phases/78-brewpad-waitlist-tracking-make-the-beer-waitlist-a-workable-/78-CUTOVER.md
@.planning/phases/78-brewpad-waitlist-tracking-make-the-beer-waitlist-a-workable-/78-HUMAN-UAT.md
@docs/APPS_SCRIPT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: author 80-CUTOVER.md</name>
  <files>.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md</files>
  <read_first>
    - .planning/phases/78-brewpad-waitlist-tracking-make-the-beer-waitlist-a-workable-/78-CUTOVER.md — the whole document; §0 (why the order is load-bearing), §2 (the redeploy procedure and rollback table), §3 (the probe gate and the `-X POST -L` double-write trap), §4 STEP 3 (the Sheets date-serial paste trap), §6 (the UAT leg table format), §7b (the second-redeploy precedent)
    - .planning/phases/78-brewpad-waitlist-tracking-make-the-beer-waitlist-a-workable-/78-HUMAN-UAT.md — the still-open "record the rollback version numbers" item this plan must not repeat
    - docs/APPS_SCRIPT.md — the deploy/redeploy procedure and the Waitlist section as corrected by plan 80-01
    - .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-RESEARCH.md — Pitfall 1 (column placement), Pitfall 6, Open Questions 1 and 3
    - .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-UI-SPEC.md — the draft email template and Assumptions/Owner Review items 1-4
    - All five SUMMARY files from plans 80-01 through 80-05
  </read_first>
  <action>
Write `80-CUTOVER.md`, modelled section-for-section on `78-CUTOVER.md`. It is a runsheet, not a
report — every step ordered, every gate with a place to record a real value.

§0 — Why this order is load-bearing. State the two ordering rules and why each exists:
  - **D-18: add the six columns to the sheet FIRST, then redeploy.** `ensureWaitlistSheet` fails
    closed on any missing required column, returning `waitlist_unavailable` and never repairing
    headers. Redeploying first takes every public beer signup down with a 503 until the columns
    land. The reverse order is safe because the currently-deployed code maps by header name and
    ignores unknown columns.
  - **RESEARCH Pitfall 1: append the columns as H, I, J, K, L, M — strictly to the RIGHT of `notes`
    (column G) — never inserted between existing columns.** The currently-deployed
    `addWaitlistEntry` writes new rows via a positional 7-element `appendRow`, so inserting a column
    anywhere left of `notes` silently corrupts every public signup made between the sheet edit and
    the redeploy. Plan 80-01 removes that fragility permanently, but the deployed code is still the
    old one during the migration window.

§1 — What already shipped. List plans 80-01..80-05 with their commits and state plainly that none of
it is live until §2 happens by hand.

§2 — Sheet migration (BLOCKING, owner). Exact header cell values to type into H1..M1, in order:
`zoho_contact_id`, `customer_name`, `customer_phone`, `recipe_ids`, `position`, `contacted_at`.
Instruct: type them, do not paste from a rich source; leave every data cell in those columns empty;
do not reorder or touch A1..G1. Note that the existing six backfilled rows plus any live rows simply
gain six blank cells. Include the Sheets paste trap from `78-CUTOVER.md` §4 STEP 3 by reference in
case anything is ever pasted into these columns later — a date-shaped value pasted into
`contacted_at` will be stored as a serial and read back as a number.
Add a verification step: after typing the headers, run the read probe from §4 against the
STILL-OLD deployment and confirm it returns `"ok":true` — proving the old code ignores the unknown
columns and the public signup path is still healthy. This is the gate that makes §3 safe.

§3 — Apps Script redeploy (BLOCKING, owner). Copy `78-CUTOVER.md` §2 steps 1-5 procedure verbatim:
open the STEINS AND VINES project (script id
`1uD14PTT2lMWV06FAKcEs6Z_YKsEvnUuk9fOFycu7emiOPyh9jC0KTvUH`), confirm the editor's `adminApi.gs`
matches `git log -1 --format=%H -- apps-script/adminApi.gs`, paste the committed file if not, run
`setupWaitlist` from the function dropdown and expect `Waitlist tab ready (13 columns).`, then
**Deploy -> Manage deployments -> the ACTIVE deployment -> Edit (pencil) -> Version: New version ->
Deploy.** State in bold that a NEW DEPLOYMENT must never be created — the deployment id and `/exec`
URL must not change or every `APPS_SCRIPT_URL` in Railway breaks.
Include a **rollback table with four rows to fill in before proceeding**: prior version number (the
rollback target), new version number, deployment id, date/time. Add a sentence stating that Phase
78's equivalent table was left unfilled and that gap is still open in `78-HUMAN-UAT.md` — this table
must be completed at the moment of the redeploy, not afterwards.
Include the rollback procedure (version switch on the same deployment, not a URL migration) and note
that during a rollback window signups stop rather than going unrecorded, which is the intended
failure mode.

§4 — Probe gate. Give copy-pasteable read probes, all plain GETs, with the `78-CUTOVER.md` §2 step 5
warning that `-X POST` must never be combined with `-L` (Apps Script's 302 must be followed as a GET;
forcing POST lands on a Drive error page AFTER the mutation ran, so a retry double-writes). Probes:
  a. `get_waitlist` returns `"ok":true` and each row object now carries `zoho_contact_id`,
     `customer_name`, `customer_phone`, `recipe_ids`, `position`, `contacted_at` keys.
  b. A `update_waitlist_status` write of `position` to a disposable row returns `ok:true`, and a
     write of `position=0` returns `{"ok":false,"error":"invalid_position"}`.
  c. A `update_waitlist_status` backward transition on a `booked` row still returns
     `{"ok":false,"error":"invalid_transition"}` — proving CR-01's guard survived the rewrite.
  d. A fresh public signup through the staging middleware's `POST /api/waitlist` lands a row whose
     `status` reads `waiting` and whose `signed_up_at` is an ISO string — the direct check for
     Pitfall 1. Anything else in those cells IS that bug.

§5 — Staging deploy. Gates first (`npm test`, `cd zoho-middleware && npm test`, `npm run lint` in
both trees, `npm run build` clean with no uncommitted `.min` drift), then `git push origin main`,
then confirm the staging middleware health endpoint returns 200. Repeat `78-CUTOVER.md`'s caveats:
the staging frontend sits behind Cloudflare so curl checks are not meaningful, and staging and
production share ONE Google Sheet, so every UAT write lands in the real `Waitlist` tab — use a
disposable address and clean up.

§6 — UAT leg table (owner performs on staging, checked against `80-UI-SPEC.md`, not general
impression). One row per leg with an "Owner result" column: customer link to an existing contact;
inline create-and-link; the D-03a guard (link a contact to a row that already has a hand-typed phone
and confirm the phone survives); attach two recipes and remove one; pin a row to position 2 and
confirm the queue reorders; clear the pin and confirm true signup order returns; contact a `waiting`
row end-to-end and confirm the customer receives the email AND the row advances to `contacted` with a
`contacted_at` value; a deliberately failing send (temporarily unset `RESEND_API_KEY` on staging
Railway only, or use an address Resend rejects) and confirm the sheet stays open, shows the inline
error, and the row's status is UNCHANGED — this is the D-08 leg and the single most important one;
Contact button disabled on a `booked` row; manual add of a brand-new address; manual add of an
address already on the list, confirming the D-23 disclosure names the signup date and status; a
public signup through `beer.html` confirming its response and copy are unchanged (D-14, Phase 78
D-06); and cleanup of every probe row and MailerLite subscriber.

§7 — Production cutover, explicitly OUT of scope, batched with the pending 73/75/76/78 cutovers
already tracked in `.planning/STATE.md`, exactly as `78-CUTOVER.md` §7 does.

§8 — Open items checklist.

Also append a single line to `78-CUTOVER.md` §8 noting that Phase 80 adds another redeploy and that
its version numbers ARE recorded in `80-CUTOVER.md` §3 — so a future reader chasing the Phase 78 gap
finds the newer, complete record. Do not alter any other part of that file.
  </action>
  <acceptance_criteria>
    - `.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md` exists
    - `grep -c "columns H" .planning/phases/80-*/80-CUTOVER.md` is at least `1`
    - `grep -c "zoho_contact_id" .planning/phases/80-*/80-CUTOVER.md` is at least `1` and the same for `contacted_at`
    - `grep -ci "rollback" .planning/phases/80-*/80-CUTOVER.md` is at least `3`
    - `grep -c "New version" .planning/phases/80-*/80-CUTOVER.md` is at least `1` and `grep -ci "never create a new deployment\|do not create a new deployment" .planning/phases/80-*/80-CUTOVER.md` is at least `1`
    - `grep -c "invalid_position" .planning/phases/80-*/80-CUTOVER.md` is at least `1` and `grep -c "invalid_transition" .planning/phases/80-*/80-CUTOVER.md` is at least `1`
    - The migration section appears BEFORE the redeploy section: `awk '/^## /{n++} /Sheet migration/{m=n} /Apps Script redeploy/{r=n} END{exit !(m<r)}' .planning/phases/80-*/80-CUTOVER.md` exits 0
    - `git diff --numstat .planning/phases/78-*/78-CUTOVER.md` shows insertions only, zero deletions
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md &amp;&amp; grep -q "columns H" .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md
  </verify>
  <done>80-CUTOVER.md exists as an ordered runsheet whose migration section precedes its redeploy section, with an empty rollback table ready to be filled at the moment of the redeploy.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: owner approval — email template, Cal.com event type, and three UI-SPEC open items</name>
  <files>.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md</files>
  <read_first>
    - .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-UI-SPEC.md — the "Draft email template" row of the Copywriting Contract and Assumptions/Owner Review items 1-5
    - .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-RESEARCH.md — Open Questions 1 and 2, Assumption A1
    - .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CONTEXT.md — the `### Claude's Discretion` block
  </read_first>
  <action>
Present the five items below to the owner and record each verdict in a new `## Owner decisions`
section of `80-CUTOVER.md`, placed before the migration section so it is settled before anything
ships. Paste the approved email subject and body verbatim rather than paraphrasing.

If the owner changes the template wording, update the pre-fill strings in `js/brewpad.js`'s contact
sheet (plan 80-05 Task 1), rerun `npx jest --config jest.config.js tests/frontend/brewpad-waitlist-contact.test.js`,
and run `npm run build`. If the owner picks a different Cal.com event type, change which service's
`bookingUrl` the contact sheet selects from `GET /api/bookings/services`. If the owner overturns
`contactdisabled`, do NOT work around it in the frontend — it needs a paired change to the post-send
status write in `POST /api/waitlist/:id/contact`. If the owner overturns `pinallrows`, restrict the
pin affordance's render condition to `status === 'waiting'`. If the owner wants WR-02 closed, amend
D-17 explicitly, add `last_updated` as column N to the migration in §2 BEFORE the redeploy, and
extend plan 80-01's `updateWaitlistStatus` with `expectedVersion` handling mirroring
`updateReservation` — it cannot be retrofitted after the redeploy without a second one.
  </action>
  <decision>Five items CONTEXT.md and the UI-SPEC deliberately left for the owner, all of which must be settled before the redeploy.</decision>
  <context>
CONTEXT.md's discretion list says to draft the email wording and "surface for owner approval during
the cutover plan rather than guessing silently". The UI-SPEC's checker pass raised four more it had
to decide unilaterally. None is a blocker to building; all are blockers to shipping.
  </context>
  <options>
    <option id="template">
      <name>Contact email subject and body</name>
      <pros>Ship the drafted default. Subject: `Your spot on the Steins &amp; Vines beer waitlist is ready!` Body: `Hi {first name or 'there'}, / Great news — it's your turn on the beer waitlist! You can book your fermentation time here: / {booking_url} / If you have any questions, just reply to this email. / Cheers, Steins &amp; Vines`. Mirrors sendBottlingInvite's tone; staff can still edit per send (D-05).</pros>
      <cons>Rewording means updating the pre-fill in js/brewpad.js and rerunning the frontend suite — small and contained.</cons>
    </option>
    <option id="eventtype">
      <name>Which Cal.com event type the booking link resolves to</name>
      <pros>Default `CALCOM_EVENT_TYPE_FERMENT_KIT` — beer batches book into the ferment-in-store flow and no beer- or waitlist-specific event type exists.</pros>
      <cons>A different or new event type changes the service selection in the contact sheet and may need a light body reword.</cons>
    </option>
    <option id="contactdisabled">
      <name>Contact button disabled on booked/removed rows (UI-SPEC Decision 3)</name>
      <pros>Default disabled, reusing the existing `actionable` predicate. Sending to an already-booked customer would leave the post-send status write refused by `waitlistTransitionAllowed` — an email sent with no record of it.</pros>
      <cons>If re-sending a booking link to a booked customer is real, it needs a paired backend decision (skip rather than attempt the status write), not a frontend workaround.</cons>
    </option>
    <option id="pinallrows">
      <name>Pin control on every row vs waiting-only (UI-SPEC Decision 5)</name>
      <pros>Default every row, so a VIP mid-conversation stays visible near the top.</pros>
      <cons>A narrower scope is a one-line render guard on `status === 'waiting'`.</cons>
    </option>
    <option id="wr02">
      <name>WR-02 optimistic locking — carried forward, not fixed</name>
      <pros>Default carry-forward with a code comment. Closing it needs a 14th `last_updated` column, which D-17 does not list and D-20's one-migration framing forbids adding silently.</pros>
      <cons>Phase 80 widens the per-row concurrent-write surface from two independently-editable fields to nine, so two staff on the same row now last-write-wins across more fields. Closing it requires amending D-17 and growing the migration BEFORE the redeploy — it cannot be retrofitted without a second redeploy.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - An `## Owner decisions` section exists in `80-CUTOVER.md` with a recorded verdict for each of the five ids
    - The approved email subject and body are pasted verbatim, not paraphrased
    - If the wording changed, the new subject string appears in `js/brewpad.js` and `npx jest --config jest.config.js tests/frontend/brewpad-waitlist-contact.test.js` exits 0
    - If `wr02` is answered "close it", `80-CUTOVER.md` §2 lists `last_updated` as column N and plan 80-01's handler is amended in the same pass, both before the redeploy step
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Owner decisions" .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md</automated>
  </verify>
  <done>All five open items have recorded owner verdicts and any resulting code change is made and green.</done>
  <resume-signal>Reply with a verdict for each of the five ids: template, eventtype, contactdisabled, pinallrows, wr02.</resume-signal>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: execute the cutover — migration, redeploy, probes, staging deploy, UAT</name>
  <files>.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md</files>
  <read_first>
    - .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md — the runsheet written in Task 1, top to bottom, in order
    - .planning/phases/78-brewpad-waitlist-tracking-make-the-beer-waitlist-a-workable-/78-CUTOVER.md §2 and §7b — the redeploy procedure this repeats
  </read_first>
  <action>
The owner executes `80-CUTOVER.md` in order; the executor's job is to hold the gate, record real
values into the runsheet as they come back, and refuse to advance a section until the previous one
has recorded a pass. There is no CLI or API path to the Apps Script editor's Run/Deploy buttons or to
the Google Sheet's header row, so none of §2, §3 or §6 can be automated.

Hard gates, in order, none skippable:
  1. §2 sheet migration completes and its read probe against the STILL-OLD deployment returns
     `"ok":true` before §3 begins. If that probe fails, do not redeploy.
  2. §3's four-row rollback table holds real values before §4 runs.
  3. §4's four probes all pass, including `invalid_position`, `invalid_transition`, and the fresh
     public signup landing `status: waiting` with an ISO `signed_up_at`.
  4. §5's gates are green before `git push origin main`.
  5. §6's D-08 leg records the row's status as UNCHANGED after a deliberately failed send.
  6. §6's cleanup leg records that probe rows and MailerLite subscribers were removed.
  </action>
  <what-built>
Plans 80-01..80-05 are committed: the 13-column Apps Script schema with a header-driven
`addWaitlistEntry` and a nine-field `updateWaitlistStatus`; the staff-tier contact-send and
MailerLite-sync endpoints plus the widened admin-proxy write allow-list; and the BrewPad queue
override, customer link, recipe attach, contact sheet and manual add. None of the Apps Script half is
live — there is no CI deploy path for it, and one Web App deployment serves staging and production
simultaneously.
  </what-built>
  <how-to-verify>
Work through `80-CUTOVER.md` in order. Do not skip ahead; §2 must complete and its probe pass before
§3 begins.

1. **§2 — Sheet migration FIRST.** Type `zoho_contact_id`, `customer_name`, `customer_phone`,
   `recipe_ids`, `position`, `contacted_at` into H1..M1 of the `Waitlist` tab, in that order,
   strictly to the right of `notes` (G1). Touch nothing in A1..G1. Then run the §4 read probe against
   the STILL-OLD deployment and confirm `"ok":true` — the old code must keep working with the extra
   columns present. If it does not, STOP; do not redeploy.
2. **§3 — Apps Script redeploy.** Paste the committed `apps-script/adminApi.gs`, run `setupWaitlist`
   and expect `Waitlist tab ready (13 columns).`, then Deploy -> Manage deployments -> the ACTIVE
   deployment -> pencil -> Version: New version -> Deploy. **Never create a new deployment.**
   **Record the prior version number, the new version number, the deployment id and the timestamp in
   §3's rollback table before doing anything else** — Phase 78's equivalent table was left empty and
   that gap is still open in `78-HUMAN-UAT.md`.
3. **§4 — Probes.** Run all four. The `position=0 -> invalid_position` and the
   `booked -> waiting -> invalid_transition` probes prove the rewrite did not weaken an existing
   guard. The fresh-signup probe is the direct check for the positional-append bug: a `status` cell
   reading anything but `waiting`, or a blank/numeric `signed_up_at`, IS that bug — roll back
   immediately if seen. Use plain GET probes; never combine `-X POST` with `-L`.
4. **§5 — Staging deploy.** All gates green, then `git push origin main`, then staging middleware
   health 200.
5. **§6 — UAT.** Run every leg against `80-UI-SPEC.md` on staging, in a real browser, on the iPad for
   any leg about touch or layout. Verify every write server-side through the deployed `get_waitlist`,
   not by UI appearance. The D-08 leg — a deliberately failing send leaving the sheet open, showing
   the inline error, and leaving the row's status UNCHANGED — is the most important one; a failure
   there is blocking. Also confirm the `beer.html` public signup path and its copy are unchanged
   (D-14) and that a repeat public signup still discloses nothing (Phase 78 D-06).
6. **Cleanup.** Delete every probe row from the `Waitlist` tab by hand and remove probe addresses
   from the MailerLite group. Staging and production share one sheet.

Record every result in `80-CUTOVER.md` — real values, not "done".
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c "OWNER TO FILL IN" .planning/phases/80-*/80-CUTOVER.md` outputs `0` — §3's rollback table holds four real values
    - §4's four probe results are recorded with actual response bodies, including a literal `"ok":true` for the read probe and `invalid_position` / `invalid_transition` for the two guard probes
    - §5's gate table records real results for `npm test`, `cd zoho-middleware && npm test`, `npm run lint`, the push, and the staging health check
    - Every §6 UAT leg has a non-empty "Owner result" cell
    - The D-08 leg explicitly records that the row's status was unchanged after the failed send
    - The cleanup leg records that probe rows and MailerLite subscribers were removed
  </acceptance_criteria>
  <verify>
    <human-check>Owner confirms the runsheet is fully filled in with real values and the UAT outcome is recorded.</human-check>
    <automated>! grep -q "OWNER TO FILL IN" .planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-CUTOVER.md</automated>
  </verify>
  <done>The columns exist, exactly one redeploy happened with its rollback target recorded, all probes pass, staging carries the change, and every UAT leg has a real recorded result.</done>
  <resume-signal>Reply "cutover complete" with the rollback version numbers and the UAT outcome, or describe what failed and at which section.</resume-signal>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner's browser -> Apps Script editor | Manual paste and deploy; no CI, no review gate, no staging isolation |
| One Apps Script Web App deployment -> both staging and production | A single version switch changes behaviour for live customers immediately |
| Owner's machine -> the shared Google Sheet | Manual header edits on the live production waitlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-34 | Denial of service | Redeploying before the columns exist -> `ensureWaitlistSheet` returns `waitlist_unavailable` -> every public beer signup 503s | mitigate | §0 and §2 make columns-first an ordered, gated step, with a probe against the OLD deployment that must pass before §3 begins (D-18) |
| T-80-35 | Tampering | Inserting the new columns left of `notes` -> the still-deployed positional `appendRow` corrupts every signup during the migration window | mitigate | §2 specifies H1..M1 explicitly and forbids insertion; §4's fresh-signup probe detects the corruption directly and §3's rollback table gives it a target |
| T-80-36 | Denial of service | Creating a NEW deployment instead of a new version -> the `/exec` URL changes and every Railway `APPS_SCRIPT_URL` breaks | mitigate | Called out in bold in §3, copying the procedure `78-CUTOVER.md` already proved twice |
| T-80-37 | Repudiation | No rollback target recorded, so the documented rollback cannot be executed | mitigate | §3's four-row table is a gated acceptance criterion, with the still-open Phase 78 gap named as the reason |
| T-80-38 | Tampering | UAT writes landing in the real production sheet | mitigate | §5 and §6 restate that staging and production share one sheet; a disposable address is mandated and a cleanup leg is required |
| T-80-39 | Information disclosure | A test contact email reaching a real customer | mitigate | The UAT contact leg uses the disposable probe address only; the read-only To field means the sheet cannot retarget it |
| T-80-40 | Tampering | Deploying a working tree that differs from the committed source | mitigate | §3 step 1 requires diffing the editor's `adminApi.gs` against `git log -1 --format=%H -- apps-script/adminApi.gs` before deploying |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed — `80-RESEARCH.md`'s Package Legitimacy Audit confirms zero new dependencies phase-wide. slopcheck N/A |
</threat_model>

<verification>
1. `80-CUTOVER.md` exists, its migration section precedes its redeploy section, and it contains no `<OWNER TO FILL IN>` placeholders after Task 3.
2. All five owner decisions are recorded with verdicts.
3. `git diff --numstat .planning/phases/78-*/78-CUTOVER.md` shows insertions only.
4. The live deployment answers `get_waitlist` with rows carrying all six new keys.
5. A fresh public signup lands `status: waiting` and an ISO `signed_up_at` — the positional-append bug is confirmed absent on the deployed code.
6. `npm test`, `cd zoho-middleware && npm test`, and `npm run lint` all exit 0 on the pushed tree.
</verification>

<success_criteria>
- The six columns exist as H..M on the `Waitlist` tab and the old deployment kept working with them present.
- Exactly one Apps Script redeploy occurred, as a new version of the existing deployment, with the prior version number, new version number and deployment id recorded.
- All four probes pass, including both guard probes and the positional-append check.
- Staging carries the middleware and frontend halves and every UAT leg has a recorded owner result.
- The D-08 leg confirms a failed send leaves the row's status unchanged.
- `beer.html` and the public signup response are unchanged (D-14, Phase 78 D-06).
- Production cutover remains explicitly out of scope, batched with the pending 73/75/76/78 pushes.
</success_criteria>

<output>
Create `.planning/phases/80-brewpad-waitlist-work-the-queue-link-customers-and-recipes-c/80-06-SUMMARY.md` when done.
</output>
