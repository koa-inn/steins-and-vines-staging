---
phase: 82-store-agnostic-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/82-store-agnostic-prerequisites/82-01-SUMMARY.md
autonomous: false
requirements: [DB-01]

must_haves:
  truths:
    - "D-20: The owner has answered every pre-migration check: per-tab formulas / named ranges / pivots / charts; Apps Script Executions failures (last 90 days); Railway plan + backup/PITR entitlement; live row count per sheet; recent activity in the Reservations and Holds tabs (D-03)"
    - "The answers are written verbatim into 82-01-SUMMARY.md so Phases 83-88 can size their work from them (ROADMAP success criterion 4)"
    - "No code in this phase depends on these answers — this plan runs in parallel with every code plan (D-20)"
  artifacts:
    - path: ".planning/phases/82-store-agnostic-prerequisites/82-01-SUMMARY.md"
      provides: "Owner-check answers table"
      contains: "Executions"
  key_links:
    - from: "82-01-SUMMARY.md"
      to: "Phase 83-88 planning"
      via: "row counts / formula inventory / PITR entitlement recorded"
      pattern: "PITR"
---

<objective>
Collect the four (plus D-03's fifth) owner-only pre-migration checks and record the answers in the phase record.

Purpose: ROADMAP Phase 82 success criterion 4 — every later migration estimate (formula carry-over, backfill sizes, backup strategy) is gated on facts only the owner can read from the Google Sheet, the Apps Script console and the Railway dashboard. Claude has no credentials for any of those surfaces.
Output: 82-01-SUMMARY.md containing the answers table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@.planning/research/sheets-to-postgres-migration.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Owner answers the pre-migration checks (D-20, D-03)</name>
  <files>.planning/phases/82-store-agnostic-prerequisites/82-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md (D-03, D-20)
    - .planning/research/sheets-to-postgres-migration.md §8 (owner checks — background only; its zero-caller list is superseded by D-01/D-21)
    - apps-script/adminApi.gs lines 49-63 (the sheet-name constants — the list of tabs the owner must count)
  </read_first>
  <action>
Present the owner with this exact checklist (a human-action checkpoint: Claude has no Google Sheet, Apps Script console or Railway dashboard access). Ask for each answer, then write them into 82-01-SUMMARY.md under a "## Owner Check Answers" table with columns Check / Answer / Source-surface / Date-checked.

1. Formulas / named ranges / pivots / charts — for EVERY tab in the production workbook (at minimum: Config, Reservations, Holds, Schedule, Homepage, Kits, Ingredients, Batches, FermSchedules, BatchTasks, PlatoReadings, VesselHistory, Recipes, RecipeIngredients, GiftCards, GiftCardTransactions, Waitlist, plus any tab not in that list): does any cell hold a formula (e.g. Kits `available`), is any named range defined (Data → Named ranges), any pivot table, any chart? Record per tab.
2. Apps Script → Executions, filter Status = Failed, last 90 days: count of failures, and the top 3 function names / error messages.
3. Railway: current plan name, whether the Postgres add-on on that plan includes automated backups and point-in-time recovery (PITR), and the retention window.
4. Live row count per sheet (last non-empty row minus header) for every tab listed in item 1.
5. (D-03) Reservations and Holds tabs: date of the most recent row created or status-changed in each, so Phase 88 can decide whether the Reservations & Holds admin tab is still in use.

Also record, as a NOTE (not work), the milestone-level gap raised during planning (D-21): the Ingredients, Schedule and Homepage sheets have no destination in Phases 83-88 (Phase 88 only covers Reservations/Holds/Kits and the public CSV reads). Write it under "## Milestone Gap Noted" so the owner sees it alongside the row counts.
  </action>
  <what-built>Nothing is built — this is a data-collection checkpoint that runs in parallel with the code plans.</what-built>
  <how-to-verify>
    1. Owner opens the production Google Sheet and walks each tab for formulas / named ranges / pivots / charts.
    2. Owner opens script.google.com → the adminApi project → Executions → filter Failed, last 90 days.
    3. Owner opens the Railway dashboard → project → Plan / Postgres backup settings.
    4. Owner reports row counts per tab and the latest Reservations/Holds activity dates.
  </how-to-verify>
  <resume-signal>Paste the answers for items 1-5 (or "skip N" for any item that cannot be answered now, with a reason)</resume-signal>
  <verify>
    <automated>test -f .planning/phases/82-store-agnostic-prerequisites/82-01-SUMMARY.md && grep -c "Executions\|PITR\|row count\|Reservations" .planning/phases/82-store-agnostic-prerequisites/82-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 82-01-SUMMARY.md exists and contains a "## Owner Check Answers" section with one row for each of items 1-5
    - Item 1 lists every tab named in adminApi.gs lines 49-63 with a formulas/named-ranges/pivots/charts answer
    - Item 3 names the Railway plan and states yes/no for backups and for PITR
    - Item 4 gives a numeric row count per tab
    - A "## Milestone Gap Noted" section names Ingredients, Schedule and Homepage as having no destination in Phases 83-88
  </acceptance_criteria>
  <done>Every owner check has an answer (or an explicit, reasoned skip) recorded in 82-01-SUMMARY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner consoles → planning docs | Owner copies operational facts (row counts, error messages) into a committed markdown file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Information Disclosure | 82-01-SUMMARY.md | mitigate | Record counts and error CLASSES only — never paste customer names/emails/phone numbers from Execution logs or sheet rows, and never paste Railway connection strings or tokens; executor redacts any such value before committing |
| T-82-01-02 | Tampering | none (read-only checks) | accept | No system state is changed by this plan |
</threat_model>

<verification>
82-01-SUMMARY.md contains answers for all five checks plus the milestone-gap note.
</verification>

<success_criteria>
ROADMAP Phase 82 success criterion 4 is satisfied: the four owner checks (plus D-03's Reservations/Holds activity check) are answered and written into the phase summary.
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-01-SUMMARY.md` when done
</output>
