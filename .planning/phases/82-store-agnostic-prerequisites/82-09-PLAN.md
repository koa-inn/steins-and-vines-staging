---
phase: 82-store-agnostic-prerequisites
plan: 09
type: execute
wave: 5
depends_on: ["82-02", "82-03", "82-04", "82-05", "82-06", "82-07", "82-08"]
files_modified:
  - docs/RUNBOOK.md
  - .planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md
autonomous: false
requirements: [DB-01]

must_haves:
  truths:
    - "D-19 step 1: ONE owner Apps Script redeploy carries the D-11 + D-21 server_token entries, get_ingredients, both D-18 lock fixes, the per-task cache bust and the 3 deletions — performed BEFORE any browser change ships, with new and rollback version numbers recorded in docs/RUNBOOK.md's deploy record"
    - "Post-redeploy probes prove, without mutating live data, that the new actions dispatch, deleted actions return invalid_action, and createBatch's dedup still refuses a duplicate"
    - "D-19 step 2: middleware + admin.js + batch.js are pushed to staging only (git push origin main) and the owner walks every admin tab and every write, plus the public batch page (view, tick task, submit reading), using test records"
    - "D-19 step 3 (production cutover) is NOT in this plan — it is 82-10, gated on this plan's approved staging walk"
  artifacts:
    - path: "docs/RUNBOOK.md"
      provides: "Apps Script deploy record row for the Phase 82 redeploy"
      contains: "Phase 82"
    - path: ".planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md"
      provides: "Rollout record, probe results, staging walk results"
  key_links:
    - from: "Apps Script redeploy"
      to: "staging push"
      via: "strict ordering — push only after probes pass"
      pattern: "Version"
---

<objective>
Ship Phase 82 through D-19 steps 1-2: Apps Script first (additive, safe while the old browser path is live), then staging with a full live walk. Production cutover (step 3) is plan 82-10.

Purpose: ROADMAP success criteria 1 (walked live on staging) and 3 (redeploy done, rollback version recorded). Staging and production share ONE Google Sheet — every staging write hits live data, so the walk uses test records only.
Output: RUNBOOK deploy-record row, 82-09-SUMMARY.md with probes and walk results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@docs/RUNBOOK.md
@.planning/phases/82-store-agnostic-prerequisites/82-02-SUMMARY.md
@.planning/phases/82-store-agnostic-prerequisites/82-03-SUMMARY.md
@.planning/phases/82-store-agnostic-prerequisites/82-05-SUMMARY.md
@.planning/phases/82-store-agnostic-prerequisites/82-06-SUMMARY.md
@.planning/phases/82-store-agnostic-prerequisites/82-07-SUMMARY.md
@.planning/phases/82-store-agnostic-prerequisites/82-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Owner redeploys Apps Script (D-19 step 1) and runs non-mutating probes</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>
    - docs/RUNBOOK.md "Deploy sequence", "Rollback" and "Deploy record" (Apps Script section, ~:140-180)
    - apps-script/adminApi.gs (final, after 82-02 and 82-03)
    - 82-02-SUMMARY.md and 82-03-SUMMARY.md (what changed; deletion grep evidence)
  </read_first>
  <action>
Before pausing, Claude prepares: (a) `git log --oneline -- apps-script/adminApi.gs` since the last recorded deploy (Version 56) and a one-paragraph change list; (b) the local SHA-256 of the last commit's adminApi.gs for the RUNBOOK editor-drift check; (c) the probe commands below with placeholders for URL and token (never print the real token).

Owner steps (RUNBOOK deploy sequence): record the currently ACTIVE version (rollback target) → editor-drift hash check → paste adminApi.gs → Manage deployments → edit → New version, description "Phase 82: admin proxy server_token entries, inventory/schedule actions, get_ingredients, lock fixes, per-task cache bust, remove get_config/update_schedule/update_kits" → Deploy (never "New deployment").

Non-mutating probes (owner runs with APPS_SCRIPT_URL and the server token; each must return the stated result):
1. GET ?action=get_ingredients&server_token=… → ok:true with data.values[0] = Ingredients header row
2. GET ?action=get_homepage&server_token=… → ok:true, values are strings
3. GET ?action=get_config&server_token=… → error 'invalid_action' (deleted)
4. POST {action:'update_hold', server_token, holdId:'H-00000000-X000', updates:{}} → NOT 'Unknown server action' (expect not_found or similar) — proves D-11 dispatch
5. POST {action:'update_inventory_cells', server_token, sheet:'Holds', updates:[]} → error 'invalid_sheet' — proves the new action exists and the sheet allowlist works, writes nothing
6. POST {action:'update_schedule_slots', server_token, updates:[]} → error 'invalid_updates'
7. POST {action:'update_kits', server_token} → 'Unknown server action' / invalid_action
8. POST {action:'create_batch', server_token, product_sku:<an existing batch's SKU>, customer_name:'probe', zoho_so_number:<that batch's invoice>, unit_total:1} → error 'duplicate_so_number' (dedup still refuses inside the lock; nothing created)
9. Load the CURRENT production admin dashboard once and confirm it still loads and a read works (old browser path still live — D-19 safety)

Claude then appends a row to RUNBOOK "Deploy record": date, new version, previous (rollback target), "Phase 82 (82-02/82-03): …", and records probe outputs (redacted) in 82-09-SUMMARY.md.
  </action>
  <what-built>adminApi.gs changes from 82-02 and 82-03, ready to paste.</what-built>
  <how-to-verify>
    1. Owner confirms the new version number and the rollback version number.
    2. Owner pastes the output (status + error code only) of probes 1-9.
  </how-to-verify>
  <resume-signal>Reply with "deployed vNN (rollback vMM)" plus probe results, or describe the failure (then roll back to vMM before anything else)</resume-signal>
  <verify>
    <automated>grep -n "Phase 82" docs/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/RUNBOOK.md deploy record has a Phase 82 row with both the new and the rollback version numbers
    - 82-09-SUMMARY.md lists probes 1-9 with their observed result; every result matches the expected one (or the task is marked failed and rolled back)
    - No real token or customer data appears in any committed file
  </acceptance_criteria>
  <done>Apps Script is live with the Phase 82 surface, proven by non-mutating probes, and the rollback target is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Push to staging only (D-19 step 2) and run automated smoke checks</name>
  <files>.planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md</files>
  <read_first>
    - CLAUDE.md "Before Every Commit" and "Deployment"
    - `git status` output (the working tree had an unrelated modified links.html at planning time — it must NOT be swept into a commit)
  </read_first>
  <action>
Only after Task 1 is signed off:
1. Run `npm test`, `cd zoho-middleware && npm test`, `npm run lint`; all must pass. Confirm js/admin.min.js and js/batch.min.js are current (`npm run build` produces no further diff other than timestamps; if it does, commit the rebuilt artifacts as a separate chore commit).
2. `git status` — commit nothing unrelated to Phase 82 (leave links.html and other user edits alone).
3. `git push origin main` (staging; D-19 authorizes this push — never `git push production` here).
4. Poll `https://svmiddleware-staging.up.railway.app/health` until status ok and the new build is serving (up to ~10 min).
5. Smoke curls against staging (with Referer https://staging.steinsandvines.ca/):
   - POST /api/admin/proxy with no credential → 401/403 (never 200)
   - GET /api/batch/public/SV-B-000000?token=00000000000000000000000000000000 → HTTP 200 with {ok:false, error:'invalid_token'} (route live, Apps Script validating, no middleware-level rejection)
   - POST /api/batch/public/SV-B-000000/tasks with body {batch_token:'0'*32, task_id:'x', updates:{}} and no API key → NOT 403 (guard exemption live), expect {ok:false,error:'invalid_token'}
6. Record commit SHA, health output and smoke results in 82-09-SUMMARY.md.
  </action>
  <verify>
    <automated>curl -s https://svmiddleware-staging.up.railway.app/health</automated>
  </verify>
  <acceptance_criteria>
    - Both suites + lint green immediately before the push (outputs recorded)
    - `git log origin/main -1` equals the local HEAD that contains all Phase 82 commits; `git status` shows links.html still uncommitted if it was before
    - All three smoke checks return the expected status/body (recorded)
  </acceptance_criteria>
  <done>Phase 82 is live on staging only, smoke-checked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner walks the full admin surface and the public batch page on staging (D-19)</name>
  <files>.planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md</files>
  <read_first>
    - 82-06/82-07/82-08 SUMMARYs (which flows changed)
  </read_first>
  <action>
Present the checklist below; record every result in the SUMMARY. Staging shares the production workbook — use a TEST kit row, TEST ingredient, a TEST batch, a TEST reservation/hold and a far-future schedule date; revert each test change afterwards.
  </action>
  <what-built>Admin panel and batch page on staging now use /api/admin/proxy and /api/batch/public/* exclusively.</what-built>
  <how-to-verify>
    Open https://staging.steinsandvines.ca/admin.html, sign in, keep DevTools → Network open (filter "script.google.com" and "sheets.googleapis.com" — must show ZERO data requests all walk; filter "429" — note any).
    1. Dashboard loads (kits, reservations, holds, schedule, summary, ingredients).
    2. Reservations & Holds: change a test reservation status; confirm a test hold (kit stock/on_hold update); release a test hold; place a manual hold on the test kit.
    3. Kit Inventory: edit two cells on the test kit + one test ingredient cell → Save All (one request per sheet); add a test kit.
    4. Ingredients: add a test ingredient; delete it.
    5. Supplier Orders: add the test kit to the order (on_order sync); Accept Delivery for it.
    6. Scheduling (far-future month): generate slots; toggle one; block a whole day; reset day; reset month; confirm a booked slot is untouched.
    7. Export / Sync: run an import preview only if a safe test file exists — otherwise record "import not exercised" (do NOT overwrite the Kits sheet with untested data).
    8. Homepage: page loads existing content; save unchanged content.
    9. Batches: open the test batch; tick a task; complete a transfer task (then reopen — the change must show immediately, no 5-min stale view); bulk-save tasks; calendar-view save across two batches, then open each batch detail — both show the new state; add a task; add/edit/delete a plato reading; regenerate token; ferm schedule edit + propagate.
    10. Leave the tab idle > 10 min, then act — no forced re-login unless the session truly expired.
    11. Public batch page: open the test batch's QR URL (staging) in a private window — view loads, tick a non-packaging task, submit a plato reading, wait 60 s for auto-refresh.
    12. BrewPad smoke (unchanged code, shared helper refactor): load dashboard and open a batch.
  </how-to-verify>
  <resume-signal>Type "approved" with any notes (incl. any 429 seen), or list failing steps</resume-signal>
  <verify>
    <automated>grep -c "Staging walk" .planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 82-09-SUMMARY.md has a "Staging walk" table with steps 1-12 marked pass/fail/not-exercised and the network-filter findings (zero script.google.com / sheets.googleapis.com data calls)
    - Any 429 observed is recorded and raised to the owner before the 82-10 cutover (T-82-06-05)
    - Test records created during the walk are listed and reverted
  </acceptance_criteria>
  <done>Owner approves the staging walk.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging ↔ production shared Google Sheet | Staging writes mutate live data |
| Operator ↔ deploy tooling | Manual Apps Script paste; gated prod workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-09-01 | Tampering | Staging walk corrupting live records | mitigate | Test records only, far-future schedule dates, import step not exercised without a safe file, every test change reverted and listed |
| T-82-09-02 | Denial of Service | Browser calling an action Apps Script no longer accepts during rollout | mitigate | Apps Script first; only zero-caller actions deleted; check_auth kept; probe 9 confirms the old prod admin still works after the redeploy |
| T-82-09-03 | Information Disclosure | server token or customer data pasted into committed docs | mitigate | Probe commands use placeholders; only status/error codes recorded |
| T-82-09-04 | Tampering | Editor drift overwritten by the paste | mitigate | RUNBOOK SHA-256 drift check before pasting |
| T-82-09-05 | Elevation of Privilege | Unreviewed code reaching production | mitigate | This plan never pushes to the production remote; prod is 82-10 behind a blocking gate |
</threat_model>

<verification>
- RUNBOOK deploy record updated; probes, smoke checks and staging walk recorded in 82-09-SUMMARY.md
</verification>

<success_criteria>
ROADMAP criteria 1 (live staging walk) and 3 (redeploy + rollback version recorded) are met on staging; the owner has approved the walk.
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md` when done
</output>
