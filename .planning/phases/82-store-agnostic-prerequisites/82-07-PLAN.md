---
phase: 82-store-agnostic-prerequisites
plan: 07
type: execute
wave: 4
depends_on: ["82-06", "82-03", "82-04"]
files_modified:
  - js/admin.js
  - js/admin.min.js
  - admin.html
  - kiosk.html
  - brewpad.html
  - index.html
  - tests/frontend/admin-inventory-proxy.test.js
  - tests/frontend/admin-schedule-homepage-proxy.test.js
autonomous: true
requirements: [DB-01]

must_haves:
  truths:
    - "D-21: every formerly-unconditional direct Google Sheets API call in js/admin.js goes through /api/admin/proxy via a typed action — kit stock/on_hold after hold confirm/release, manual hold, Kit Inventory/Ingredients inline save, add kit, add/delete ingredient, Supplier Orders syncOnOrder/acceptDelivery, Scheduling generate/toggle/bulk-day/reset-day/reset-month, applyImport, and loadHomepageData"
    - "Each user action issues ONE proxy write per sheet (multi-cell changes batched into one update_inventory_cells / update_schedule_slots call), never a per-cell request loop"
    - "D-16: sheetsGet, sheetsUpdate, sheetsAppend and sheetsBatchUpdate are deleted; js/admin.js contains no 'sheets.googleapis.com/v4' URL (the GIS OAuth scope string at login is not a data call)"
    - "D-06: no data path depends on the Google access token (e.g. syncOnOrder's `!accessToken` guard removed)"
    - "D-17/D-23: admin.html no longer loads js/admin-config.js; js/admin-config.js itself stays (kiosk.html, brewpad.html load it); npm run build regenerated js/admin.min.js"
    - "User-visible behaviour of every rewired flow is unchanged (same toasts, same re-render/reload calls, booked slots never changed)"
  artifacts:
    - path: "tests/frontend/admin-inventory-proxy.test.js"
      provides: "One parity test per rewired inventory/holds/orders/import flow"
    - path: "tests/frontend/admin-schedule-homepage-proxy.test.js"
      provides: "One parity test per rewired scheduling flow + homepage load"
    - path: "js/admin.js"
      provides: "Rewired flows; Sheets helpers deleted"
  key_links:
    - from: "saveAllChanges / acceptDelivery / syncOnOrder / updateKitStockAfterConfirm"
      to: "update_inventory_cells"
      via: "adminApiPost('update_inventory_cells', {sheet, updates})"
      pattern: "adminApiPost\\('update_inventory_cells'"
    - from: "toggleSlot / bulkUpdateDay / resetDayToDefault / resetMonthToDefaults"
      to: "update_schedule_slots"
      via: "adminApiPost('update_schedule_slots', {updates})"
      pattern: "adminApiPost\\('update_schedule_slots'"
    - from: "loadHomepageData"
      to: "get_homepage"
      via: "adminApiGet('get_homepage')"
      pattern: "adminApiGet\\('get_homepage'\\)"
---

<objective>
Rewire the ~15 admin.js functions that call the Google Sheets API directly onto the typed proxy actions (D-21), then delete the Sheets helpers (D-16) and the admin-config.js script tag (D-23).

Purpose: Completes DB-01's "no browser code talks to Apps Script or the Google Sheets API for data" for js/admin.js. After this plan the Google token is used only at login.
Output: js/admin.js (+ rebuilt min), admin.html script-tag removal, two new parity suites.

Milestone note (D-21, raise — not work): Ingredients, Schedule and Homepage now sit behind the proxy but have no destination in Phases 83-88; record this in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@.planning/phases/82-store-agnostic-prerequisites/82-03-SUMMARY.md
@.planning/phases/82-store-agnostic-prerequisites/82-06-SUMMARY.md

<interfaces>
Action contracts (implemented by 82-03, allowlisted by 82-04):
- update_inventory_cells {sheet:'Kits'|'Ingredients', updates:[{row, field, value}]} → {ok, count}
- append_inventory_row {sheet:'Kits'|'Ingredients', values:[...]} → {ok}
- import_kits {values:[[headers...],[row...],...]} (values[0] must equal the live Kits header row) → {ok, rows}
- add_hold {hold_id:'H-YYYYMMDD-Mnnn', sku, product_name, qty, notes} → {ok, hold_id}
- append_schedule_slots {rows:[[date,time,status]]} → {ok, count}
- update_schedule_slots {updates:[{row, status}]} → {ok, updated, skipped:[row...]}   ('booked' rows are skipped server-side)
- get_homepage → {ok, data:{values:[[...]]}} (display strings; loadHomepageData reads columns A..E)

From 82-06: adminApiGet(action, params) (reads, retried on 502/503/504) and adminApiPost(action, payload) (writes, sent once) — both resolve to the parsed {ok:true,...} body or reject with Error.

Functions to rewire (js/admin.js; line numbers approximate after 82-06 — locate by name):
updateKitStockAfterConfirm(kit, qty) ~1907 · updateKitOnHoldAfterRelease(kit, qty) ~2033 · openManualHoldModal(kit) ~2470 · saveAllChanges() ~2612 · openAddKitModal() ~2674 · deleteIngredient(ing) ~2793 · openAddIngredientModal() ~2820 · syncOnOrder(changedSkus) ~2991 · acceptDelivery() ~3677 · generateSlotsForMonth() ~4030 · toggleSlot(date, time, rowIndex) ~4265 · bulkUpdateDay(dateStr, newStatus) ~4284 · resetDayToDefault(dateStr) ~4309 · resetMonthToDefaults() ~4362 · applyImport() ~4658 · loadHomepageData() ~4945
State vars: kitsData/kitsHeaders/ingredientsData/ingredientsHeaders/scheduleData/scheduleHeaders :63-74, pendingChanges :88, importPreviewData ~4575. Row objects carry _rowIndex (1-based sheet row, parseSheetData :1272).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire holds / inventory / supplier-orders / import flows (D-21)</name>
  <files>js/admin.js, tests/frontend/admin-inventory-proxy.test.js</files>
  <read_first>
    - js/admin.js: updateKitStockAfterConfirm, updateKitOnHoldAfterRelease, openManualHoldModal, saveAllChanges, openAddKitModal, deleteIngredient, openAddIngredientModal, syncOnOrder, getOrder/saveOrder, acceptDelivery, applyImport (read each function whole), plus the module.exports seam
    - tests/frontend/admin-proxy-transport.test.js (82-06 fixture to reuse)
    - apps-script/adminApi.gs validator helpers from 82-03 (buildManualHoldRow, validateInventoryCellUpdates) to match field names exactly
  </read_first>
  <behavior>
    Each test seeds state via a seam, stubs window.confirm → true, mocks fetch, runs the function, and asserts (a) fetch URLs all equal MIDDLEWARE_URL + '/api/admin/proxy', (b) no URL contains 'sheets.googleapis.com', (c) the exact action + payload:
    - updateKitStockAfterConfirm({_rowIndex:5, stock:'10', on_hold:'3'}, 2) with kitsHeaders [..'stock','on_hold'..] → ONE call update_inventory_cells {sheet:'Kits', updates:[{row:5,field:'stock',value:8},{row:5,field:'on_hold',value:1}]}
    - updateKitOnHoldAfterRelease(kit, 2) → ONE call with only on_hold
    - openManualHoldModal(kit) + submit qty 2, notes 'phone' → first call add_hold {hold_id matching /^H-\d{8}-M\d{3}$/, sku, product_name:'<brand> <name>', qty:2, notes:'phone'}, then update_inventory_cells on_hold = old+2; then loadAllData is triggered as today
    - saveAllChanges with 2 kit changes + 1 ingredient change → exactly TWO calls (one per sheet), each including the field updates plus a last_updated update per changed row
    - openAddKitModal submit → append_inventory_row {sheet:'Kits', values:<header-ordered row, on_hold '0', available ''>}
    - openAddIngredientModal submit → append_inventory_row {sheet:'Ingredients', values:<header-ordered row>}
    - deleteIngredient(ing) → ONE update_inventory_cells {sheet:'Ingredients', updates: one {row: ing._rowIndex, field:h, value:''} per ingredientsHeaders entry}; ing removed from ingredientsData after success
    - syncOnOrder(['K1']) → ONE update_inventory_cells Kits on_order; runs even when the Google access token is null
    - acceptDelivery with 2 checked items → ONE update_inventory_cells call containing stock, on_order 0 and last_updated for each accepted kit
    - applyImport → import_kits {values:[kitsHeaders, ...mapped rows]}
    - A rejected proxy call shows the same error toast text as before (e.g. 'Failed to place hold: ...')
  </behavior>
  <action>
RED: create tests/frontend/admin-inventory-proxy.test.js implementing the <behavior> list. Add to the admin.js module.exports seam: `_inventoryForTest: { updateKitStockAfterConfirm: ..., updateKitOnHoldAfterRelease: ..., openManualHoldModal: ..., saveAllChanges: ..., openAddKitModal: ..., deleteIngredient: ..., openAddIngredientModal: ..., syncOnOrder: ..., acceptDelivery: ..., applyImport: ... }` and `_setSheetStateForTest: function (s) { assigns any of kitsData, kitsHeaders, ingredientsData, ingredientsHeaders, scheduleData, scheduleHeaders, pendingChanges, importPreviewData present on s }` (ES5 syntax).

GREEN — rewrite each function body's Sheets calls; keep all UI/state logic, toasts, confirm() prompts, local state mutation and reload calls exactly as they are:
- Add a local helper `function inventoryCellsUpdate(sheetKey, updates) { if (!updates.length) return Promise.resolve(); return adminApiPost('update_inventory_cells', { sheet: sheetKey, updates: updates }); }` where sheetKey is the literal 'Kits' or 'Ingredients' (the Apps Script contract names, not SHEETS_CONFIG ranges).
- Build `{row: item._rowIndex, field: <header name>, value}` entries only for headers present (keep the existing `headers.indexOf(x) !== -1` checks); drop colLetter/range strings from these paths.
- saveAllChanges: group pendingChanges into Kits vs Ingredients update lists (same membership test `kitsData.indexOf(change.item) !== -1`), then Promise.all of at most two inventoryCellsUpdate calls.
- openManualHoldModal: adminApiPost('add_hold', {hold_id: holdId, sku: kit.sku || '', product_name: (brand + ' ' + name), qty, notes}) then the on_hold inventoryCellsUpdate.
- openAddKitModal / openAddIngredientModal: adminApiPost('append_inventory_row', {sheet, values: row}).
- deleteIngredient: inventoryCellsUpdate('Ingredients', ingredientsHeaders.map(h → {row: ing._rowIndex, field: h, value: ''})).
- syncOnOrder: remove the `!accessToken` part of the guard (keep the kitsData.length/on_order checks) — D-06.
- acceptDelivery: one inventoryCellsUpdate('Kits', ...) with all accepted kits' stock/on_order/last_updated entries.
- applyImport: adminApiPost('import_kits', {values: sheetRows}).
  </action>
  <verify>
    <automated>npx jest tests/frontend/admin-inventory-proxy.test.js tests/frontend/admin-proxy-transport.test.js</automated>
  </verify>
  <acceptance_criteria>
    - None of the 10 rewired function bodies contains `sheetsUpdate(`, `sheetsAppend(` or `colLetter(` (asserted by slicing each function in the suite or by grep on the rewritten ranges)
    - `grep -c "adminApiPost('update_inventory_cells'" js/admin.js` = 1 (inside inventoryCellsUpdate) and `grep -c "inventoryCellsUpdate(" js/admin.js` ≥ 7
    - `grep -c "adminApiPost('add_hold'\|adminApiPost('append_inventory_row'\|adminApiPost('import_kits'" js/admin.js` ≥ 4
    - Suite has ≥ 10 flow tests and passes
  </acceptance_criteria>
  <done>Holds side-effects, Kit Inventory, Ingredients, Supplier Orders and Import all write through the proxy with one call per sheet per action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire Scheduling flows and the Homepage load (D-21)</name>
  <files>js/admin.js, tests/frontend/admin-schedule-homepage-proxy.test.js</files>
  <read_first>
    - js/admin.js: generateSlotsForMonth, toggleSlot, bulkUpdateDay, resetDayToDefault, resetMonthToDefaults, renderScheduleCalendar (signature only), loadHomepageData (whole), homepageConfig/homepageHeaders vars
    - tests/frontend/admin-inventory-proxy.test.js (Task 1 fixture)
  </read_first>
  <behavior>
    - generateSlotsForMonth (confirm → true) → ONE call append_schedule_slots {rows: [[YYYY-MM-DD, time, 'available'], ...]} then loadAllData as today
    - toggleSlot(date, time, 7) on an 'available' slot → ONE call update_schedule_slots {updates:[{row:7,status:'blocked'}]}; local slot.status flips on success
    - bulkUpdateDay(date, 'blocked') over 3 non-booked slots → ONE call with 3 updates
    - resetDayToDefault / resetMonthToDefaults → ONE call each with every needed update; booked slots never appear in the payload
    - When the response has skipped:[row], that slot's local status is NOT changed (server refused because it became booked)
    - loadHomepageData → ONE adminApiGet('get_homepage') call; with data.values [['type','date','title','text','sku'],['news','2026-09-01','T','X',''],['featured','','','Desc','SKU-1']] it fills homepageConfig promo-news and promo-featured-skus exactly as the old sheetsGet path did
    - No URL contains 'sheets.googleapis.com'
  </behavior>
  <action>
RED: create tests/frontend/admin-schedule-homepage-proxy.test.js; extend the seam with `_scheduleForTest: { generateSlotsForMonth, toggleSlot, bulkUpdateDay, resetDayToDefault, resetMonthToDefaults, loadHomepageData }` and `_getHomepageConfigForTest: function () { return homepageConfig; }` (ES5). Stub the default-schedule source getDefaultSchedule reads (localStorage or whatever it uses — read it) and window.confirm.

GREEN:
- Add `function scheduleSlotsUpdate(updates) { if (!updates.length) return Promise.resolve({ ok: true, updated: 0, skipped: [] }); return adminApiPost('update_schedule_slots', { updates: updates }); }`.
- toggleSlot / bulkUpdateDay / resetDayToDefault / resetMonthToDefaults: build `[{row: slot._rowIndex, status: newStatus}]` from the slots they already select (booked filtering unchanged), make ONE scheduleSlotsUpdate call, and on success set each slot.status = newStatus except rows listed in result.skipped; keep the existing toast/render/reload handling.
- generateSlotsForMonth: replace sheetsAppend with adminApiPost('append_schedule_slots', { rows: newRows }).
- loadHomepageData: replace sheetsGet(... 'Homepage!A:E') with adminApiGet('get_homepage').then(function (result) { var rows = ((result.data && result.data.values) || []).map(function (r) { return r.slice(0, 5); }); ...existing parsing unchanged... }) — slice keeps the old A:E window.
  </action>
  <verify>
    <automated>npx jest tests/frontend/admin-schedule-homepage-proxy.test.js tests/frontend/admin-inventory-proxy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adminApiPost('update_schedule_slots'" js/admin.js` = 1 and `grep -c "scheduleSlotsUpdate(" js/admin.js` ≥ 5
    - `grep -c "adminApiPost('append_schedule_slots'" js/admin.js` = 1; `grep -c "adminApiGet('get_homepage')" js/admin.js` = 1
    - Suite has ≥ 7 tests (one per rewired function + skipped-row case) and passes
  </acceptance_criteria>
  <done>Scheduling and Homepage load go through the proxy; booked-slot protection holds on both sides.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the Sheets helpers, drop admin-config.js from admin.html, rebuild, grep gates (D-16, D-17, D-23)</name>
  <files>js/admin.js, js/admin.min.js, admin.html, kiosk.html, brewpad.html, index.html</files>
  <read_first>
    - js/admin.js: sheetsGet/sheetsUpdate/sheetsAppend/sheetsBatchUpdate definitions (~734-835) and `grep -n "sheetsGet(\|sheetsUpdate(\|sheetsAppend(\|sheetsBatchUpdate(\|googleapis.com\|accessToken" js/admin.js`
    - admin.html lines 1065-1080 (script tags)
    - kiosk.html and brewpad.html script tags (confirm they still load js/admin-config.js — they keep it)
  </read_first>
  <action>
1. Confirm zero remaining callers: `grep -n "sheetsGet(\|sheetsUpdate(\|sheetsAppend(\|sheetsBatchUpdate(" js/admin.js` must show only the four definitions. If any other call remains, rewire it using the Task 1/2 helpers first (record it in the SUMMARY as a site the planning inventory missed).
2. Delete the function definitions sheetsGet, sheetsUpdate and sheetsAppend (sheetsBatchUpdate was already deleted by 82-06) and the "===== Sheets API Helpers =====" header. Then `grep -n "colLetter(" js/admin.js` — after Tasks 1-2 its only remaining hit should be its own definition (~:5353); if so delete `function colLetter` too (orphaned). If any caller remains, keep it and record why in the SUMMARY.
3. `grep -n "googleapis.com" js/admin.js` — the only allowed hit is the GIS OAuth scope string at login (~:426, `SHEETS_CONFIG.SCOPES + ' https://www.googleapis.com/auth/userinfo.email'`). Any 'sheets.googleapis.com' hit is a defect to fix.
4. `grep -n "accessToken" js/admin.js` — every remaining use must be login/logout/session-exchange (tokenClient, onTokenResponse, checkAuthorization's POST /auth/google, signOut revoke, test seams). Record the list in the SUMMARY; remove any remaining data-path use (D-06).
5. admin.html: delete `<script src="js/admin-config.js" defer></script>` (D-23). Leave js/admin-config.js in the repo and in kiosk.html / brewpad.html (D-17). Do not add a CSP to admin.html (none exists; orchestrator guidance 3) — note the pre-existing gap in the SUMMARY.
6. Note in the SUMMARY (not work): SHEETS_CONFIG.SCOPES still requests the spreadsheets OAuth scope at admin login even though admin no longer needs it — a least-privilege follow-up, left untouched because brewpad/kiosk share sheets-config.js.
7. `npm run build`, then `npm test`, `cd zoho-middleware && npm test`, `npm run lint`.
  </action>
  <verify>
    <automated>npm run build && npm test && npm run lint && (cd zoho-middleware && npm test) && test "$(grep -c 'function sheets\(Get\|Update\|Append\|BatchUpdate\)(' js/admin.js)" -eq 0 && test "$(grep -c 'sheets.googleapis.com' js/admin.js)" -eq 0 && test "$(grep -c 'admin-config.js' admin.html)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sheets.googleapis.com" js/admin.js` = 0 and `grep -c "sheets.googleapis.com" js/admin.min.js` = 0
    - `grep -cE "function sheets(Get|Update|Append|BatchUpdate)\(" js/admin.js` = 0
    - `grep -c "colLetter" js/admin.js` = 0 (orphaned helper deleted) — or the SUMMARY names the surviving caller
    - `grep -v '^\s*//' js/admin.js | grep -c ADMIN_API_URL` = 0 and `grep -c ADMIN_API_URL js/batch.js` = 0 (ROADMAP criterion 1)
    - `grep -c "admin-config.js" admin.html` = 0; `grep -c "admin-config.js" kiosk.html brewpad.html` unchanged (each ≥ 1); js/admin-config.js still exists
    - Both suites + lint pass; `git diff --name-only tests/` lists only new files plus the 82-06 D-22 file
  </acceptance_criteria>
  <done>admin.js has no direct Google Sheets or Apps Script data path left; artifacts rebuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staff browser → /api/admin/proxy → Apps Script typed actions | Inventory/schedule/homepage writes now cross the middleware instead of going browser → Google |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-07-01 | Information Disclosure | Google OAuth token with spreadsheets scope used from the browser for data | mitigate | All Sheets API calls and helpers deleted; grep gates on 'sheets.googleapis.com' in admin.js and admin.min.js |
| T-82-07-02 | Tampering | Per-cell request loops partially applying a multi-cell change | mitigate | One update_inventory_cells / update_schedule_slots call per sheet per user action; server validates the whole batch before writing (82-03) |
| T-82-07-03 | Tampering | Blocking a booked slot through a stale client view | mitigate | Server skips booked rows (82-03); client does not flip local status for skipped rows |
| T-82-07-04 | Denial of Service | Proxy call volume against the 60 req/min per-IP apiLimiter | mitigate | Batching collapses N-cell operations (e.g. reset month) to one request; staging walk (82-09) watches for 429s |
| T-82-07-05 | Tampering | XSS through stored kit/ingredient/homepage values | mitigate | Client keeps sanitizeInput on form values; server sanitizeInput on every string (82-03) |
</threat_model>

<verification>
- Both new suites green; `npm test`, `cd zoho-middleware && npm test`, `npm run lint` green
- Grep gates: no sheets.googleapis.com, no Sheets helpers, no ADMIN_API_URL in admin.js/batch.js, no admin-config.js in admin.html
</verification>

<success_criteria>
Every admin tab's data reads and writes go through /api/admin/proxy; the direct-Sheets helpers are gone; the Google token is used only at login.
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-07-SUMMARY.md` when done
</output>
