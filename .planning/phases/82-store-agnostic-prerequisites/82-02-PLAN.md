---
phase: 82-store-agnostic-prerequisites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps-script/adminApi.gs
  - tests/frontend/adminapi-phase82-locks.test.js
  - tests/frontend/adminapi-phase82-dispatch.test.js
autonomous: true
requirements: [DB-01]

must_haves:
  truths:
    - "D-18: updateGiftCardInvoice reads and writes the GiftCards row only while holding acquireScriptLock(15000), released in a finally block"
    - "D-18: createBatch's invoice+SKU dedup check runs AFTER acquireScriptLock(15000) inside the same try that generates the batch ID and appends the row — check-then-append is atomic"
    - "The CR-01 / unit_total dedup CONTRACT is unchanged (x3 with unit_total:3 all create; 4th is duplicate_so_number; legacy no-unit_total allows 1; different SKU counts independently)"
    - "D-11: doPost's server_token branch dispatches update_reservation, update_hold, update_homepage, add_batch_task, update_batch_task, propagate_ferm_schedule and regenerate_batch_token (7 actions) with 'middleware' as the actor"
    - "D-09 (server half): update_batch_task busts gb:<batchId> using the task row's own batch_id even when the caller omits payload.batch_id, and bulk_update_batch_tasks busts every distinct batch its tasks belong to (fixes the multi-batch calendar-view save, orchestrator guidance 1)"
    - "Nothing that currently has a caller is removed — this file stays safe to redeploy while the old browser path is live (D-19)"
  artifacts:
    - path: "apps-script/adminApi.gs"
      provides: "Lock fixes, 7 server_token write entries, per-task batch cache-bust"
      contains: "function batchDedupDecision("
    - path: "tests/frontend/adminapi-phase82-locks.test.js"
      provides: "Pure dedup-contract tests + source-shape lock-ordering assertions"
    - path: "tests/frontend/adminapi-phase82-dispatch.test.js"
      provides: "Source-shape assertions for the 7 server_token entries + pure _uniqueBatchIds tests"
  key_links:
    - from: "createBatch"
      to: "acquireScriptLock"
      via: "lock acquired before batchDedupDecision + sheetToObjects(BATCHES_SHEET_NAME, true)"
      pattern: "acquireScriptLock\\(15000\\)"
    - from: "bulkUpdateBatchTasks"
      to: "_invalidateBatchCache"
      via: "loop over _uniqueBatchIds(results)"
      pattern: "_uniqueBatchIds\\("
---

<objective>
Close the two Apps Script lock holes (D-18), open the doPost server_token path to the 7 admin write actions the new proxy will forward (D-11), and make batch-task writes bust the right per-batch cache regardless of what the caller sends (D-09, orchestrator guidance 1).

Purpose: The middleware proxy (82-04) authenticates to Apps Script with server_token; writes only reach handlers listed in doPost's server_token if-chain. Lock fixes are ROADMAP success criterion 3.
Output: Edited apps-script/adminApi.gs plus two new Jest suites (source-shape + pure-function, the established harness for this file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@.planning/phases/82-store-agnostic-prerequisites/82-RESEARCH.md
@.planning/phases/82-store-agnostic-prerequisites/82-PATTERNS.md

<interfaces>
From apps-script/adminApi.gs (current, verified 2026-09-23):
- acquireScriptLock(timeoutMs) at :1252 — returns a LockService script lock; caller MUST lock.releaseLock() in finally.
- doPost(e) at :249. batch-token branch :255-259; server_token branch :262-383 ends with `return _jsonResponse({ ok: false, error: 'invalid_action', message: 'Unknown server action: ' + action });` at :382. Staff switch :391-507.
- Existing server_token entry shape to copy (:338-342): `if (action === 'update_batch') { var sUpdateBatchResult = updateBatch(payload, 'middleware'); _invalidateBatchCache(payload.batch_id); return _jsonResponse(sUpdateBatchResult); }`
- Handler signatures: updateReservation(payload, userEmail) :972; updateHold(payload, userEmail) :1055; updateHomepage(payload) :1174; updateBatchTask(payload, completedBy) :2716 (final success return `{ ok: true, message: 'Task updated' }`, `current` = task row object incl. current.batch_id); bulkUpdateBatchTasks(payload, email) :2807; addBatchTask(payload, userEmail) :2823; propagateFermSchedule(payload, userEmail) :3245; regenerateBatchToken(payload) :3399.
- _invalidateBatchCache(batchId) :3454 — removes gbl/gtu/gbds/gbi/gfs plus gb:<id> and gbp:<id> when id given.
- createBatch(payload, userEmail) :2129; dedup block :2158-2193 (two branches: invoice+SKU with unit_total, and invoice-only fallback); lock acquired :2227 `var lock = acquireScriptLock(15000); try {`; finally :2375.
- updateGiftCardInvoice(payload) :4847 — NO lock today.
- sheetToObjects(sheetName, skipCache) :1439 — per-execution _sheetCache unless skipCache is true.

Test harness pattern (tests/frontend/adminapi-giftcard-ledger.test.js): read the file with fs, build `new Function(src + '\nreturn { name: (typeof name !== "undefined" ? name : undefined), ... }')` to extract PURE helpers; use a brace-matching `sliceFunctionSource(src, name)` for source-shape assertions. Apps Script globals (SpreadsheetApp, LockService, CacheService) are NOT available — only pure helpers may be executed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-18 lock fixes — updateGiftCardInvoice under the lock, createBatch dedup inside the lock</name>
  <files>apps-script/adminApi.gs, tests/frontend/adminapi-phase82-locks.test.js</files>
  <read_first>
    - apps-script/adminApi.gs lines 1245-1260 (acquireScriptLock), 1439-1470 (sheetToObjects), 2129-2240 and 2370-2380 (createBatch incl. dedup block, lock, finally), 4840-4890 (updateGiftCardInvoice)
    - tests/frontend/adminapi-giftcard-ledger.test.js lines 1-90 (loader + sliceFunctionSource pattern to copy) and ~495-510 (existing lock source-shape assertion style)
    - .planning/phases/82-store-agnostic-prerequisites/82-RESEARCH.md "Pitfall 6"
  </read_first>
  <behavior>
    - batchDedupDecision(rows, {zoho_so_number:'INV-001', product_sku:'SKU-A', unit_total:3}) with 0,1,2 matching rows → null (allowed); with 3 matching rows → {ok:false, error:'duplicate_so_number', ...} whose message lists the matching batch_ids
    - batchDedupDecision with no unit_total and 1 matching row → duplicate_so_number; with 0 → null
    - batchDedupDecision for SKU-B when rows only match SKU-A → null
    - batchDedupDecision({zoho_so_number:'INV-9'} (no product_sku)) with a row on INV-9 → duplicate_so_number (invoice-only fallback preserved)
    - batchDedupDecision({}) (no zoho_so_number) → null
    - Source shape: in createBatch, the index of `acquireScriptLock(15000)` is LOWER than the index of `batchDedupDecision(`, and `sheetToObjects(BATCHES_SHEET_NAME, true)` appears after the lock; the old pre-lock `var existingBatches = sheetToObjects(BATCHES_SHEET_NAME);` is gone
    - Source shape: in updateGiftCardInvoice, `acquireScriptLock(15000)` precedes `findRowById(GIFT_CARDS_SHEET_NAME`, and the function contains `finally` + `lock.releaseLock()`
    - Whole adminApi.gs still evaluates via `new Function(src)` without throwing
  </behavior>
  <action>
Write the test file first (RED), then edit adminApi.gs (GREEN). Per D-18 these are two SEPARATE edits in one task (Pitfall 6 — different verification shapes).

Edit A — createBatch (D-18, TOCTOU close): extract the dedup logic at :2158-2193 into a new PURE top-level function `batchDedupDecision(existingRows, payload)` placed immediately above createBatch. It takes the already-read row objects (array of {batch_id, zoho_so_number, product_sku}) and returns either null (allowed) or the exact existing error object `{ ok: false, error: 'duplicate_so_number', message: ... }` with the SAME message strings as today for both branches (invoice+SKU with unit_total floor/legacy-1 logic, and invoice-only fallback). It must not reference SpreadsheetApp/LockService/CacheService. Move the full CR-01/2026-07-11 comment block and CONTRACT lines onto the new function. In createBatch, delete the pre-lock dedup block entirely, and as the FIRST statements inside the existing `try {` that follows `var lock = acquireScriptLock(15000);` (:2227-2228), add: when payload.zoho_so_number is present, `var dedup = batchDedupDecision(sheetToObjects(BATCHES_SHEET_NAME, true), payload); if (dedup) return dedup;` — the existing finally releases the lock on that early return. Do NOT move the schedule lookup or checkLocationConflict (out of scope). Do not change the lock timeout.

Edit B — updateGiftCardInvoice (D-18): keep the certNum/invoiceNumber missing_fields validation before the lock; then `var lock = acquireScriptLock(15000); try { ...existing findRowById + header lookup + two setValue calls + invalidateSheetCache(GIFT_CARDS_SHEET_NAME) + return { ok: true }... } finally { lock.releaseLock(); }` — the not_found early return moves inside the try. Behaviour and return shapes otherwise unchanged.

Test file tests/frontend/adminapi-phase82-locks.test.js: copy the loader and sliceFunctionSource helper from adminapi-giftcard-ledger.test.js; the `new Function` factory exposes only batchDedupDecision via a typeof guard. Implement every behavior bullet above. Include a purity assertion that batchDedupDecision's source does not match /SpreadsheetApp|LockService|CacheService|sheetToObjects/.
  </action>
  <verify>
    <automated>npx jest tests/frontend/adminapi-phase82-locks.test.js tests/frontend/adminapi-giftcard-ledger.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function batchDedupDecision(" apps-script/adminApi.gs` returns exactly 1 line
    - `grep -c "sheetToObjects(BATCHES_SHEET_NAME, true)" apps-script/adminApi.gs` ≥ 1, and inside the createBatch slice the lock index < dedup index (asserted by the new test)
    - The new suite passes; adminapi-giftcard-ledger.test.js (unmodified) still passes
    - `git diff --stat tests/frontend/adminapi-giftcard-ledger.test.js` is empty
  </acceptance_criteria>
  <done>Both D-18 lock holes are closed in source and pinned by tests; the dedup contract is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-11 server_token write entries + per-task batch cache-bust (D-09 server half)</name>
  <files>apps-script/adminApi.gs, tests/frontend/adminapi-phase82-dispatch.test.js</files>
  <read_first>
    - apps-script/adminApi.gs lines 249-512 (doPost: batch-token branch, server_token if-chain, staff switch), 2716-2822 (updateBatchTask, bulkUpdateBatchTasks), 3101-3137 (handleBatchTokenPost), 3454-3462 (_invalidateBatchCache)
    - .planning/phases/82-store-agnostic-prerequisites/82-PATTERNS.md section "7 new doPost server_token entries (D-11)"
    - .planning/phases/82-store-agnostic-prerequisites/82-RESEARCH.md "Pitfall 1" and "Pitfall 3"
  </read_first>
  <behavior>
    - Source shape: the server_token branch of doPost (text between `if (payload.server_token) {` and the `Unknown server action` return) contains `action === 'X'` for each of: update_reservation, update_hold, update_homepage, add_batch_task, update_batch_task, propagate_ferm_schedule, regenerate_batch_token
    - Source shape: in that branch, update_reservation/update_hold/add_batch_task/update_batch_task/propagate_ferm_schedule pass the literal 'middleware' as the second argument
    - Source shape: the staff switch still contains every case it had before (update_reservation, update_hold, update_homepage, add_batch_task, update_batch_task, propagate_ferm_schedule, regenerate_batch_token remain for the old browser path — D-19)
    - Pure: _uniqueBatchIds([{ok:true,batch_id:'SV-B-000001'},{ok:true,batch_id:'SV-B-000002'},{ok:true,batch_id:'SV-B-000001'},{ok:false},{ok:true}]) → ['SV-B-000001','SV-B-000002']
    - Pure: _uniqueBatchIds([]) → []; _uniqueBatchIds(undefined) → []
    - Source shape: updateBatchTask's final success return includes `batch_id:`; bulkUpdateBatchTasks calls `_uniqueBatchIds(` and `_invalidateBatchCache(` and returns `affected_batch_ids`
    - Source shape: every doPost dispatch of update_batch_task (server_token entry, staff case) invalidates `r.batch_id || payload.batch_id` (or the equivalent result variable)
  </behavior>
  <action>
Write tests first (RED), then edit (GREEN).

Edit A — D-11: inside the `if (payload.server_token) { ... }` branch of doPost, immediately before the final `return _jsonResponse({ ok: false, error: 'invalid_action', message: 'Unknown server action: ' + action });` (:382), add a commented block "Admin panel write actions (server_token-gated, Phase 82 D-11)" with seven `if (action === '...')` entries following the :338-342 shape:
- update_reservation → `_jsonResponse(updateReservation(payload, 'middleware'))`
- update_hold → `_jsonResponse(updateHold(payload, 'middleware'))`
- update_homepage → `_jsonResponse(updateHomepage(payload))`
- add_batch_task → call addBatchTask(payload, 'middleware'), then `_invalidateBatchCache(payload.batch_id)`, return result
- update_batch_task → `var sUpdateBatchTaskResult = updateBatchTask(payload, 'middleware'); _invalidateBatchCache(sUpdateBatchTaskResult.batch_id || payload.batch_id);` return result
- propagate_ferm_schedule → `_jsonResponse(propagateFermSchedule(payload, 'middleware'))`
- regenerate_batch_token → call regenerateBatchToken(payload), `_invalidateBatchCache(payload.batch_id)`, return result
Do NOT add read actions here — reads reach handleReadAction through doGet's server_token bypass (Pitfall 3). Do NOT remove any staff-switch case (the live browser still uses them until cutover, D-19).

Edit B — per-task cache-bust: (1) change updateBatchTask's final success return to `{ ok: true, message: 'Task updated', batch_id: String(current.batch_id || '') }` (additive field only). (2) Add a PURE top-level helper `_uniqueBatchIds(results)` returning the de-duplicated, order-preserving list of non-empty `batch_id` strings from entries whose `ok` is truthy; tolerate undefined/non-array input by returning []. (3) In bulkUpdateBatchTasks, after the loop, `var affected = _uniqueBatchIds(results); for each id → _invalidateBatchCache(id);` and return `{ ok: true, results: results, affected_batch_ids: affected }`. (4) In the staff switch `case 'update_batch_task'`, invalidate `r.batch_id || payload.batch_id`. Leave the existing `_invalidateBatchCache(payload.batch_id)` calls at the bulk dispatch sites as-is (harmless extra bust).

Test file tests/frontend/adminapi-phase82-dispatch.test.js: same loader/slicer as Task 1 (copy, do not import across test files); factory exposes _uniqueBatchIds. Slice doPost with sliceFunctionSource, then cut the server_token sub-block with indexOf('if (payload.server_token) {') and indexOf("Unknown server action") to scope the assertions.
  </action>
  <verify>
    <automated>npx jest tests/frontend/adminapi-phase82-dispatch.test.js tests/frontend/adminapi-phase82-locks.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Within the doPost server_token sub-block, each of the 7 action names appears exactly once as `action === '<name>'` (asserted by the new suite)
    - `grep -n "function _uniqueBatchIds(" apps-script/adminApi.gs` returns exactly 1 line
    - `grep -n "affected_batch_ids" apps-script/adminApi.gs` returns ≥ 1 line
    - Full frontend suite `npm test` passes with zero modified existing test files (`git diff --name-only tests/` lists only the two new files)
    - `npm run lint` passes (adminApi.gs is not linted, but the new test files are)
  </acceptance_criteria>
  <done>The server_token path accepts every admin write the proxy will forward, and batch caches are busted per task's own batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway middleware → Apps Script doPost | server_token-authenticated writes; the token is the only credential |
| Concurrent Apps Script executions | Two requests racing on the same sheet rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02-01 | Tampering | createBatch dedup (check-then-append race) | mitigate | Dedup read uses sheetToObjects(BATCHES_SHEET_NAME, true) AFTER acquireScriptLock(15000), inside the same try as generateNextId + appendRow (D-18) |
| T-82-02-02 | Tampering | updateGiftCardInvoice racing redeem/reload on GiftCards | mitigate | Whole read-modify-write wrapped in acquireScriptLock(15000) / finally releaseLock (D-18) |
| T-82-02-03 | Elevation of Privilege | 7 new server_token entries | mitigate | Entries sit inside the existing SERVER_WRITE_TOKEN check (:263-267); the if-chain keeps its closed `invalid_action` default; the allowlist boundary for which actions the browser may request is enforced upstream by 82-04's hardcoded proxy allowlist |
| T-82-02-04 | Repudiation | Admin writes recorded as 'middleware' instead of staff email | accept | Same trade Phase 76 accepted for BrewPad; updateReservation/updateHold do not persist the actor, and holds carry resolved_by in the client-sent updates. Flag in SUMMARY as a candidate for passing req.staffEmail through later |
| T-82-02-05 | Denial of Service | Lock contention from the extra locked function | accept | updateGiftCardInvoice is a single-row write; 15 s wait matches the other 10 lock sites |
</threat_model>

<verification>
- `npx jest tests/frontend/adminapi-phase82-locks.test.js tests/frontend/adminapi-phase82-dispatch.test.js` green
- `npm test` and `cd zoho-middleware && npm test` green; `npm run lint` clean
- No existing test file modified
</verification>

<success_criteria>
adminApi.gs carries both D-18 lock fixes, the 7 D-11 server_token entries and per-task cache busting, all pinned by new tests; nothing with a live caller removed (safe for the D-19 step-1 redeploy).
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-02-SUMMARY.md` when done
</output>
