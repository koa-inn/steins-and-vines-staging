---
phase: 82-store-agnostic-prerequisites
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - js/batch.js
  - js/batch.min.js
  - batch.html
  - tests/frontend/batch-public-proxy.test.js
  - js/admin.js
  - js/admin.min.js
  - admin.html
  - kiosk.html
  - brewpad.html
  - index.html
autonomous: true
requirements: [DB-01]

must_haves:
  truths:
    - "D-12/D-13: batch.html's view, auto-refresh, task tick and bulk plato submit all go to MIDDLEWARE_URL + /api/batch/public/:id[/tasks|/readings]; js/batch.js no longer reads SHEETS_CONFIG.ADMIN_API_URL (ROADMAP criterion 1)"
    - "D-14: the 60 s auto-refresh interval and exponential backoff are unchanged; no retry wrapper is added"
    - "Rendering and user-visible behaviour are unchanged — the response shape {ok, data:{batch, tasks, plato_readings, vessel_history}} is identical"
    - "js/batch.js has a module.exports test seam (net-new, orchestrator guidance 2 — its own task) and a Jest suite covering all 3 actions"
    - "D-23: batch.html no longer loads js/admin-config.js; npm run build regenerated js/batch.min.js"
  artifacts:
    - path: "js/batch.js"
      provides: "Middleware transport + test seam"
      contains: "/api/batch/public/"
    - path: "tests/frontend/batch-public-proxy.test.js"
      provides: "Parity tests for view, refresh, tick, readings"
      min_lines: 80
    - path: "js/batch.min.js"
      provides: "Rebuilt artifact"
  key_links:
    - from: "js/batch.js"
      to: "/api/batch/public/:id"
      via: "fetch(apiUrl + '/api/batch/public/' + encodeURIComponent(batchId) ...)"
      pattern: "api/batch/public/"
---

<objective>
Move the public batch page off direct Apps Script calls onto the token routes from 82-05, with a new test seam and parity tests.

Purpose: DB-01 — js/batch.js is one of the two browser surfaces that still call ADMIN_API_URL.
Output: js/batch.js (+ rebuilt min), batch.html script tag change, new tests/frontend/batch-public-proxy.test.js.

Wave note: this plan runs `npm run build`, which also stamps js/admin.js and several HTML pages — that is why the admin.js plans (82-06, 82-07) are sequenced after it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@.planning/phases/82-store-agnostic-prerequisites/82-PATTERNS.md

<interfaces>
Route contract (built by 82-05; tests here mock fetch so no runtime dependency):
- GET  MIDDLEWARE_URL + '/api/batch/public/' + encodeURIComponent(batchId) + '?token=' + encodeURIComponent(batchToken)
- POST MIDDLEWARE_URL + '/api/batch/public/' + encodeURIComponent(batchId) + '/tasks'    JSON {batch_token, task_id, updates:{completed}}
- POST MIDDLEWARE_URL + '/api/batch/public/' + encodeURIComponent(batchId) + '/readings' JSON {batch_token, readings:[...]}
Headers for POST: {'Content-Type': 'application/json'}. No credentials/session header (public page).

js/batch.js today (406 lines, IIFE): state vars batchId/batchToken/batchData/apiUrl (:4-7); init() :28 (apiUrl from ADMIN_API_URL :29); loadBatch() :45; toggleTask(taskId, completed) :142 (POST text/plain to apiUrl :151); bindStagingHandlers() :275 with the submit click handler :285-320 (POST bulk_add_plato_readings :289); startAutoRefresh() :377 (poll body :379-397, URL :388); DOMContentLoaded → init(); bindPlatoSubmit() :402.
SHEETS_CONFIG.MIDDLEWARE_URL is set per hostname by js/sheets-config.js (:55-70), already loaded by batch.html :92.
Test-seam pattern: bottom-of-file `if (typeof module !== 'undefined' && module.exports) { module.exports = Object.assign(module.exports || {}, {...}); }` (js/brewpad.js end of file). Harness pattern: tests/frontend/brewpad-read-retry.test.js :18-30 (global stubs) + tests/frontend/admin-session-auth.test.js :14-42 (DOM fixture + SHEETS_CONFIG stub + require).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a batch.js test seam with characterization tests of today's behaviour</name>
  <files>js/batch.js, tests/frontend/batch-public-proxy.test.js</files>
  <read_first>
    - js/batch.js (full file, 406 lines)
    - batch.html (element ids the script touches)
    - tests/frontend/brewpad-read-retry.test.js lines 1-60 and tests/frontend/admin-session-auth.test.js lines 1-60 (harness patterns)
  </read_first>
  <action>
Pure refactor + seam, NO behaviour change (orchestrator guidance 2: separate from the transport rewrite).
1. Extract the bulk-submit click handler body (:285-320) into a named function `submitPlatoReadings(submitBtn)`; the click listener calls it. Extract the auto-refresh poll body (everything inside the setInterval callback from `_lastRefreshAttempt = Date.now();` through the fetch chain) into `refreshBatchOnce()`; the setInterval callback keeps the document.hidden and backoff checks, then calls refreshBatchOnce(). Hoist `var _lastRefreshAttempt = 0;` above startAutoRefresh if needed for clarity.
2. Append the test seam at the end of the IIFE: module.exports = Object.assign(module.exports || {}, { init, loadBatch, toggleTask, submitPlatoReadings, refreshBatchOnce, _setStateForTest: function (s) { batchId, batchToken, apiUrl from s }, _setStagingRowsForTest: function (rows) { _platoStagingRows = rows; }, _getStateForTest: function () { return { batchId, batchToken, apiUrl, batchData }; } }) — written in ES5 object-literal form (no shorthand properties; ES5 per CLAUDE.md).
3. Create tests/frontend/batch-public-proxy.test.js: DOM fixture with the batch.html elements batch.js touches (at least #batch-loading, #batch-content, #batch-error, #batch-toast-container and whatever renderBatch/renderTasks write into — copy ids from batch.html), global SHEETS_CONFIG = {ADMIN_API_URL:'https://script.google.com/test/admin', MIDDLEWARE_URL:'http://mw.test'}, global.fetch = jest.fn resolving {ok:true,status:200,json:()=>Promise.resolve({ok:true,data:{batch:{...minimal},tasks:[],plato_readings:[],vessel_history:[]}})}, jsdom location set via window.history.pushState({}, '', '/batch.html?id=SV-B-000123&token=' + 'a'.repeat(32)). Characterization tests (they describe TODAY's transport and will be flipped in Task 2): init() fetches ADMIN_API_URL + '?action=get_batch_public&batch_id=SV-B-000123&token=…'; toggleTask('BT-000001', true) POSTs to ADMIN_API_URL with text/plain and body containing action update_batch_task, batch_token, batch_id, task_id, updates.completed true; submitPlatoReadings posts bulk_add_plato_readings with readings from _setStagingRowsForTest; refreshBatchOnce fetches the same GET URL as loadBatch.
  </action>
  <verify>
    <automated>npx jest tests/frontend/batch-public-proxy.test.js && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function submitPlatoReadings(" js/batch.js` = 1 and `grep -c "function refreshBatchOnce(" js/batch.js` = 1
    - `grep -c "module.exports" js/batch.js` ≥ 1
    - The characterization suite (≥ 4 tests) passes against the unchanged transport
    - `npm test` passes; `npm run lint` passes (no ES6 shorthand introduced)
  </acceptance_criteria>
  <done>batch.js is testable and its current request shapes are pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint batch.js to /api/batch/public/* (D-13), drop admin-config.js from batch.html (D-23), rebuild</name>
  <files>js/batch.js, tests/frontend/batch-public-proxy.test.js, batch.html, js/batch.min.js, js/admin.js, js/admin.min.js, admin.html, kiosk.html, brewpad.html, index.html</files>
  <read_first>
    - js/batch.js (after Task 1)
    - tests/frontend/batch-public-proxy.test.js (after Task 1)
    - batch.html lines 85-95 (script tags)
    - js/sheets-config.js lines 50-75 (MIDDLEWARE_URL per hostname)
    - package.json "build" script
  </read_first>
  <behavior>
    - init() with SHEETS_CONFIG.MIDDLEWARE_URL 'http://mw.test' and NO ADMIN_API_URL fetches exactly 'http://mw.test/api/batch/public/SV-B-000123?token=' + 'a'*32 (GET, no options body) and renders
    - init() with MIDDLEWARE_URL empty shows the 'Configuration error' state and issues no fetch
    - toggleTask('BT-000001', true) → one fetch to 'http://mw.test/api/batch/public/SV-B-000123/tasks', method POST, Content-Type application/json, body exactly {batch_token, task_id:'BT-000001', updates:{completed:true}} (no action, no batch_id)
    - submitPlatoReadings(btn) → one fetch to '.../SV-B-000123/readings', POST JSON, body exactly {batch_token, readings:<staged rows>}
    - refreshBatchOnce() → same GET URL as init; on {ok:false} it increments the failure counter (backoff unchanged)
    - A 429 / 502 JSON response ({ok:false,error:...}) surfaces through the existing error/toast paths without throwing
    - No request URL anywhere contains 'script.google.com' or '?action='
    - Batch ids with characters needing encoding are passed through encodeURIComponent in the path
  </behavior>
  <action>
RED: update the Task-1 characterization tests in the new suite (this plan's own file — not an existing test) to the <behavior> expectations; set ADMIN_API_URL absent in SHEETS_CONFIG. GREEN:
1. init(): `apiUrl = (typeof SHEETS_CONFIG !== 'undefined' && SHEETS_CONFIG.MIDDLEWARE_URL) || '';` keep the `!apiUrl → showError('Configuration error')` guard.
2. Add a small helper `function publicBatchUrl(suffix)` returning apiUrl + '/api/batch/public/' + encodeURIComponent(batchId) + (suffix || ''). loadBatch() and refreshBatchOnce() use `fetch(publicBatchUrl('?token=' + encodeURIComponent(batchToken)))`.
3. toggleTask(): `fetch(publicBatchUrl('/tasks'), { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ batch_token: batchToken, task_id: taskId, updates: { completed: completed } }) })` — response handling unchanged.
4. submitPlatoReadings(): `fetch(publicBatchUrl('/readings'), { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ batch_token: batchToken, readings: _platoStagingRows }) })` — response handling unchanged.
5. No retry logic added (PATTERNS "do not introduce retry logic to batch.js"); poll interval 60 s and backoff unchanged (D-14).
6. batch.html: delete the `<script src="js/admin-config.js" defer></script>` line (D-23). Do not add a CSP (none exists on batch.html today; out of scope per orchestrator guidance 3 — note it in the SUMMARY as a pre-existing gap).
7. Run `npm run build` (D-23; regenerates js/batch.min.js and stamps admin.js/HTML cache versions). Run `bash scripts/check-artifact-drift.sh` if it applies to batch.min.js.
  </action>
  <verify>
    <automated>npx jest tests/frontend/batch-public-proxy.test.js && npm test && npm run lint && ! grep -n "ADMIN_API_URL" js/batch.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADMIN_API_URL" js/batch.js` = 0 (ROADMAP criterion 1 for batch.js)
    - `grep -c "script.google.com\|?action=" js/batch.js` = 0
    - `grep -c "admin-config.js" batch.html` = 0
    - `grep -c "api/batch/public" js/batch.min.js` ≥ 1 (rebuilt artifact carries the new transport)
    - `npm test`, `cd zoho-middleware && npm test` and `npm run lint` pass; `git diff --name-only tests/` shows only the new suite
  </acceptance_criteria>
  <done>The public batch page talks only to the middleware; artifacts rebuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser (QR link holder) → middleware | batch_token travels in URL query (GET) and JSON body (POST) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-08-01 | Tampering | path injection via batchId from the URL | mitigate | batchId always passed through encodeURIComponent before being placed in the path; Apps Script regex-validates SV-B-\d{6} |
| T-82-08-02 | Information Disclosure | batch token in URLs/logs | accept | Unchanged from today (token already in the page URL and the Apps Script GET) |
| T-82-08-03 | Tampering | XSS from batch data rendered by batch.js | accept | Rendering code unchanged (esc() already used); transport-only change |
| T-82-08-04 | Denial of Service | poll storm against the new rate-limited route | mitigate | 60 s interval + exponential backoff unchanged; 429 treated as a failure by the existing backoff |
</threat_model>

<verification>
- New suite green; full frontend + middleware suites green; lint clean
- `grep ADMIN_API_URL js/batch.js` empty
</verification>

<success_criteria>
batch.js reaches Apps Script only through /api/batch/public/*, with parity tests per action and rebuilt artifacts.
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-08-SUMMARY.md` when done
</output>
