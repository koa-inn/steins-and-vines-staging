---
phase: 82-store-agnostic-prerequisites
plan: 06
type: execute
wave: 3
depends_on: ["82-08", "82-03", "82-04"]
files_modified:
  - js/admin.js
  - js/admin.min.js
  - admin.html
  - kiosk.html
  - brewpad.html
  - index.html
  - tests/frontend/admin-batch-id-cache-bust.test.js
  - tests/frontend/admin-proxy-transport.test.js
  - tests/frontend/admin-api-get-token.test.js
autonomous: true
requirements: [DB-01]

must_haves:
  truths:
    - "D-05/D-06: adminApiGet and adminApiPost keep their signatures but POST JSON to MIDDLEWARE_URL + '/api/admin/proxy' with credentials:'include'; no Google access token is sent in any data call"
    - "D-08: reads retry twice on HTTP 502/503/504 (and on network rejection); writes are sent exactly once — no status retry and no network-rejection retry"
    - "D-07: the body-substring isUnauthorizedError/handleUnauthorized path is gone; only a real middleware res.status === 401 logs the user out"
    - "D-09 + orchestrator guidance 4: every admin.js batch-mutating write carries batch_id — including the transfer-confirm and skip-transfer update_batch_task calls in showTransferPrompt (regression test written first), the per-batch bulk_update_batch_tasks save, and the upcoming-tasks confirm save"
    - "D-15/D-16: loadAllData loads Ingredients via adminApiGet('get_ingredients') in the same Promise.all as kits/holds/schedule; every `if (SHEETS_CONFIG.ADMIN_API_URL) … else <direct Sheets>` branch and the loadAllData fallback are deleted"
    - "D-22: only the admin.js settings object of tests/frontend/admin-api-get-token.test.js is changed (to the D-06/D-07 contract); its shared suite body is untouched"
    - "D-23: npm run build regenerated js/admin.min.js"
  artifacts:
    - path: "js/admin.js"
      provides: "Proxy transport, reads-retry/writes-once, 401-only logout, fallback deletion"
      contains: "/api/admin/proxy"
    - path: "tests/frontend/admin-batch-id-cache-bust.test.js"
      provides: "Regression tests for batch_id on transfer/skip/bulk/upcoming task writes"
    - path: "tests/frontend/admin-proxy-transport.test.js"
      provides: "Transport, retry and 401 contract tests"
  key_links:
    - from: "adminApiGet"
      to: "/api/admin/proxy"
      via: "fetchWithRetry(mwUrl() + '/api/admin/proxy', ..., 2, [502, 503, 504])"
      pattern: "fetchWithRetry\\(mwUrl\\(\\) \\+ '/api/admin/proxy'"
    - from: "loadAllData"
      to: "get_ingredients"
      via: "adminApiGet('get_ingredients') in Promise.all"
      pattern: "adminApiGet\\('get_ingredients'\\)"
---

<objective>
Switch js/admin.js's generic API helpers to the session-authenticated middleware proxy with Phase-76 retry and 401 semantics, fix the missing batch_id cache-bust sites, and delete every ADMIN_API_URL-gated fallback branch.

Purpose: DB-01 — this is the bulk of the "55 call sites" move; keeping the helper signatures means the call sites do not change (Claude's discretion, CONTEXT specifics).
Output: js/admin.js (+ rebuilt min), two new test suites, the D-22-approved amendment to one existing test's admin.js settings block.

Scope boundary: the ~15 functions that call the Sheets API directly and unconditionally are 82-07's job; this plan only strips the check_auth pre-check out of the Sheets helpers so they keep working in the interim commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@.planning/phases/82-store-agnostic-prerequisites/82-PATTERNS.md
@.planning/phases/82-store-agnostic-prerequisites/82-03-SUMMARY.md
@.planning/phases/82-store-agnostic-prerequisites/82-04-SUMMARY.md

<interfaces>
Proxy contract (82-04): POST {MIDDLEWARE_URL}/api/admin/proxy, JSON body {action, ...params}; identity via x-session-token header (added automatically to every MIDDLEWARE_URL request by admin.js's fetch-wrapper IIFE :10-39 in the browser). Responses: upstream JSON {ok, data|error|message}; 400 {ok:false,error:'invalid_action'}; 401/403 from requireTiers; 502 {ok:false,error:'server_error'}.

js/admin.js today (verified):
- fetchWithRetry(url, options, retries) :634 — retries network rejections only, used for reads AND writes
- isUnauthorizedError :655, handleUnauthorized :660, `_handlingUnauthorized` var :59 and reset in onTokenResponse :497
- adminApiGet :677, adminApiPost :709 — POST text/plain to ADMIN_API_URL with token: accessToken
- sheetsGet :734, sheetsUpdate :745 / sheetsAppend :776 / sheetsBatchUpdate :807 each pre-call adminApiGet('check_auth') when ADMIN_API_URL is set
- loadAllData :839 (API branch :846-873, ingredients via sheetsGet :863, fallback :875-894); loadReservationsPage guard :901
- ADMIN_API_URL branches: initFilterListeners :1383, renderReservationsTab :1431, confirmHold :1809 (fallback :1850-1904), releaseHold :1933 (fallback :1974-2030), setReservationStatus :2067 (fallback :2096-2117), checkReservationStatus :2140 (else :2154-2172), homepage save :5148 (else :5163-5174), finishDataLoad wrapper :8605
- showTransferPrompt(batchId, taskId) :6300 — update_batch_task without batch_id at :6346 (confirm) and :6368 (skip)
- bindCheckboxes (batch detail) :6467 — bulk_update_batch_tasks :6499 without batch_id
- calendar view save :8042 — multi-batch bulk_update_batch_tasks (fixed server-side by 82-02's per-task bust; do NOT invent a batch_id here)
- doUpcomingSave :8166 — update_batch_task :8229 per task without batch_id
- module.exports seam :10148 (has _adminApiGetForTest: adminApiGet, _setAccessToken, _setUserEmail)
- Reference implementation: js/brewpad.js :1690-1786 (fetchWithRetry with retryStatuses, adminApiGet/adminApiPost, mwUrl, _handleMiddlewareResponse)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regression tests first, then add batch_id to every batch-mutating task write (D-09, guidance 4)</name>
  <files>js/admin.js, tests/frontend/admin-batch-id-cache-bust.test.js</files>
  <read_first>
    - js/admin.js lines 6300-6385 (showTransferPrompt), 6440-6520 (openBatchDetail's bindCheckboxes + bulk save), 8150-8250 (doUpcomingSave), 8014-8060 (calendar save), 10148-10185 (export seam)
    - tests/frontend/admin-session-auth.test.js lines 1-70 (admin.js DOM fixture + require pattern)
    - .planning/phases/82-store-agnostic-prerequisites/82-RESEARCH.md "Pitfall 1" and "Pitfall 2"
  </read_first>
  <behavior>
    - showTransferPrompt('SV-B-000123', 'BT-000009') then clicking #transfer-skip issues exactly one fetch whose JSON body has action 'update_batch_task', task_id 'BT-000009' and batch_id 'SV-B-000123'
    - Same, with #transfer-vessel set to 'V-1' and clicking #transfer-confirm → body has batch_id 'SV-B-000123' and transfer_location.vessel_id 'V-1'
    - Assertions parse the body regardless of request URL, so the tests stay green after Task 2 moves the URL
    - Both tests FAIL before the fix (RED) and pass after (GREEN)
  </behavior>
  <action>
1. RED: create tests/frontend/admin-batch-id-cache-bust.test.js — admin.js DOM fixture incl. the modal elements openModal writes into (#admin-modal, #admin-modal-title, #admin-modal-body — copy from admin-session-auth.test.js) and whatever showTransferPrompt/bindVesselSearch need; global SHEETS_CONFIG with MIDDLEWARE_URL 'http://mw.test' AND ADMIN_API_URL 'https://script.google.com/test/admin' (so the pre-Task-2 transport also issues a fetch); fetch mocked to resolve {ok:true,status:200,json:{ok:true}}. Export `showTransferPrompt` through the existing module.exports seam as `_showTransferPromptForTest`. Run → confirm the two tests fail on missing batch_id.
2. GREEN: add `batch_id: batchId` to both update_batch_task payloads in showTransferPrompt.
3. In openBatchDetail's bulk save (:6499), add `batch_id: <the batch id in scope>` to the bulk_update_batch_tasks payload (read the enclosing scope to find the variable name; if none is in scope, record that in the SUMMARY and rely on 82-02's server-side per-task bust).
4. In doUpcomingSave (:8229), set `payload.batch_id = t.batch_id` when t.batch_id is present (confirm from get_tasks_upcoming's row shape in apps-script getTasksUpcoming that tasks carry batch_id; record the finding).
5. Leave the calendar-view multi-batch save (:8042) without a batch_id — 82-02 made bulkUpdateBatchTasks bust each task's own batch server-side; add a one-line comment there pointing to that.
6. Audit: `grep -n "adminApiPost('\(update_batch\|update_batch_task\|add_batch_task\|bulk_add_plato_readings\|update_plato_reading\|delete_plato_reading\|delete_batch\|update_batch_schedule\|regenerate_batch_token\)'" js/admin.js` and confirm every hit's payload carries batch_id (or is the calendar site). Paste the audit table in the SUMMARY.
  </action>
  <verify>
    <automated>npx jest tests/frontend/admin-batch-id-cache-bust.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Both regression tests fail on the pre-fix commit and pass after (executor records the RED run output in the SUMMARY)
    - `sed -n '/function showTransferPrompt/,/^  }/p' js/admin.js | grep -c "batch_id: batchId"` = 2
    - SUMMARY contains the batch_id audit table covering every batch-mutating adminApiPost site
  </acceptance_criteria>
  <done>No admin.js batch write relies on a stale gb:&lt;batchId&gt; cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proxy transport — reads retry, writes once, 401-only logout (D-05..D-08, D-22)</name>
  <files>js/admin.js, tests/frontend/admin-proxy-transport.test.js, tests/frontend/admin-api-get-token.test.js</files>
  <read_first>
    - js/admin.js lines 54-60, 480-520 (onTokenResponse), 600-730 (clearSession, signOut, fetchWithRetry, isUnauthorizedError, handleUnauthorized, adminApiGet, adminApiPost), 10148-10185 (seam)
    - js/brewpad.js lines 1680-1790 (reference transport + _handleMiddlewareResponse / _enterLoggedOutState)
    - tests/frontend/admin-api-get-token.test.js (full — the admin.js settings object at the bottom is the ONLY part to change)
    - tests/frontend/brewpad-read-retry.test.js (retry-assertion pattern with fake timers)
    - .planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md D-06, D-07, D-08, D-22
  </read_first>
  <behavior>
    - adminApiGet('get_kits') → one fetch to MIDDLEWARE_URL + '/api/admin/proxy', method POST, credentials 'include', Content-Type application/json, body {action:'get_kits'} with no token key
    - adminApiGet('get_reservations', {limit:50, offset:0, status:'pending'}) → body has action + the three params
    - adminApiPost('update_hold', {holdId:'H-1'}) → body {action:'update_hold', holdId:'H-1'} with no token key
    - Read: first response 502, second 200 {ok:true} → resolves, fetch called twice; three consecutive 503 → rejects after exactly 3 calls
    - Read: fetch rejects (network) then 200 → resolves after 2 calls
    - Write: 502 → rejects after exactly ONE fetch call; network rejection → rejects after exactly ONE call
    - Response 200 {ok:false, message:'unauthorized'} → promise rejects, sign-in screen NOT shown, sv_session_token NOT cleared
    - Response status 401 → promise rejects AND the logged-out state is entered (#admin-signin shown, #admin-dashboard hidden, localStorage sv_session_token removed)
    - adminApiGet/adminApiPost no longer reject with 'Admin API not configured' when ADMIN_API_URL is absent
    - `grep -c "isUnauthorizedError\|handleUnauthorized" js/admin.js` = 0
  </behavior>
  <action>
RED: create tests/frontend/admin-proxy-transport.test.js (fixture copied from admin-api-get-token.test.js's admin surface; jest fake timers for the 1 s backoff) covering every bullet; expose `_adminApiPostForTest: adminApiPost` in the seam. Then apply the D-22 amendment: in tests/frontend/admin-api-get-token.test.js change ONLY the second runAdminApiGetTokenSuite({...}) argument (name 'admin.js'): urlTestTitle → 'issues a fetch to the middleware admin proxy (no ADMIN_API_URL, no query string) -- Phase 82 D-05', expectedUrl → `function (cfg) { return cfg.MIDDLEWARE_URL + '/api/admin/proxy'; }`, bodyTestTitle → 'POSTs action + params in the JSON body, no token field -- Phase 82 D-06', expectTokenField → false, unauthorizedTestTitle → '{ ok:false, message: "unauthorized" } rejects WITHOUT logging out (Phase 82 D-07: only a real middleware 401 does)', assertUnauthorizedOutcome → assert `document.getElementById('admin-dashboard').style.display` is not 'none'; update that object's inline comment to cite Phase 82 D-22. The runAdminApiGetTokenSuite function, the brewpad.js settings object and every other line stay byte-identical (verify with git diff).

GREEN in js/admin.js:
1. Replace fetchWithRetry with the js/brewpad.js :1711-1730 version (4th param retryStatuses; network rejection retried while retries > 0), keeping ES5.
2. Add `function mwUrl()` (copy of brewpad.js :1784-1786) near the helpers.
3. Delete isUnauthorizedError and handleUnauthorized. Add `function enterLoggedOutState()` holding handleUnauthorized's old body (timer stop, clearSession, null accessToken/userEmail, show #admin-signin, hide dashboard/denied/signout, clear email, showSignInButton) with a re-entrancy guard var renamed `_enteringLoggedOut`; update the onTokenResponse reset line (:497) to the new var name. Add `function handleProxyResponse(r)` that, when r.status === 401, calls enterLoggedOutState() and throws Error('Session expired'); otherwise parses JSON and throws on `!r.ok || !data || !data.ok` with (data.message || data.error || 'HTTP ' + r.status), else returns data.
4. adminApiGet(action, params): body {action, ...params}; `return fetchWithRetry(mwUrl() + '/api/admin/proxy', { method:'POST', credentials:'include', headers:{'Content-Type':'application/json'}, body: JSON.stringify(body) }, 2, [502, 503, 504]).then(handleProxyResponse);` — no ADMIN_API_URL check, no token.
5. adminApiPost(action, payload): payload = payload || {}; payload.action = action; NO token; `return fetchWithRetry(mwUrl() + '/api/admin/proxy', {...same options...}, 0).then(handleProxyResponse);` — retries 0: D-08 says writes are sent ONCE; this is intentionally stricter than brewpad.js (which retries a write once on network rejection) because a dropped connection can follow a write that Apps Script already applied.
6. Do NOT edit the fetch-wrapper IIFE (:10-39) — it already attaches x-session-token to MIDDLEWARE_URL requests.
If any EXISTING test other than the D-22 settings block fails, stop and report — do not edit it.
  </action>
  <verify>
    <automated>npx jest tests/frontend/admin-proxy-transport.test.js tests/frontend/admin-api-get-token.test.js tests/frontend/admin-batch-id-cache-bust.test.js tests/frontend/admin-session-auth.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isUnauthorizedError\|handleUnauthorized" js/admin.js` = 0
    - `grep -c "'/api/admin/proxy'" js/admin.js` ≥ 2
    - `sed -n '/function adminApiPost/,/^  }/p' js/admin.js | grep -c "token"` = 0
    - `git diff tests/frontend/admin-api-get-token.test.js` touches only lines inside the second runAdminApiGetTokenSuite({...}) call (D-22)
    - New transport suite passes, incl. write-sent-once on 502 and on network rejection
  </acceptance_criteria>
  <done>admin.js data calls go through the proxy with Phase-76 semantics and the stricter D-08 write rule.</done>
</task>

<task type="auto">
  <name>Task 3: Delete every ADMIN_API_URL-gated fallback, load Ingredients via the proxy, rebuild (D-15, D-16, D-23)</name>
  <files>js/admin.js, js/admin.min.js, admin.html, kiosk.html, brewpad.html, index.html</files>
  <read_first>
    - js/admin.js lines 734-925 (Sheets helpers, loadAllData, loadReservationsPage), 1374-1440, 1801-2180 (confirmHold, releaseHold, setReservationStatus, checkReservationStatus), 5130-5180 (homepage save), 8598-8610 (finishDataLoad wrapper)
    - .planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md D-15, D-16, D-17
  </read_first>
  <action>
1. loadAllData: remove the `if (SHEETS_CONFIG.ADMIN_API_URL) {` wrapper, the trailing `return;` and the whole fallback Promise.all below it. Add `adminApiGet('get_ingredients')` as a sixth element of the existing Promise.all and parse it with `parseSheetData(results[5].data, 'ingredients')` before finishDataLoad(); delete the chained `sheetsGet(SHEETS_CONFIG.SHEET_NAMES.INGREDIENTS + '!A:Z')` step (D-15).
2. loadReservationsPage: delete the `if (!SHEETS_CONFIG.ADMIN_API_URL)` early return.
3. initFilterListeners (:1383): keep the server reload branch unconditionally, delete the client-side else. renderReservationsTab (:1431): delete the `if (!SHEETS_CONFIG.ADMIN_API_URL) { client-side filtering }` block (the server filters, as today in prod).
4. confirmHold / releaseHold: unwrap the ADMIN_API_URL branch (keep the adminApiPost('update_hold') chain exactly) and delete everything after its `return;` down to the function end (the "Fallback: direct Sheets API" blocks). updateKitStockAfterConfirm / updateKitOnHoldAfterRelease stay as they are here — 82-07 rewires them.
5. setReservationStatus: unwrap and delete the fallback. checkReservationStatus: unwrap and delete the else branch.
6. Homepage save (:5148): unwrap the adminApiPost('update_homepage') branch and delete the else branch (the direct-Sheets clear + sheetsUpdate).
7. finishDataLoad wrapper (:8605): call loadBatchDashboardSummary() unconditionally.
7a. Orphaned helpers — delete the FUNCTION DEFINITIONS, not only call sites. Planning-time grep (2026-09-23) found: `sheetsBatchUpdate` (definition :807, ZERO callers anywhere in js/ or tests/) → delete it now. Re-run `grep -n "sheetsBatchUpdate\|colLetter(" js/admin.js` after the fallback deletions and record the counts. NOT orphaned, keep: `saveHomepageToSheets` (:5114) is the live Homepage save-button handler (:5108) — only its `else` direct-Sheets branch is deleted (step 6); `colLetter` still has callers in the live direct-Sheets flows that 82-07 rewires, so 82-07 Task 3 deletes it; `sheetsGet`/`sheetsUpdate`/`sheetsAppend` still have live callers until 82-07. For any OTHER function the executor finds whose only callers were inside a deleted fallback branch, delete it too and list it in the SUMMARY.
8. Interim coherence: in sheetsUpdate, sheetsAppend and sheetsBatchUpdate remove the `if (SHEETS_CONFIG.ADMIN_API_URL) { adminApiGet('check_auth') ... }` pre-check (check_auth is not on the proxy allowlist and would now 400); they remain plain direct Sheets calls until 82-07 deletes them. Record in the SUMMARY that after this change admin.js has no check_auth caller (D-16) — the Apps Script case stays until prod cutover (82-03 note).
9. After edits: `grep -n "ADMIN_API_URL" js/admin.js` must return nothing except comments (remove or reword stale comments mentioning it, e.g. the adminApiGet docblock).
10. Run `npm run build` (D-23), then `npm test`, `cd zoho-middleware && npm test`, `npm run lint`. If admin-session-auth.test.js (unmodified) now fails because loadAllData's proxy calls reach DOM code, stop and report rather than editing the test.
  </action>
  <verify>
    <automated>npm run build && npm test && npm run lint && (cd zoho-middleware && npm test) && test "$(grep -v '^\s*//' js/admin.js | grep -c ADMIN_API_URL)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' js/admin.js | grep -c ADMIN_API_URL` = 0 (ROADMAP criterion 1 for admin.js)
    - `grep -c "adminApiGet('get_ingredients')" js/admin.js` = 1
    - `grep -c "check_auth" js/admin.js` = 0
    - `grep -c "Fallback: direct Sheets API" js/admin.js` = 0
    - `grep -c "sheetsBatchUpdate" js/admin.js` = 0 (orphaned helper deleted, not just unreferenced)
    - `grep -c "function saveHomepageToSheets(" js/admin.js` = 1 and its body contains `adminApiPost('update_homepage'` but no `sheets.googleapis.com` / `sheetsUpdate(`
    - SUMMARY lists every function deleted as orphaned (at minimum sheetsBatchUpdate) with a grep proving zero remaining references
    - js/admin.min.js contains '/api/admin/proxy' (`grep -c "api/admin/proxy" js/admin.min.js` ≥ 1)
    - Both test suites and lint pass; `git diff --name-only tests/` lists only this plan's new files plus the D-22 file
  </acceptance_criteria>
  <done>No ADMIN_API_URL branch or fallback remains in admin.js; Ingredients load through the proxy; artifacts rebuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staff browser → middleware | x-session-token is the only credential on data calls after this plan |
| Browser storage | Google access token no longer needed for admin data (still minted at login) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-06-01 | Information Disclosure | Google OAuth token sent on every data call | mitigate | adminApiGet/adminApiPost send no token field (tests assert); identity only via x-session-token (D-06) |
| T-82-06-02 | Tampering | Retried write double-applies after a proxy 502 or dropped connection | mitigate | adminApiPost uses retries 0 — sent exactly once (D-08); tests assert one fetch on 502 and on rejection |
| T-82-06-03 | Denial of Service | Spurious logout from an upstream "unauthorized" body | mitigate | Logout only on real HTTP 401 via handleProxyResponse (D-07); test asserts body-level unauthorized does not log out |
| T-82-06-04 | Tampering | Stale batch detail served for 300 s after a task write | mitigate | batch_id on every batch write (D-09) + server-side per-task bust (82-02); regression tests |
| T-82-06-05 | Denial of Service | Admin traffic now counted by the global 60 req/min per-IP apiLimiter (shared shop IP with kiosk/BrewPad) | accept (monitor) | Dashboard load is ~7 calls; 82-07 batches multi-cell writes into one call; the 82-09 staging walk explicitly watches for 429s and escalates to the owner if seen |
</threat_model>

<verification>
- `npm test`, `cd zoho-middleware && npm test`, `npm run lint` green
- `grep -v '^\s*//' js/admin.js | grep -c ADMIN_API_URL` = 0
- Only the D-22 settings block of an existing test changed
</verification>

<success_criteria>
admin.js's generic helpers talk only to /api/admin/proxy with reads-retry/writes-once and 401-only logout; every ADMIN_API_URL branch and fallback is gone; batch writes carry batch_id.
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-06-SUMMARY.md` when done
</output>
