---
phase: 82-store-agnostic-prerequisites
plan: 10
type: execute
wave: 5
depends_on: ["82-09"]
files_modified:
  - .planning/phases/82-store-agnostic-prerequisites/82-10-SUMMARY.md
autonomous: false
requirements: [DB-01]

must_haves:
  truths:
    - "D-19 step 3: production cutover is a single gated step that runs only after the owner approved the 82-09 staging walk AND explicitly says to cut over"
    - "Production middleware is live before the new admin.js/batch.js are served (gated-deploy health gate)"
    - "Rollback targets (Railway deployment id + Apps Script rollback version) are recorded"
    - "The phase summary records: 7→3 deletion correction (D-02/D-21), the Ingredients/Schedule/Homepage milestone gap (D-21), check_auth kept until the old admin.js is gone (D-16), admin/batch pages have no CSP (pre-existing), writes attributed to 'middleware', the batch-token task-ownership gap, the spreadsheets OAuth scope still requested, and any 429s observed"
  artifacts:
    - path: ".planning/phases/82-store-agnostic-prerequisites/82-10-SUMMARY.md"
      provides: "Cutover record + notes for later phases"
      contains: "Notes for later phases"
  key_links:
    - from: "82-09 staging approval"
      to: "production deploy"
      via: "blocking human-action gate"
      pattern: "prod live"
---

<objective>
Promote Phase 82 to production in one gated step (D-19 step 3) and close out the phase record.

Purpose: DB-01 is only delivered when production browsers stop calling Apps Script / Google Sheets directly.
Output: 82-10-SUMMARY.md with deploy ids, smoke results, rollback targets and notes for later phases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@docs/RUNBOOK.md
@.planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Gated production cutover (D-19 step 3)</name>
  <files>.planning/phases/82-store-agnostic-prerequisites/82-10-SUMMARY.md</files>
  <read_first>
    - docs/RUNBOOK.md (middleware Rollback section ~:82-135, gated deploy / smoke-check semantics ~:185-200)
    - .github/workflows/gated-deploy.yml (workflow_dispatch with a 'reason' input)
  </read_first>
  <action>
Runs ONLY after the owner types "approved" in 82-09 Task 3 AND explicitly says "cut over prod" — no auto-approval (workflow.auto_advance does not apply to this gate). Owner triggers the production deploy per RUNBOOK (gated-deploy.yml workflow_dispatch, reason "Phase 82 store-agnostic prerequisites"), which deploys the production middleware and then the production site (`git push production main --force` path per CLAUDE.md). Middleware must be live before the new admin.js/batch.js are served — the gated workflow's health gate enforces this; if doing it by hand, deploy middleware first.

Post-cutover smoke (owner, production): admin dashboard loads with zero script.google.com/sheets.googleapis.com data calls in Network; one harmless write on a test record; the public batch page for a real batch loads read-only (no tick). Record Railway deployment id, site commit SHA, and the rollback path (Railway dashboard rollback to the previous deployment id + Apps Script rollback version from 82-09 Task 1 — note the Apps Script version is backward-compatible with the old browser code, so it can stay even if the site is rolled back).
  </action>
  <what-built>Everything from 82-02..82-09, promoted to production.</what-built>
  <how-to-verify>
    1. gated-deploy run green (health gate passed).
    2. Production admin dashboard loads; Network shows only middleware data calls.
    3. Production batch page for a real batch loads.
  </how-to-verify>
  <resume-signal>Reply "prod live" with the deploy ids, or "rolled back" with the reason</resume-signal>
  <verify>
    <automated>curl -s https://svmiddleware-production.up.railway.app/health</automated>
  </verify>
  <acceptance_criteria>
    - 82-10-SUMMARY.md records the production deployment id, site SHA, smoke results and rollback targets
    - The 82-10 SUMMARY's "Notes for later phases" section lists: 7→3 deletion correction (D-02/D-21); Ingredients/Schedule/Homepage have no destination in Phases 83-88 (milestone gap); check_auth kept until the old admin.js is gone (delete in a later phase); admin.html/batch.html have no CSP (pre-existing); admin writes attributed to 'middleware' (candidate: pass req.staffEmail); batch-token path does not check task→batch ownership (Phase 87); spreadsheets OAuth scope still requested at admin login; any 429 observations
  </acceptance_criteria>
  <done>Phase 82 is live in production with rollback targets recorded, or cleanly rolled back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → production deploy tooling | gated-deploy.yml workflow_dispatch, production git remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-10-01 | Elevation of Privilege | Unreviewed code reaching production | mitigate | Blocking human-action gate after an approved staging walk; never auto-approved |
| T-82-10-02 | Denial of Service | New site served before new middleware is live (admin calls a missing route) | mitigate | Middleware deploys first; gated-deploy health gate; rollback path recorded |
| T-82-10-03 | Tampering | Production smoke writes touching real data | mitigate | One harmless write on a test record; the real-batch page is opened read-only |
</threat_model>

<verification>
- 82-10-SUMMARY.md has deploy ids, smoke results, rollback targets and the notes list
</verification>

<success_criteria>
Production runs the Phase 82 code with rollback targets recorded, or the cutover is cleanly rolled back with the reason recorded.
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-10-SUMMARY.md` when done
</output>
