---
context: phase
phase: 82-store-agnostic-prerequisites
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-24T00:56:17.320Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: owner's `links.html` — uncommitted unrelated owner edit (data-track attrs) — never `git add` it; stage files explicitly; revert build restamps with `git diff --name-only | grep -v '^links.html$' | xargs git checkout --`
- [ ] CONSTRAINT: staging writes hit LIVE data — staging and production share one Google Sheet and Apps Script v57 — only touch test records; never click Accept Delivery / Save All with real rows changed / reservation status on real customers
- [ ] CONSTRAINT: no production push without explicit owner approval — `git push production main --force` is plan 82-10 only

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Build/drift-check in main checkout | `npm run build` / `scripts/check-artifact-drift.sh` restamp ~27 pages; a leftover restamp blocked a worktree merge this session | blocking | After any build in main, immediately run the revert command above (excludes links.html) and confirm `git status` shows only links.html |
| Native confirm() in browser automation | Generate Slots, Accept Delivery, etc. use window.confirm which freezes Chrome automation | advisory | Read the handler first; if it calls confirm(), skip or hand to owner |
| Reloading admin page | Page reload logs out when GIS silent refresh >5 s; the header ↻ button = "Clear app cache and reload" | advisory | Never reload/click ↻ during a walk; navigate tabs instead |

<current_state>
Phase 82: all 8 code plans merged (82-02..08) + 82-01 owner checks recorded. 82-09: Apps Script v57 deployed (rollback v56, probes 1-7+9 pass), pushed to staging (origin/main abd4485e, smoke checks pass). Staging walk partly done by Claude in Chrome — results table in 82-09-SUMMARY.md "Staging walk". Waiting on owner to finish remaining walk steps and one decision.
</current_state>

<completed_work>
- 82-01..82-08: done, merged, tests green (frontend 2026, middleware 1705, lint, drift)
- 82-09 T1: Apps Script v57 + probes; RUNBOOK deploy row
- 82-09 T2: staging push + 3 smoke checks
- 82-09 T3 (partial): steps 0,1,3,4-add,5-add,8 pass; 9: create batch + transfer-task immediate refresh pass
</completed_work>

<remaining_work>
- Owner: walk step 9 remainder (tick+Save Tasks, add task, plato add/edit/delete, regenerate URL on SV-B-000218), 10 idle >10 min, 11 public batch page (SV-B-000218 QR, private window), 12 BrewPad smoke
- Owner: cleanup test records (list in 82-09-SUMMARY.md)
- Owner decision: kiosk-middleware attribution — fix before 82-10 or follow-up
- Then: set 82-09-SUMMARY status complete, roadmap.update-plan-progress 82 82-09, commit
- 82-10 production promotion (gated) → code review gate → regression gate → gsd-verifier → phase.complete
</remaining_work>

<decisions_made>
- D-18 supersedes 44-02: updated gift-card ledger test assertion kept (owner approved)
- 82-06 amended admin-api-get-token.test.js per D-22 (plan-mandated), kept
- Staging walk: skip hold confirm/release + reservation status; scheduling tested only against existing slots (none render — pre-existing bug)
</decisions_made>

<blockers>
- Owner walk approval + attribution decision (human action)
</blockers>

## Required Reading (in order)
1. `.planning/phases/82-store-agnostic-prerequisites/82-09-SUMMARY.md` — deploy record, staging walk table, findings, cleanup list
2. `.planning/phases/82-store-agnostic-prerequisites/82-10-PLAN.md` — production cutover steps
3. `docs/RUNBOOK.md` Apps Script deploy record — v57 / rollback v56
4. `.planning/phases/82-store-agnostic-prerequisites/82-01-SUMMARY.md` — owner checks (Railway backups unconfirmed)

## Critical Anti-Patterns (do NOT repeat these)
- Leaving build restamps in main → revert immediately (see table)
- Asking owner to paste secrets into commands → use `SERVER_TOKEN="$(pbpaste)"` with only the value on the clipboard

## Infrastructure State
- Apps Script: v57 active (rollback 56), shared by staging + prod
- Staging middleware: new build live (uptime reset ~2026-09-23 21:5x UTC); staging site on abd4485e
- Production: untouched (still pre-Phase-82 frontend/middleware; uses v57 Apps Script, which is additive/backward-compatible — prod admin confirmed loading on v57)
- Local main ahead of origin/main by docs commits (3af5b44c) — push with 82-10 or next staging push
- `scripts/phase82-appsscript-probes.sh` committed (reads token from env/clipboard)
- Follow-up: rotate Apps Script server token after 82-10

<context>
Executing via /gsd-execute-phase 82 orchestrator with worktree executors (all code plans done). Remaining work is gated human steps, so resume in orchestrator mode: collect owner walk result, close 82-09, then 82-10.
</context>

<next_action>
Start with: ask the owner for (a) staging-walk result for steps 9-remainder/10/11/12 and (b) the kiosk-middleware attribution decision. Then close 82-09 and present the 82-10 production checklist.
</next_action>
