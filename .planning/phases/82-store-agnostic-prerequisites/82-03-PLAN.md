---
phase: 82-store-agnostic-prerequisites
plan: 03
type: execute
wave: 2
depends_on: ["82-02"]
files_modified:
  - apps-script/adminApi.gs
  - tests/frontend/adminapi-phase82-inventory-actions.test.js
autonomous: true
requirements: [DB-01]

must_haves:
  truths:
    - "D-15: get_ingredients exists as a read action returning {values: [[...]]} in the same shape the admin Ingredients tab's parseSheetData(..., 'ingredients') consumes"
    - "D-21: get_homepage is KEPT (it gains the admin.js loadHomepageData caller) and returns display-formatted values matching the Sheets API read it replaces"
    - "D-21: six typed, allowlisted write actions exist behind the server_token path — update_inventory_cells (sheet limited to Kits|Ingredients, header-name addressed), append_inventory_row (Kits|Ingredients), import_kits, add_hold, append_schedule_slots, update_schedule_slots — none is a generic cell writer"
    - "update_schedule_slots never changes a slot whose current status is 'booked'"
    - "D-02 as amended by D-21: get_config, update_schedule and update_kits are deleted (case + handler) after a repo-wide grep proves zero callers; check_auth is NOT deleted in this phase because the live pre-cutover admin.js still calls it (D-16 'otherwise leave it and note it', D-19 ordering)"
    - "Milestone gap noted (D-21): Ingredients/Schedule/Homepage have no destination in Phases 83-88"
  artifacts:
    - path: "apps-script/adminApi.gs"
      provides: "get_ingredients, 6 inventory/schedule write actions, 3 deletions"
      contains: "function getIngredients("
    - path: "tests/frontend/adminapi-phase82-inventory-actions.test.js"
      provides: "Pure validator tests + dispatch/deletion source-shape tests"
  key_links:
    - from: "doPost server_token branch"
      to: "updateInventoryCells / appendInventoryRow / importKits / addManualHold / appendScheduleSlots / updateScheduleSlots"
      via: "if (action === '<name>') entries"
      pattern: "action === 'update_inventory_cells'"
    - from: "handleReadAction"
      to: "getIngredients"
      via: "case 'get_ingredients'"
      pattern: "case 'get_ingredients'"
---

<objective>
Give Apps Script the typed actions that replace every browser → Google Sheets API call in js/admin.js (D-15, D-21), and delete the actions that are genuinely dead (D-02 as amended by D-21).

Purpose: 82-07 rewires ~15 admin.js functions onto these actions through /api/admin/proxy; the Sheets helpers can only be deleted once these exist. Row-index addressing is kept for parity (D-21) — no concurrency regression versus today's direct Sheets writes.
Output: Edited adminApi.gs + new Jest suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-store-agnostic-prerequisites/82-CONTEXT.md
@.planning/phases/82-store-agnostic-prerequisites/82-PATTERNS.md
@.planning/phases/82-store-agnostic-prerequisites/82-02-SUMMARY.md

<interfaces>
ACTION CONTRACTS (consumed by 82-04 allowlist and 82-07 admin.js — do not rename):

Reads (dispatched by handleReadAction; reached via doGet server_token bypass):
- get_ingredients → { ok:true, data: { values: string[][] } }   (header row first; getDisplayValues)
- get_homepage    → { ok:true, data: { values: string[][] } }   (existing case; switch getValues → getDisplayValues)

Writes (server_token branch of doPost ONLY; all run under acquireScriptLock(15000)):
- update_inventory_cells { sheet:'Kits'|'Ingredients', updates:[{ row:int>=2, field:string(header name), value:string|number }] } (1..500 updates)
    → { ok:true, count } | { ok:false, error:'invalid_sheet'|'invalid_updates'|'invalid_field'|'invalid_row', message }
    Validates ALL updates before writing ANY. Strings pass through sanitizeInput.
- append_inventory_row { sheet:'Kits'|'Ingredients', values: (string|number)[] (length 1..header count) }
    → { ok:true } | { ok:false, error:'invalid_sheet'|'invalid_values' }
- import_kits { values: (string|number)[][] } — values[0] must equal the current Kits header row exactly; every row length == header length; 2..2001 rows
    → { ok:true, rows:<values.length-1> } | { ok:false, error:'header_mismatch'|'invalid_values' }
    Writes getRange(1,1,values.length,headers.length).setValues(values) — same overwrite-from-A1, no clear, as today's sheetsUpdate('Kits!A1:..').
- add_hold { hold_id:/^H-\d{8}-M\d{3}$/, sku:non-empty, product_name, qty:int>=1, notes }
    → appends [hold_id, '', sku, product_name, qty, 'pending', <now ISO>, '', '', notes] (the exact 10-column order admin.js builds today) → { ok:true, hold_id } | { ok:false, error:'invalid_hold' }
- append_schedule_slots { rows:[[ 'YYYY-MM-DD', time:string(1..16 chars), 'available'|'blocked' ]] } (1..1000 rows)
    → appends rows to Schedule columns A:C → { ok:true, count } | { ok:false, error:'invalid_rows' }
- update_schedule_slots { updates:[{ row:int>=2, status:'available'|'blocked' }] } (1..1000)
    → for each row, reads current status (header 'status'); if it is 'booked' (case-insensitive) the row is skipped; otherwise sets status
    → { ok:true, updated:<n>, skipped:[row,...] } | { ok:false, error:'invalid_updates' }

Existing references (adminApi.gs, verified): constants :49-63 (no INGREDIENTS_SHEET_NAME yet); handleReadAction :143-243 (case 'get_homepage' :162, 'get_kits' :165, 'get_config' :168); getHomepage :735; getKits :744; getConfig :753; updateSchedule :1143; updateKits :1218; staff switch cases 'update_schedule' :398, 'update_kits' :404; sanitizeInput (existing helper); acquireScriptLock :1252. Line numbers shift after 82-02 — locate by name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure validators + get_ingredients / get_homepage reads (D-15, D-21)</name>
  <files>apps-script/adminApi.gs, tests/frontend/adminapi-phase82-inventory-actions.test.js</files>
  <read_first>
    - apps-script/adminApi.gs: constants block (:40-63), handleReadAction (:143-243), getHomepage/getKits/getConfig (:735-765), updateKits (:1218) and sanitizeInput (grep `function sanitizeInput`)
    - js/admin.js lines 1262-1297 (parseSheetData — reads response.values only), 2470-2525 (manual hold row), 2612-2640 (saveAllChanges), 2700-2740 (add kit row), 2793-2870 (ingredient delete/add), 4030-4080 (slot generation), 4265-4430 (slot status updates), 4658-4690 (applyImport)
    - tests/frontend/adminapi-giftcard-ledger.test.js lines 1-90 (loader pattern)
  </read_first>
  <behavior>
    - validateInventoryCellUpdates(['sku','stock','on_hold','last_updated'], 10, [{row:2, field:'stock', value:5}]) → {ok:true, writes:[{row:2, col:2, value:5}]}
    - unknown field 'price' → {ok:false, error:'invalid_field'}; row 1 (header) → invalid_row; row 11 (> lastRow 10) → invalid_row; row '2' non-integer string → invalid_row; empty array or >500 entries → invalid_updates; ANY invalid entry fails the whole batch (no partial writes list)
    - resolveInventorySheetName('Kits') → 'Kits'; ('Ingredients') → 'Ingredients'; ('Holds'), (''), (undefined) → null
    - validateAppendRow(['a','b','c'], ['x','y']) → ok; values longer than headers → invalid_values; empty/ non-array → invalid_values
    - validateKitsImport(headers, [headers, row]) → ok when row.length === headers.length; first row ≠ headers → header_mismatch; a short row → invalid_values; only header row (length 1) → invalid_values
    - buildManualHoldRow({hold_id:'H-20260923-M123', sku:'K1', product_name:'X', qty:2, notes:'n'}, '2026-09-23T00:00:00.000Z') → ['H-20260923-M123','', 'K1','X',2,'pending','2026-09-23T00:00:00.000Z','','','n']; bad hold_id / empty sku / qty 0 → {ok:false, error:'invalid_hold'}
    - validateScheduleSlotRows([['2026-10-01','10:00 AM','available']]) → ok; bad date '2026-1-1', status 'booked', empty time, >1000 rows → invalid_rows
    - planScheduleSlotUpdates([{row:2,status:'blocked'},{row:3,status:'available'}], {2:'Booked', 3:'blocked'}) → {writes:[{row:3,status:'available'}], skipped:[2]}; status 'booked' in an update → invalid_updates
    - Source shape: handleReadAction contains `case 'get_ingredients'`; getIngredients and getHomepage use getDisplayValues(); `var INGREDIENTS_SHEET_NAME = 'Ingredients';` exists
  </behavior>
  <action>
Write the test suite first (RED), then implement (GREEN). All helpers are PURE top-level functions (no SpreadsheetApp/LockService/CacheService references — assert this in the suite).

1. Add `var INGREDIENTS_SHEET_NAME = 'Ingredients';` to the constants block beside KITS_SHEET_NAME.
2. Add `function getIngredients()` mirroring getKits (getActiveSpreadsheet → getSheetByName(INGREDIENTS_SHEET_NAME) → `{ values: [] }` if missing) but returning `{ values: sheet.getDataRange().getDisplayValues() }` — display values match the Sheets API FORMATTED_VALUE read admin.js used (per D-15 "preserving the data shape"). Register `case 'get_ingredients': return { ok: true, data: getIngredients() };` in handleReadAction next to get_kits.
3. Change getHomepage's `getValues()` to `getDisplayValues()` (same parity reason: loadHomepageData reads news dates as strings). Before editing, `grep -rn "get_homepage\|getHomepage" js/ zoho-middleware/ apps-script/ tests/ --include=*.js --include=*.gs` and confirm there is no other caller whose behaviour depends on Date objects; record the grep result in the SUMMARY.
4. Add the pure helpers named in <behavior> exactly: resolveInventorySheetName(key), validateInventoryCellUpdates(headers, lastRow, updates), validateAppendRow(values, headers), validateKitsImport(headers, values), buildManualHoldRow(payload, nowIso), validateScheduleSlotRows(rows), planScheduleSlotUpdates(updates, currentStatusByRow). Error objects use the error codes in the <interfaces> contract. Limits: 500 cell updates, 1000 schedule rows/updates, 2001 import rows.

Test file: loader/slicer copied from adminapi-giftcard-ledger.test.js; factory exposes the 7 helpers via typeof guards.
  </action>
  <verify>
    <automated>npx jest tests/frontend/adminapi-phase82-inventory-actions.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case 'get_ingredients'" apps-script/adminApi.gs` = 1
    - `grep -n "function getIngredients(" apps-script/adminApi.gs` returns 1 line and the getIngredients slice contains `getDisplayValues()`
    - All 7 helper names each appear exactly once as `function <name>(` in adminApi.gs
    - Suite passes, including the purity assertion over all 7 helpers
  </acceptance_criteria>
  <done>Reads and pure validation logic for every new action exist and are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the 6 write actions into doPost (server_token) + delete get_config, update_schedule, update_kits</name>
  <files>apps-script/adminApi.gs, tests/frontend/adminapi-phase82-inventory-actions.test.js</files>
  <read_first>
    - apps-script/adminApi.gs doPost (server_token branch incl. the 82-02 "Phase 82 D-11" block), staff switch, getConfig, updateSchedule, updateKits, checkAuthorization (to confirm CONFIG_SHEET_NAME is still used)
    - tests/frontend/adminapi-phase82-dispatch.test.js (82-02's server_token sub-block slicing approach — reuse the same technique)
  </read_first>
  <behavior>
    - Source shape: the doPost server_token sub-block contains `action === '<name>'` for update_inventory_cells, append_inventory_row, import_kits, add_hold, append_schedule_slots, update_schedule_slots
    - Source shape: each of updateInventoryCells, appendInventoryRow, importKits, addManualHold, appendScheduleSlots, updateScheduleSlots contains `acquireScriptLock(15000)` and `lock.releaseLock()` inside a finally, and calls its pure validator before any `setValue`/`setValues`/`appendRow`
    - Source shape: the staff switch does NOT contain the 6 new action names (server_token only — no browser can reach them directly)
    - Deletion: adminApi.gs contains none of `case 'get_config'`, `function getConfig(`, `case 'update_schedule'`, `function updateSchedule(`, `case 'update_kits'`, `function updateKits(`
    - Retention: `case 'check_auth'` and `case 'get_homepage'` still exist
    - Whole file still evaluates via `new Function(src)`
  </behavior>
  <action>
Tests first (extend the same new suite), then implement.

1. Impure wrappers (each: resolve sheet → return {ok:false,error:'invalid_sheet'} if null → `var lock = acquireScriptLock(15000); try { read headers/lastRow; run pure validator; return its error unchanged if !ok; perform writes; return success } finally { lock.releaseLock(); }`). Sanitize every string value with sanitizeInput before writing (numbers untouched):
   - updateInventoryCells(payload): headers = sheet.getRange(1,1,1,sheet.getLastColumn()).getValues()[0]; lastRow = sheet.getLastRow(); writes via sheet.getRange(row, col).setValue(value). Return {ok:true, count}.
   - appendInventoryRow(payload): sheet.appendRow(values padded with '' to header length). Return {ok:true}.
   - importKits(payload): Kits headers; validateKitsImport; sheet.getRange(1,1,values.length,headers.length).setValues(sanitized values). Return {ok:true, rows}.
   - addManualHold(payload): buildManualHoldRow(payload, new Date().toISOString()) on HOLDS_SHEET_NAME; appendRow. Return {ok:true, hold_id}.
   - appendScheduleSlots(payload): validateScheduleSlotRows; sheet.getRange(sheet.getLastRow()+1, 1, rows.length, 3).setValues(rows). Return {ok:true, count}.
   - updateScheduleSlots(payload): status column from header row ('status'); build currentStatusByRow by reading each requested row's status cell (or one getRange over min..max rows); planScheduleSlotUpdates; setValue per write. Return {ok:true, updated, skipped}.
2. In doPost's server_token branch, directly after the 82-02 "Phase 82 D-11" block and before the `Unknown server action` return, add a block "Admin panel inventory/schedule actions (Phase 82 D-21)" with six `if (action === '...') { return _jsonResponse(<wrapper>(payload)); }` entries. Do NOT add them to the staff switch.
3. Deletions (D-02 amended by D-21). FIRST run `grep -rn "get_config\|getConfig\|update_schedule\|updateSchedule\|update_kits\|updateKits" js/ zoho-middleware/ apps-script/ tests/ *.html` (exclude node_modules and *.min.js) and paste the output into the SUMMARY. Only if every hit is the definition/case itself (or a comment), delete: `case 'get_config'` + `function getConfig`; staff-switch `case 'update_schedule'` + `function updateSchedule`; staff-switch `case 'update_kits'` + `function updateKits`. If any live caller appears, STOP that deletion and record it. Keep CONFIG_SHEET_NAME (checkAuthorization uses it). Keep `case 'check_auth'` and add a one-line comment above it: "Phase 82: browser caller removed at cutover; kept until the old admin.js is gone from prod (D-16/D-19) — delete in a later phase."
4. Add a header comment above getIngredients noting the milestone gap (D-21): Ingredients/Schedule/Homepage have no destination in Phases 83-88.

Record in the SUMMARY: "ROADMAP criterion 2's '7 zero-caller actions' is corrected: D-02 said 4; D-21 keeps get_homepage (now has a caller) → 3 deleted (get_config, update_schedule, update_kits); update_kits/update_schedule are superseded by update_inventory_cells / append_schedule_slots / update_schedule_slots."
  </action>
  <verify>
    <automated>npx jest tests/frontend/adminapi-phase82-inventory-actions.test.js tests/frontend/adminapi-phase82-dispatch.test.js tests/frontend/adminapi-phase82-locks.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "case 'get_config'|function getConfig\(|case 'update_schedule'|function updateSchedule\(|case 'update_kits'|function updateKits\(" apps-script/adminApi.gs` = 0
    - `grep -c "case 'check_auth'" apps-script/adminApi.gs` = 1 and `grep -c "case 'get_homepage'" apps-script/adminApi.gs` = 1
    - The six wrapper functions each exist once and each contains `acquireScriptLock(15000)`
    - `npm test` passes; `git diff --name-only tests/` lists only new files
    - SUMMARY contains the pre-deletion grep output and the 7→3 correction note
  </acceptance_criteria>
  <done>Every admin.js direct-Sheets flow has a typed, locked Apps Script action; only genuinely dead actions are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Middleware (server_token) → Apps Script writes | Payload fields originate from a staff browser, relayed by the proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03-01 | Elevation of Privilege | update_inventory_cells used as a generic sheet writer | mitigate | sheet restricted by resolveInventorySheetName to Kits/Ingredients; fields must be existing header names; rows 2..lastRow only; header row never writable (D-21 "never a generic cell writer") |
| T-82-03-02 | Tampering | Stored XSS via sheet cells rendered in admin/public pages | mitigate | Every string value passes sanitizeInput before setValue/appendRow/setValues (same control updateKits/updateHomepage use today) |
| T-82-03-03 | Tampering | import_kits overwriting the Kits sheet with a misaligned table | mitigate | values[0] must equal the live header row and every row must match header length (header_mismatch / invalid_values) |
| T-82-03-04 | Tampering | Blocking a booked appointment slot | mitigate | update_schedule_slots re-reads current status under the lock and skips 'booked' rows |
| T-82-03-05 | Denial of Service | Oversized batches hold the script lock | mitigate | Hard caps: 500 cell updates, 1000 schedule rows, 2001 import rows; validation happens before writes |
| T-82-03-06 | Tampering | Row-index addressing after a concurrent insert/delete shifts rows | accept | Parity with today's direct Sheets API writes (D-21); nothing deletes Kits/Ingredients rows in place (deleteIngredient clears cells) |
| T-82-03-07 | Elevation of Privilege | New actions reachable by a staff OAuth token without the proxy | mitigate | Added to the server_token branch only, never the staff switch |
</threat_model>

<verification>
- The three phase-82 Apps Script suites pass; full `npm test` and `cd zoho-middleware && npm test` green; `npm run lint` clean
- Deletion grep evidence recorded in SUMMARY
</verification>

<success_criteria>
adminApi.gs exposes get_ingredients plus 6 typed locked write actions and has lost exactly get_config, update_schedule, update_kits; still safe to redeploy ahead of the browser cutover (D-19).
</success_criteria>

<output>
Create `.planning/phases/82-store-agnostic-prerequisites/82-03-SUMMARY.md` when done
</output>
